# JOSCM Tithes App — Frontend Context (`CLAUDE_CLIENT.md`)

> This file is layer-specific context for the React frontend. It is loaded alongside `CLAUDE.md` whenever I work on UI, components, routing, state management, or anything client-side.
>
> Domain knowledge (roles, business rules, status flows) lives in `CLAUDE.md` — do not duplicate it here.

---

## 1. Tech Stack

| Layer | Choice | Why |
|---|---|---|
| Build tool | **Vite** | Fast HMR, minimal config, standard for React in 2026 |
| UI framework | **React 19** (JavaScript, not TypeScript) | Intermediate dev comfort, faster iteration |
| Styling | **Tailwind CSS v4** | Utility-first, pairs with shadcn, single source of truth for design tokens |
| Component library | **shadcn/ui** (Slate base color) | Copy-paste components I own, not a black-box library |
| Routing | **React Router v6** | Outlet-based nested layouts |
| HTTP client | **fetch** (`apiFetch`) | `credentials: 'include'` for cookie auth + single-flight 401 refresh |
| Global state | **React Context API** | Auth only — no Redux, no Zustand until genuinely needed |
| Icons | **lucide-react** | Consistent icon set, tree-shakeable |
| Notifications | **sonner** (via shadcn) | Modern toast library |

**Package manager:** npm

**Backend base URL:** `http://localhost:7001/api`

---

## 2. Project Structure

Scalable structure chosen deliberately. Grow into it, don't fight it.

```
tithes/
├── public/
├── src/
│   ├── components/
│   │   ├── layout/              Shell: Sidebar, Header, Layout
│   │   │   ├── Layout.jsx
│   │   │   ├── Sidebar.jsx
│   │   │   └── Header.jsx
│   │   ├── ui/                  shadcn components (managed by CLI, minimal edits)
│   │   └── shared/              Reusable building blocks I built
│   │       ├── PageHeader.jsx
│   │       ├── StatusBadge.jsx
│   │       ├── StatCard.jsx
│   │       ├── DataTable.jsx
│   │       ├── EmptyState.jsx
│   │       ├── ConfirmDialog.jsx
│   │       └── FormField.jsx
│   │
│   ├── features/                Feature-specific logic + components
│   │   ├── tithes/
│   │   │   ├── TithesTable.jsx
│   │   │   ├── TithesForm.jsx
│   │   │   ├── DenominationInput.jsx
│   │   │   └── useTithes.js     (custom hook for fetching + state)
│   │   ├── request-form/
│   │   │   ├── RFTable.jsx
│   │   │   ├── RFStatusStepper.jsx
│   │   │   ├── RFCreateForm.jsx
│   │   │   └── useRequestForm.js
│   │   ├── voucher/
│   │   ├── expense/
│   │   ├── reports/
│   │   └── admin/
│   │       ├── users/
│   │       └── categories/
│   │
│   ├── pages/                   Thin "assemblers" — mostly just import features
│   │   ├── Login.jsx
│   │   ├── Dashboard.jsx
│   │   ├── TithesPage.jsx
│   │   ├── RequestFormPage.jsx
│   │   ├── VoucherPage.jsx
│   │   ├── ExpensePage.jsx
│   │   ├── ReportsPage.jsx
│   │   └── admin/
│   │       ├── UsersPage.jsx
│   │       └── CategoriesPage.jsx
│   │
│   ├── routes/
│   │   ├── AppRoutes.jsx
│   │   └── ProtectedRoute.jsx
│   │
│   ├── context/
│   │   └── AuthContext.jsx
│   │
│   ├── hooks/
│   │   └── useAuth.js
│   │
│   ├── services/
│   │   └── api.js               Single Axios instance + all API call functions
│   │
│   ├── lib/                     Pure helpers, no React
│   │   ├── utils.js             (created by shadcn — holds cn() helper)
│   │   ├── formatters.js        formatCurrency, formatDate, formatRelativeTime
│   │   ├── constants.js         STATUS_STYLES, ROLES, SERVICE_TYPES, DENOMINATIONS
│   │   └── validators.js        Form validation helpers
│   │
│   ├── utils/
│   │   └── rolePermissions.js   Sidebar + route access config
│   │
│   ├── App.jsx
│   ├── main.jsx
│   └── index.css
│
├── .env
├── index.html
├── jsconfig.json
├── package.json
├── tailwind.config.js
└── vite.config.js
```

### 2.1 The pages/ vs features/ rule

- `pages/` files should be **thin**. Under 50 lines ideally. They import from `features/` and assemble the page.
- `features/` holds the real logic: data fetching, forms, tables, modals, custom hooks.
- **If a page file crosses 100 lines, extract components into `features/<name>/`.** No exceptions.

### 2.2 The components/shared vs features rule

- If a component is used in **two or more features**, it belongs in `components/shared/`.
- If a component is used in **one feature only**, it stays in `features/<name>/`.
- Don't preemptively put things in `shared/`. Promote them when the second usage appears.

---

## 3. Design System

Target vibe: **corporate professional** — Linear, Mercury, Wise, Stripe Dashboard. Boring on purpose. Trustworthy. Data-dense but not cluttered.

### 3.1 Color Palette

Base gray family: **slate** (cool-toned, modern, professional).

Primary accent: **[DECIDE BEFORE FIRST PAGE]**

Three finalists — pick one and delete the rest:

- `indigo-600` — modern, Linear-like, slightly playful
- `blue-900` — traditional banking, conservative
- `slate-900` — ultra-minimal monochrome, ultra-serious

Once chosen, update `src/index.css` to set the shadcn `--primary` CSS variable and do not use other accent colors anywhere in the app.

**Semantic status colors** (used by `StatusBadge`):

```js
// src/lib/constants.js
export const STATUS_STYLES = {
  pending:         { label: 'Pending',         class: 'bg-amber-50 text-amber-700 ring-amber-600/20' },
  approved:        { label: 'Approved',        class: 'bg-emerald-50 text-emerald-700 ring-emerald-600/20' },
  rejected:        { label: 'Rejected',        class: 'bg-red-50 text-red-700 ring-red-600/20' },
  draft:           { label: 'Draft',           class: 'bg-slate-50 text-slate-600 ring-slate-500/20' },
  submitted:       { label: 'Submitted',       class: 'bg-blue-50 text-blue-700 ring-blue-600/20' },
  for_approval:    { label: 'For Approval',    class: 'bg-violet-50 text-violet-700 ring-violet-600/20' },
  voucher_created: { label: 'Voucher Created', class: 'bg-orange-50 text-orange-700 ring-orange-600/20' },
  disbursed:       { label: 'Disbursed',       class: 'bg-cyan-50 text-cyan-700 ring-cyan-600/20' },
  received:        { label: 'Received',        class: 'bg-teal-50 text-teal-700 ring-teal-600/20' },
}
```

Pattern used: `bg-{color}-50 text-{color}-700 ring-{color}-600/20`. This is the "subtle badge" style from Stripe and Linear — light bg, dark text, faint ring. **Do not use solid-color badges.** They look amateur in financial apps.

### 3.2 Typography

- **Font family:** Inter (load via Google Fonts or `fontsource`)
- **Base size:** 14px (`text-sm` as default in data-heavy views)
- **Numbers in tables:** use `tabular-nums` utility class — `font-variant-numeric: tabular-nums` — so digits align vertically in columns. Non-negotiable for money displays.
- **Font weights:** 400 (normal), 500 (medium — labels, emphasized text), 600 (semibold — headings). Avoid 700 (bold) except for page titles.

### 3.3 Spacing

- **Form fields:** `space-y-2` between label and input, `space-y-4` between fields, `space-y-6` between sections.
- **Cards:** `p-6` padding by default, `p-4` on mobile.
- **Page container:** `p-4 md:p-6 lg:p-8` progressive padding.
- **Table rows:** `py-3 px-4` comfortable density. Not too tight, not too airy.

### 3.4 Shadows and Borders

- **Default:** `border border-slate-200` — thin border, no shadow
- **Elevated (modals, popovers):** `shadow-sm` max. Never `shadow-lg` or higher.
- **No gradients anywhere.** This is a financial app, not a landing page.

### 3.5 Reading "Corporate Pro" in Practice

When in doubt about a styling decision, ask: *"Would this look out of place on the Stripe Dashboard?"* If yes, simplify.

---

## 4. Routing

```jsx
// src/routes/AppRoutes.jsx
<Routes>
  <Route path="/login" element={<Login />} />
  <Route path="/forgot-password" element={<ForgotPassword />} />  {/* public */}
  <Route path="/reset-password" element={<ResetPassword />} />    {/* public, ?token= */}

  <Route element={<ProtectedRoute />}>
    <Route element={<Layout />}>
      <Route path="/dashboard" element={<Dashboard />} />
      <Route path="/tithes" element={<TithesPage />} />
      <Route path="/request-form" element={<RequestFormPage />} />
      <Route path="/reports" element={<ReportsPage />} />

      <Route element={<ProtectedRoute allowedRoles={['admin','do','validator','auditor']} />}>
        <Route path="/voucher" element={<VoucherPage />} />
      </Route>

      <Route element={<ProtectedRoute allowedRoles={['admin','auditor']} />}>
        <Route path="/expense" element={<ExpensePage />} />
      </Route>

      <Route element={<ProtectedRoute allowedRoles={['admin']} />}>
        <Route path="/admin/users" element={<UsersPage />} />
        <Route path="/admin/categories" element={<CategoriesPage />} />
      </Route>
    </Route>
  </Route>

  <Route path="*" element={<Navigate to="/dashboard" replace />} />
</Routes>
```

**Rules:**

- All authenticated routes go through `<ProtectedRoute />` which reads `user` from `AuthContext` and redirects to `/login` if there is no user.
- Role-gated routes use nested `<ProtectedRoute allowedRoles={...} />`.
- The `<Layout />` wraps everything authenticated — it provides the sidebar, header, and `<Outlet />`.

---

## 5. Auth Flow

**Auth model:** the JWT lives in an **httpOnly cookie** the browser manages — JS never reads or stores the token (XSS can't steal it). `AuthContext` only keeps the non-sensitive `user` profile (in `localStorage`) for UI/role rendering. The cookie is the real source of auth truth; a 401 from the server is what actually ends a session.

### 5.1 AuthContext

```jsx
// src/context/AuthContext.jsx — no token state; user only
const login = (userData) => {
  setUser(userData)
  localStorage.setItem('user', JSON.stringify(userData))
}

const logout = () => {
  setUser(null)
  localStorage.removeItem('user')
  apiFetch('/auth/logout', { method: 'POST' }).catch(() => {}) // clears httpOnly cookies
}
```

**Important:** the `isLoading` flag is critical. Without it, on page refresh, `ProtectedRoute` briefly sees `user = null` and redirects to `/login` before rehydration finishes. Always gate the redirect behind `!isLoading`.

### 5.2 api.js — cookie + silent refresh

`apiFetch` (plain `fetch`, **not axios**) sends `credentials: 'include'` on every call so the cookie rides along. On a `401` it performs a **single-flight** `POST /auth/refresh` (the access token is short-lived, 15m; refresh token 7d), then replays the original request once. If the refresh fails, it clears `user` and redirects to `/login`.

```js
// src/services/api.js (shape)
const res = await fetch(`${BASE_URL}${path}`, { ...opts, headers, credentials: 'include' })
if (res.status === 401) {
  if (!_retry && !path.startsWith('/auth/') && await refreshSession()) {
    return apiFetch(path, { ...options, _retry: true }) // replay once
  }
  endSession() // clear user + redirect /login
}
```

A legacy `Authorization: Bearer` header is still sent if a token happens to sit in `localStorage` (dev-login only) — prod relies purely on the cookie. `socket.js` connects with `withCredentials: true` so the same cookie authenticates the websocket handshake.

---

## 5c. Theming (dark mode)

App-wide light/dark via Tailwind v4's `@custom-variant dark (&:is(.dark *))` (see `index.css`). The `:root` / `.dark` blocks define every shadcn token (`--background`, `--card`, `--muted`, `--border`, `--sidebar`, `--chart-*`).

- **State:** `ThemeContext` (`useTheme()` → `{ theme, isDark, toggleTheme }`) adds/removes `.dark` on `<html>` and persists `localStorage.theme`. Defaults to the OS preference (`prefers-color-scheme`) until the user toggles, then their choice wins.
- **No flash:** an inline script in `index.html` `<head>` sets `.dark` before first paint.
- **Toggle:** `DarkModeToggle` lives in the sidebar footer as an on/off **switch** (sliding pill with a Sun/Moon knob) labelled `You're in light/dark mode` — switch-only on the collapsed rail, label + switch expanded / mobile. The sidebar footer also holds the user's avatar + name with **settings** (→ `/profile`) and **logout** quick-action buttons, sitting above the divider/theme row. The church logo, not the profile, is what sits in `SideBarHeader`.
- **Rule for new UI:** use **semantic tokens** (`bg-card`, `bg-background`, `bg-muted`, `text-foreground`, `text-muted-foreground`, `border-border`, `bg-primary`) — NOT hardcoded `bg-white`/`bg-gray-*`/`bg-black`, which don't react to `.dark`. For colored status chips, always pair the light class with a dark variant, e.g. `bg-green-100 text-green-700 dark:bg-green-500/15 dark:text-green-400`. The brand teal `#2f6a7a` is intentionally kept in both themes.

---

## 6. Shared Components (build in this order)

These are the reusable building blocks. **Build them before building feature pages.** Every time I resist this and "just put it in the page for now," I regret it within two weeks.

### 6.1 `<StatusBadge status="pending" />`

Reads from `STATUS_STYLES` in `lib/constants.js`. Single source of truth for every status pill in the app. Used on Tithes list, RF list, Voucher list, Dashboard widgets.

### 6.2 `<PageHeader title="Tithes" description="..." actions={<Button />} />`

Every page has a header. Standardize it: title, optional description, optional action buttons on the right. Prevents every page from inventing its own header layout.

### 6.3 `<StatCard label="Total Tithes" value="₱125,430" trend="+12%" icon={Coins} />`

Dashboard summary cards. Takes a label, a primary value, an optional trend indicator, and an icon.

### 6.4 `<DataTable columns={...} data={...} />`

Wraps shadcn's Table component with sorting, pagination, empty state, and loading state built in. Used on every list page.

### 6.5 `<EmptyState icon={Inbox} title="No tithes yet" description="..." action={<Button />} />`

Shown when a list is empty. Never let a user see a blank page — always explain why it's empty and what to do next.

### 6.6 `<ConfirmDialog open={...} onConfirm={...} title="..." description="..." />`

For approve / reject / delete actions. Wraps shadcn's AlertDialog. All destructive or irreversible actions must go through this.

### 6.7 `<FormField label="..." htmlFor="..." error="...">`

Wraps Label + Input + error message. Ensures every form field in the app has identical spacing and error display.

---

## 7. Data Fetching Pattern

No React Query, no SWR — plain `useEffect` + custom hooks per feature. This is simpler for an intermediate dev, and we can migrate later if needed.

```js
// src/features/tithes/useTithes.js
export function useTithes(params) {
  const [data, setData] = useState([])
  const [isLoading, setIsLoading] = useState(true)
  const [error, setError] = useState(null)

  const refetch = useCallback(async () => {
    setIsLoading(true)
    setError(null)
    try {
      const res = await getTithes(params)
      setData(res.data)
    } catch (err) {
      setError(err)
    } finally {
      setIsLoading(false)
    }
  }, [JSON.stringify(params)])

  useEffect(() => { refetch() }, [refetch])

  return { data, isLoading, error, refetch }
}
```

**Every list page gets one of these hooks.** The page component stays clean:

```jsx
function TithesPage() {
  const { data, isLoading, error, refetch } = useTithes({ status: 'pending' })
  // ... render
}
```

---

## 8. Loading, Error, and Empty States

**Every data-fetching page must handle all three states.** This is the #1 thing I forget as an intermediate dev.

- **Loading:** show skeleton loaders (shadcn's `<Skeleton />`) that match the shape of the eventual content. Do not show a spinner alone — it's jarring.
- **Error:** show an inline error card with a retry button. Use the `Alert` component from shadcn. Never leave the user with a blank page.
- **Empty:** show the `<EmptyState />` component. Explain why it's empty and what to do.

Loading → Error check → Empty check → Actual content. In that order, always.

---

## 9. Mobile Strategy

Target users by device:

- **Officers** (DO, Validator, Pastor, Auditor, Admin) — desktop-first. Data-heavy pages, tables, exports. Optimize for laptop.
- **Members** (Berna, Lourdes, Kiya) — mobile-first. Tithes submission and RF creation will mostly happen on phones after Sunday service.

### 9.1 Responsive Approach

Tailwind is mobile-first by syntax. Default classes = mobile. Prefix with `md:` (768px+) or `lg:` (1024px+) for larger screens.

- **Tables** on desktop become **card stacks** on mobile via `md:hidden` / `hidden md:block`.
- **Sidebar** is a `<Sheet />` drawer on mobile (`< md`), an **icon-only rail** on tablet (`md` to `xl`, `w-16`, labels + footer hidden, logo centered), and a **full column** on desktop (`xl+`, `w-96`, labels + name/role + footer visible). The breakpoint for the expanded form is `xl` (not `lg`) so iPad Pro portrait (1024px) and similar mid-size devices stay on the rail variant — at `lg`, `w-96` flex-shrinks awkwardly because the available main width is too narrow.
- **Bottom nav** (`components/layout/BottomNav.jsx`) is a `md:hidden` **floating frosted-glass** bottom tab bar shown only on mobile. It always renders the user's **first 3 role pages** (from `getNavItemsForRole(role)`) as tabs **plus a "More" tab for every role** that opens the `MobileSidebar` Sheet (`onOpenMore` → `setSidebarOpen(true)`). More is always present so even members/pastor (≤4 pages) can reach the **dark-mode toggle** (which lives in the drawer); pages beyond the first 3 are reached via More. Tabs use `py-3` + 22px icons + 11px labels for comfortable touch targets. Glass styling: floats with `left-3 right-3 bottom-[calc(0.75rem+env(safe-area-inset-bottom))]`, `rounded-2xl`, a defined `border-gray-200 dark:border-white/10` (keeps the glass edge from looking harsh), `overflow-hidden` (clips blur to corners), `bg-white/60 dark:bg-card/50 backdrop-blur-xl backdrop-saturate-150` + `shadow-lg`. **For the blur to actually read as glass, page content must scroll *behind* the bar** — so clearance padding lives on each page's scroll container (`pb-[calc(6rem+env(safe-area-inset-bottom))] md:pb-0` on the shared `flex-1 min-h-0 ... overflow-auto` wrapper in every `src/pages/*.jsx`), NOT on the Layout content column. Putting the padding on the outer column instead leaves a solid dead band behind the bar (no glass effect) and visible empty space — avoid that.
- **Mobile header menu trigger:** on mobile (`md:hidden`) the old hamburger icon is replaced by the **church logo** (Cloudinary `joscm-logo`), which is still a button wired to `onOpenSidebar` — so tapping the logo opens the drawer (preserves drawer + dark-mode access for member/pastor, who have no "More" tab). Desktop is unchanged.
- **Forms** stack to single column on mobile, can go two-column on desktop (`grid grid-cols-1 md:grid-cols-2`).

### 9.2 Device Testing

Before marking a page "done," test in Chrome DevTools device toolbar at:

- iPhone 14 (390px)
- iPad (768px)
- Desktop (1440px)

If any of the three look broken, it's not done.

---

## 10. File Downloads (Excel / PDF)

Reports export as binary files. Pattern:

```jsx
const handleExportExcel = async () => {
  try {
    const response = await exportTithesExcel()
    const url = URL.createObjectURL(new Blob([response.data]))
    const link = document.createElement('a')
    link.href = url
    link.download = `tithes-report-${new Date().toISOString().split('T')[0]}.xlsx`
    link.click()
    URL.revokeObjectURL(url)
  } catch (err) {
    toast.error('Failed to export report')
  }
}
```

**Always include:**

1. Date in the filename (audit trail)
2. `URL.revokeObjectURL` to prevent memory leaks
3. Error handling with toast
4. The request itself must set `responseType: 'blob'` in `api.js`

---

## 11. Anti-Patterns to Reject

If I write any of these, Claude should stop me:

- **Hardcoded status colors in a component.** Always use `STATUS_STYLES` from `lib/constants.js`.
- **Inline API calls in components.** Always go through `services/api.js`.
- **Duplicate fetching logic.** Extract to a custom hook in `features/<name>/use<Name>.js`.
- **Direct `localStorage` access in components.** Go through `AuthContext`.
- **Hardcoded role strings.** Use the `ROLES` constant from `lib/constants.js`.
- **`any` escape hatches** (in TypeScript — but this project is JS, so N/A for now).
- **Pages over 100 lines.** Extract to `features/`.
- **Custom Tailwind classes in components/ui/.** Don't edit shadcn components directly for styling — customize via `className` prop at the usage site.
- **`console.log` in committed code.** Remove before commit.
- **Blank catches** (`catch (err) {}`). Always handle or re-throw.

---

## 12. Pages Roadmap

Build in this order — each one teaches the next.

1. **Login** — teaches form basics, AuthContext usage, shadcn Card pattern
2. **Layout shell** — Sidebar + Header + Outlet, teaches routing and role-filtered nav
3. **Dashboard (admin view first)** — teaches StatCard, data fetching pattern, loading states
4. **Tithes List** — teaches DataTable, StatusBadge, filtering, mobile card fallback
5. **Tithes Form** — teaches DenominationInput, computed totals, form validation
6. **Request Form List** — teaches complex status flow display, RFStatusStepper
7. **Request Form Create** — teaches multi-step form, file upload
8. **Voucher List** — similar to Tithes List, good reuse practice
9. **Expense List** — teaches manual entry dialog, category filtering
10. **Reports** — teaches date range pickers, export buttons, tabs
11. **User Management** — teaches CRUD modal pattern
12. **Categories** — simplest CRUD, good refactor opportunity

Don't skip ahead. Each page builds on patterns from the previous ones.

---

## 13. Established Page Pattern (Tithes / RF / Voucher — follow for every new list page)

This is the real-world pattern the codebase has converged on. **When building a new feature page, don't reinvent the layout — mirror this.**

### 13.1 Folder layout per feature

Actual folder convention (supersedes the planned `features/<name>/` in §2):

```
src/components/<feature>-components/
  mockData.js                 ← mock arrays + statusConfig + formatPHP/formatDate/formatDateTime
  <Feature>SummaryStats.jsx   ← Card with CardHeader + CardContent grid of StatTile
  <Feature>Table.jsx          ← Card: header with filters, CardContent with Table, CardFooter with pagination
  <Feature>DetailsDialog.jsx  ← read-only details modal
  Create<Feature>Dialog.jsx   ← create/submit modal
  (optional) <Feature>PipelineTracker.jsx, PendingRfsCard.jsx, RejectDialog.jsx, etc.

src/pages/<Feature>.jsx       ← thin assembly (~30–50 lines): useState hoist, import components, layout
```

### 13.2 Page shell (copy verbatim)

```jsx
<div className="w-full flex-1 min-h-0 flex flex-col gap-5 overflow-auto">
  {/* Header row: title block on left, CTA on right */}
  <div className="flex items-center justify-between">
    <div>
      <h1 className="text-2xl font-semibold">Page Title</h1>
      <p className="text-sm text-muted-foreground">One-line description.</p>
    </div>
    <div className="w-40" onClick={() => launch()}>
      <CustomButton titleName="Create X" icon={GoPlus} />
    </div>
  </div>

  <FeatureSummaryStats />
  {/* optional: pipeline tracker, pending-action card */}
  <div className="h-[32rem]">
    <FeatureTable onViewItem={setViewing} />
  </div>

  <FeatureDetailsDialog item={viewing} open={!!viewing} onOpenChange={...} />
</div>
```

- **Gap rule:** `gap-5` between page sections, never mix.
- **Table height:** fixed `h-[32rem]` wrapper so rows scroll internally, not the whole page.
- **CTA button:** use the custom `@/components/Buttons.jsx` (react-icons `GoPlus`) wrapped in `<div className="w-40" onClick>`. Cancel/Submit inside dialogs still use shadcn `Button` for `variant`/`disabled` support.
- **Custom hook for dialog state** when the dialog needs to be launched from multiple places (CTA + pending card): hoist `open` and `preselectedId` to the page, pass as controlled props.

### 13.3 Modal (Dialog) pattern

```jsx
<DialogContent className="max-w-2xl max-h-[90vh] overflow-y-auto overflow-x-hidden">
  <DialogHeader>
    <DialogTitle>Submit New X</DialogTitle>
    <DialogDescription>One-line context.</DialogDescription>
  </DialogHeader>
  <form className="space-y-4"> ... </form>
</DialogContent>
```

- `overflow-x-hidden` is **required** — without it the `DialogFooter -mx-4` bleed causes a horizontal scrollbar when vertical scroll appears.
- `DialogTitle` default styling (`text-lg font-semibold`) is set in `components/ui/dialog.jsx`; do not override per modal.
- Form sections use `space-y-4`; label+input pairs use `space-y-1.5`; two-column rows use `grid grid-cols-2 gap-3`.
- File upload uses a **dropzone label** (hidden `<input type="file">`) + a list of uploaded file chips with remove button. No URL-string inputs for attachments.
- Remarks/notes fields use `<Textarea rows={3}>` (imported from `@/components/ui/textarea`).

### 13.4 Table pattern

- Outer `<Card className="w-full h-full flex flex-col">` so it fills the wrapper.
- `CardHeader` has two rows: title/description row, then filter row (`Input` search + `Select` filters).
- `CardContent className="flex-1 min-h-0 overflow-auto"` with `<TableHeader className="sticky top-0 bg-background z-10">`.
- `CardFooter` has `Showing X–Y of N` on left, Previous/Next pagination on right.
- Row action column uses `DropdownMenu` with a state-aware items array (items vary by row status — see `RfTable.jsx` `ActionMenu`).
- Status badges: `<Badge variant="secondary" className={cfg.color}>{cfg.label}</Badge>` where `cfg` comes from the feature's `statusConfig` in mockData.

### 13.5 Summary stats pattern

- `Card` → `CardHeader` (title + description) → `CardContent className="grid grid-cols-2 lg:grid-cols-4 gap-3">` of `StatTile`.
- Each `StatTile` shows: label + icon top row, big `formatPHP(amount)` value, small `{count} unit` subtext.
- Pastel accent per tile via `accent="bg-blue-50/50"` style classes.
- For fixed-height cards (e.g., Voucher summary), use `h-auto lg:h-80` — hug content on narrow screens, fixed on desktop.

### 13.6 Mock data conventions

Every `mockData.js` exports:

- `mockCategories` (string array)
- `mock<Feature>s` (array of domain objects, with `id`, timestamps, nested `timeline` where applicable)
- `statusConfig` — `{ [status]: { label, color: "bg-X-100 text-X-700", order? } }`
- `formatPHP(n)` — `Intl.NumberFormat("en-PH", { style: "currency", currency: "PHP", maximumFractionDigits: 0 })`
- `formatDate(d)` — `toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" })`
- `formatDateTime(d)` — same + hour/minute

Keep payload shapes close to the backend Mongoose schemas in `CLAUDE.md` so the later swap to real API is a one-line change inside the hook/component.

### 13.7 Select (dropdown) conventions

- **Value must be the user-facing identifier**, not a numeric id. e.g., use `rfNo` (`"RF-0008"`), not `id` (`8`). Otherwise the trigger falls back to showing the raw value after selection.
- For multi-line items, wrap children in `<div className="flex flex-col items-start text-left ...">`. Set `<SelectTrigger className="h-auto py-2">` when items are multi-line.

### 13.8 Absolute rules

- **Never add horizontal scrollbars to modals** — always `overflow-x-hidden` alongside `overflow-y-auto`.
- **Never use solid-color status badges** — always the `bg-X-100 text-X-700` pastel pattern.
- **Never inline status strings in UI** — always go through `statusConfig`.
- **Never duplicate `formatPHP`/`formatDate`** — import from the feature's `mockData.js` (or later, `lib/formatters.js` after migration).
- **TODO comments point to the real API endpoint** (e.g., `// TODO: POST /api/vouchers`). Mock `console.log` payloads stay until the feature is wired to the backend.

---

## 14. Update History

- **2026-07-04** — `feat/presence-dots-everywhere` pushed (frontend-only; extends `feat/online-presence-frontend`). **Green online dot on every avatar across the app, including your own.** Adrian noticed the dot only appeared in the header facepile, not on his own picture (header popover / sidebar / profile) even while online. **`UserAvatar` is now presence-aware**: new optional **`userId`** prop — it calls `usePresence()` and shows the green dot when `onlineIds.has(userId)`. An explicit `online` prop still wins (the facepile), and callers passing *neither* `userId` nor `online` get the original dot-less markup, so nothing else changed. Wired `userId` through: **`UserChip`** (so all table/detail avatars — RfTable/VoucherTable/TithesTable/ExpenseTable + their detail dialogs + RF approval timeline — get dots automatically), **Header** popover + **Sidebar** footer + **Profile** (self, via `user._id ?? user.id` — the auth object stores the id as `id`), **UsersTable** (both rows), **UserDetailsDialog**, **RfComments** (comment author). For **RecentActivity**, `buildActivity` (dashboardUtils) now carries a `userId` per item (parallel to the existing `avatarUrl`, sourced from the same populated actor ref) and `toUserRef` includes it as `_id` so those avatars dot too. Presence ids are Mongo `_id` strings on the wire; the login response exposes the current user's id as `id`, so self-dot uses `user._id ?? user.id`. `vite build` + lint clean. **Pattern:** to show a live per-user indicator on an existing shared leaf, make the leaf read the context by an id prop (`userId`) rather than threading a boolean through every call site — most sites already have the id.

- **2026-07-04** — `feat/online-presence-frontend` pushed (needs backend `Backend-Tithes#feat/online-presence` — **deploy/merge both together**). **"Who's online" facepile in the header + green presence dots on avatars.** Backend is a **heartbeat, not a websocket**, because the prod socket is disabled behind the Vercel proxy (`services/socket.js` returns null when proxied) — so presence had to be plain HTTP. New **`src/context/PresenceContext.jsx`** (`PresenceProvider` + `PresenceContext`, same combined-export shape as NotificationsContext): every 20s it `POST`s `/presence/heartbeat` and stores the returned `online` list; tab-visibility aware (skips while hidden, re-beats on focus/visibility) and clears by unmounting on logout. Exposes `{ onlineUsers, onlineIds:Set }`. **`src/hooks/usePresence.js`** reads it (safe empty default outside the provider). Mounted in **`Layout.jsx`** inside `NotificationsProvider`. **`src/components/layout/OnlinePresence.jsx`** — the facepile: small overlapping (`-space-x-2`) `UserAvatar`s of *other* online users (excludes self), each with a green dot, `+N` overflow past 5, `hidden lg:flex` (mirrors the date label), and renders nothing when nobody else is online; placed just before the date in `Header.jsx`. **`UserAvatar`** gained an optional **`online`** prop → green dot overlay (`bg-green-500 ring-2 ring-white dark:ring-card`); callers that don't pass `online` get byte-identical original markup (gated on `online === undefined`). Verified end-to-end against the local backend (signed a real JWT → heartbeat returned the online user with name/role/avatar, HTTP 200). `vite build` clean; lint adds no new errors beyond the pre-existing context `react-refresh/only-export-components` pattern shared with NotificationsContext/AuthContext. Follow-up idea: reuse the `online` prop + `usePresence().onlineIds` to dot avatars in the Users table / RecentActivity / RF timeline.

- **2026-07-04** — `feat/dashboard-animated-greeting` pushed. **Dashboard hero line is now an animated greeting** (replaces the static `Welcome, {firstName} 👋`). New **`src/components/dashboard-components/AnimatedGreeting.jsx`**: cycles through time/Sunday-aware prefixes every 10s — `Happy Sunday` (only on Sundays) → `Welcome` → `Good morning/afternoon/evening` (by hour) — each typed out char-by-char (`{prefix}, {name}`), followed by a one-shot hand wave 👋. **Structure avoids the repo's `react-hooks/set-state-in-effect` lint rule**: an outer component owns the 10s cycle `index`; an inner `GreetingLine` is remounted via `key={index}` so its `typed`/`waving` state resets on mount (no synchronous setState in an effect — all setState lives inside `setInterval`/`setTimeout` callbacks). **Reduced-motion**: `prefers-reduced-motion` renders the full line statically (no typing, no caret, no wave). **CSS** in `src/index.css`: `@keyframes joscm-wave` (hand rotate, `.joscm-wave`, 1.4s once) + `@keyframes joscm-caret-blink` (`.joscm-caret` while typing). Wired in `Dashboard.jsx` with `min-h-[1.6em]` on the line to prevent layout shift while text is empty at the start of each cycle. Accessibility: full text on `aria-label`, animated spans `aria-hidden`. **Files: 3** (new component, Dashboard wire-in, index.css keyframes). lint + `vite build` clean. First of the requested Dashboard polish items; online-presence facepile (needs Socket.IO presence tracking) is a separate, later feature.

- **2026-07-04** — `fix/tithes-summary-first-on-mobile` pushed. **Reorder Tithes page so `TithesSummary` is the first block on mobile** (members wanted the summary numbers, not the trend chart, at the top on phones) **without changing tablet/desktop.** Reworked `Tithes.jsx`'s root container: DOM order is now header → **Summary → Chart** → Breakdown → Table (Summary moved above the chart in the markup — and `TithesSummary` was pulled out of the old `grid grid-cols-1 lg:grid-cols-2` wrapper and wrapped in a plain `<div>` so its internal `h-full` doesn't stretch against the tall page column). Mobile (`<md`) has no order classes, so it renders in DOM order = Summary-first. **`md:order-{1..5}`** on each block restores the tablet stack (header, Chart, Summary, Breakdown, Table). The root now switches to **`lg:grid lg:grid-cols-2 lg:content-start`**, and `lg:col-span-2` (header, chart, table) + `lg:col-span-1` (summary, breakdown) with the same `md:order-*` values drive grid auto-placement to reproduce the **unchanged desktop layout** (Chart full-width, Summary | Breakdown side-by-side, Table full-width). The outer `gap-5` now supplies both the row and the summary/breakdown column gap (the removed nested grid also used `gap-5`, so spacing is identical). **Files: 1.** lint + `vite build` clean. **Pattern:** to reorder one block per breakpoint while keeping a 2-col desktop row intact, drive MOBILE via DOM order and reconstruct desktop with `lg:grid` + `col-span` + `order` (the pair that must sit side-by-side stays as grid cells, so no duplication and the working breakpoints are untouched). See [[feedback_scope_fix_to_breakpoint]].

- **2026-07-04** — `fix/tithes-chart-mobile-only-height` pushed (**supersedes** the same-day `fix/tithes-trend-chart-height`, PR #105, which overreached). **Tithes-page trend chart was squished on mobile only — desktop was fine.** First attempt (#105) restructured `TithesTrendChart` globally (dropped the `h-72 md:h-96` wrapper, removed `h-full` from the Card, gave `CardContent` a fixed `h-64 md:h-80`) — but that changed **desktop** too (which Adrian had confirmed was fine) and the chart appeared to break/disappear for him. **Corrected approach — touch mobile only:** fully reverted `TithesTrendChart.jsx` back to original (`Card w-full h-full flex flex-col`, `CardContent flex-1 min-h-0`) and made a **single** change in `Tithes.jsx` — the chart wrapper `<div>` went from `h-72 md:h-96` to **`h-[28rem] md:h-96`**. Only the mobile height grows (288px → 448px, giving the `flex-1` plot real room after the header/footer wrap onto extra rows on narrow screens); **`md:h-96` (desktop) is untouched**, so desktop renders exactly as the proven-good original. **Files: 2** (one is a pure revert). lint clean. **Lesson:** when a bug is reported for one breakpoint, scope the fix to that breakpoint (`h-[…] md:…`) — don't restructure shared layout that already works elsewhere.

- **2026-06-08** — `feat/avatar-placement-tweaks` pushed (frontend-only; relies on the already-merged `avatarUrl` populate). **Avatar placement clean-up.** (1) **Sidebar** — profile avatar moved out of `SideBarHeader` (now church-logo only) into a new **`SidebarFooter`** block sitting above the divider: avatar + name + role with **settings** (→ `/profile`) and **logout** icon buttons (follows the `md:hidden xl:*` collapse pattern; mobile sheet closes via `onNavigate`). (2) **`DarkModeToggle`** rebuilt as an on/off **switch** (sliding pill + Sun/Moon knob, `role="switch"`) labelled `You're in light/dark mode`; switch-only on the collapsed rail. (3) **RfDetailsDialog** approval timeline — `TimelineItem`/`UserChip` gained `showAvatar`; **Created + Submitted** rows are now text-only (the requester already shows an avatar in the field above), all other actors keep avatars. (4) **`<UserChip showAvatar />`** prop added (renders name-only when false). (5) **Dashboard Recent Activity** now shows avatars — `buildActivity` adds an `avatarUrl` per item (parallel to the existing `user` name string, which the Dashboard "own" filter still matches on); `RecentActivity` rebuilds a `{name, avatarUrl}` ref via a local `toUserRef` for both the desktop table and mobile cards. **Files: ~7.** `vite build` clean; lint adds no new errors.

- **2026-06-08** — `feat/avatar-in-records-ui` pushed (frontend; backend companion `Backend-Tithes#feat/avatar-in-records` shipped + merged first — added `avatarUrl` to every user-ref `.populate()` that feeds a table/modal). **Avatars next to names across records.** New **`<UserChip user size fallback nameClassName />`** (`components/shared/UserChip.jsx`) — avatar + name, tolerant of a populated object (`{name, avatarUrl}`) / bare string / null. Wired in: **RfTable** (requester column) + **RfDetailsDialog** (Requester field + each "by" actor in the approval timeline, incl. Rejected — `TimelineItem` now takes `byUser` instead of a name string; the old `personName` helper was removed); **VoucherTable** (Created By column) + **VoucherDetailsDialog** (Created By); **TithesTable** (Submitter column, mobile card, and the inline view dialog); **ExpenseTable** (Recorded By column) + **ExpenseDetailsDialog** (Recorded By, linked-RF Requester, Approved By). Compact mobile *subtitles* and dashboard pending-work cards intentionally left text-only (too dense for avatars). Audit Log excluded (its `actorName` is a string snapshot, no live ref). **Files: ~9.** `vite build` clean; lint adds no new errors (pre-existing `set-state-in-effect` focus effects untouched). **Pattern:** render any person ref in a table/modal with `<UserChip>` (it degrades gracefully when the ref isn't populated).

- **2026-06-08** — `feat/avatar-frontend` pushed (frontend; backend companion `Backend-Tithes#feat/avatar-upload` shipped + merged first — adds `avatarUrl`/`avatarPublicId`, `PATCH/DELETE /api/users/me/avatar`, admin `…/admin/users/:id/avatar`, `GET /api/users/me`, `avatarUrl` in login + RF-comment populate). **User profile pictures.** **(1) `src/lib/avatar.js`** (new) — single source for `getInitials` + `colorFromName` (deterministic colored initials so every user has a stable swatch even without a photo) + `validateAvatarFile`/`AVATAR_ACCEPT`/`AVATAR_MAX_BYTES` (≤5MB, images). `users-components/mockData.js` now re-exports `getInitials` from here (call sites unchanged). **(2) `<UserAvatar name src size className />`** (`components/shared/UserAvatar.jsx`, new) — renders the photo when `src` set, else the colored initials circle; `size` ∈ xs/sm/md/lg/xl. The one component reused everywhere. **(3) `/profile` page** (`pages/Profile.jsx`, new + route in `App.jsx`, all roles) — big avatar, upload/change/remove (FormData → `apiFetch` `PATCH /users/me/avatar`, field `avatar`), account info, and a Change-password button reusing `ChangePasswordDialog`. Reached via a new **"My profile"** item in the Header settings popover. **(4) AuthContext** gained `updateUser(partial)` (merges into user + localStorage, no cache-clear) and a best-effort `GET /users/me` hydrate on mount so pre-existing sessions pick up `avatarUrl`. **(5) Avatars wired into**: Header popover, `SideBarHeader` (desktop sidebar + mobile drawer), `UsersTable` (replaces the gray initials divs), `UserDetailsDialog` (+ **admin** upload/remove via `useUsers.setUserAvatar`/`removeUserAvatar` → `/admin/users/:id/avatar`), and `RfComments` (author avatar from populated `authorId.avatarUrl`). New toast keys `avatarUpdated`/`avatarRemoved`. **Files: ~14.** `vite build` clean; lint adds no new errors (the pre-existing `AuthContext` `only-export-components` + the `Voucher.jsx`/etc. `set-state-in-effect` errors are untouched). **Pattern:** always render an identity with `<UserAvatar>` (never a bespoke initials div); size avatars via its `size` prop.

- **2026-06-02** — `feat/require-expense-particulars` pushed (frontend half; backend enforcement companion `Backend-Tithes#feat/require-expense-particulars` ships after this merges). Makes the **remarks/particulars field required** on the two forms that feed an expense's "Details / Particulars" column in the financial reports — so the new Monthly Breakdown export (Excel + PDF, shipped on the backend `feat/excel-monthly-breakdown` / `feat/pdf-monthly-breakdown`) always shows *what* money was spent on instead of falling back to "—". **(1) `RecordExpenseDialog.jsx`** (manual expense): added `remarks.trim()` to the `canSubmit` gate, marked the `<Textarea>` `required`, relabeled "Remarks" → "Remarks / Particulars", changed the placeholder to a "what was this spent on?" prompt, added a muted helper line ("Required — appears as the expense detail in financial reports"), and the payload now sends `remarks: remarks.trim()` (was `remarks.trim() || undefined`). **(2) `CreateRfDialog.jsx`** (request form — feeds voucher→RF→expense detail): added `!!remarks.trim()` to `canSubmit` (gates **both** Save-as-Draft and Submit-for-Validation, and Save-Changes in edit mode), marked the `<Textarea>` `required`, same relabel/placeholder/helper treatment, and `buildPayload` now sends `remarks: remarks.trim()`. **FE-first ordering** is deliberate: shipping the required field before the backend `400` enforcement means valid submissions never break in the gap between merges. **Files: 2.** Lint clean on both edited files (the repo's 22 pre-existing lint errors in `Voucher.jsx` / `vite.config.js` / etc. are untouched). No new components, no shared-primitive changes.

- **2026-06-02** — `feat/combined-report-and-export-redesign` pushed (frontend; backend companion same branch shipped + merged first). Wires the redesigned/combined report exports into the UI and adds three auditor upgrades. **(1) New "Financial Summary" tab** on Reports, gated by `can.viewCombinedReport` (`['admin','auditor']`, mirrors the backend `authorizeRoles` on `/api/reports/combined*`). `Reports.jsx` adds the 3rd tab (Scale icon) + an `effectiveTab` guard that falls back to "tithes" if combined is selected without permission. When combined, it renders the new `FinancialSummary` component instead of `ReportSummary`/`ReportPreviewTable`. **(2) `useReports`** now handles the combined JSON shape `{ summary, tithes, expenses }` (vs `{ data }`) — exposes a `summary` (null on single-entity tabs), and fires `notifyAction("reportExported")` on a successful download (new `reportExported` toast key); combined downloads are named `financial-summary-report.{xlsx,pdf}`. **(3) `FinancialSummary.jsx`** (new) reuses `ReportSummary`'s StatTile pattern: Total Tithes / Total Expenses / **NET Position** (green if ≥0, red if <0) tiles + an Expenses-by-Category card, driven by `summary`. `ExportBar` gained a `combined` description branch; `Reports.jsx` passes `rowCount = summary.tithesCount + summary.expenseCount` for the combined tab. **(4) Auditor dashboard oversight tile** (`PendingWorkSection.jsx`): `Dashboard.jsx` now passes `expenses` (already returned by `useDashboardData` for auditor); the auditor branch adds three this-month financial stats (Tithes, Expenses, Net — Net colored) using `formatPHP`, and **all** oversight tiles are now clickable deep-links (`navigate` to `/tithes` / `/request-form` / `/voucher` / `/reports`); the stat grid went `lg:grid-cols-7` → `lg:grid-cols-5` to fit 10 tiles in 2 rows. **Files: 8** (`rolePermissions.js`, `lib/toast.js`, `hooks/useReports.js`, `pages/Reports.jsx`, `reports-components/FinancialSummary.jsx` [new], `reports-components/ExportBar.jsx`, `pages/Dashboard.jsx`, `dashboard-components/PendingWorkSection.jsx`). `vite build` clean (10.67s). **Lint note:** the `'Icon' is defined but never used` errors in `FinancialSummary` (and the pre-existing one in `ReportSummary` on main) are a repo eslint-config quirk (JSX-used identifiers aren't counted) — `FinancialSummary` intentionally mirrors `ReportSummary`'s `icon: Icon` convention; `rfBySetStatus` in PendingWorkSection is pre-existing dead code, untouched.

- **2026-06-02** — `fix/dashboard-pending-work-focus-refresh` pushed. Follow-up: after the realtime listener landed, Adrian tested (member submits an RF → validator gets the notification) but "Your Pending Work" still needed a manual refresh on the validator's dashboard. Root cause: the notification **bell** stays fresh via *two* paths in `NotificationsContext` — the socket push (`handleNew`, which dispatches the window `notification:new`) **and** a `focus`/`connect` reconcile (`handleReconcile`, a plain `refetch` that does **not** dispatch the window event). The dashboard only subscribed to the window event, so when the bell updated via the focus-reconcile path (e.g. two tabs on one machine — switching to the validator tab fires `focus`, not a socket push the dashboard hears), the dashboard never refetched. **Fix (`useDashboardData.js`, 1 file):** the realtime `useEffect` now also listens for `window` `focus` and silently refetches, mirroring the bell's reconcile — so the dashboard is at least as fresh as the bell, and also recovers when a socket push is missed (socket dropped / notif arrived while tab backgrounded). `vite build` clean (7.01s); lint clean. **Note:** backend realtime is wired correctly (`app.js` socket auth → `socket.join(String(userId))`, `emitToUser` → `notification:new`); this is purely the dashboard subscribing to the same fallback the bell already had.

- **2026-06-02** — `feat/dashboard-realtime-pending-work` pushed. Adrian noticed the Dashboard's **"Your Pending Work"** section (and stats) didn't update live like the notification bell — it needed a manual refresh. Root cause: realtime delivery works via `NotificationsContext`, which connects the socket and on every `notification:new` push (a) updates the bell list and (b) dispatches a **window** `CustomEvent("notification:new")`. The resource hooks (`useVouchers`/`useRequestForms`/`useTithes`) already listen to that window event and refetch — but **`useDashboardData` never subscribed**, so the Dashboard only loaded on mount. **Fix (`useDashboardData.js`, 1 file):** renamed the fetch to `load(silent = false)` (skips `setLoading` when silent so a realtime refresh doesn't flash skeletons over rendered data), kept `refetch = () => load(false)` for the existing manual/action flows, and added a `useEffect` that listens for the window `notification:new` and calls `load(true)`. Refetches on any notification (the `refModel` enum is only `Tithes`/`RequestForm`/`Voucher`, all of which can change pending work or stats), mirroring the resource-hook pattern. Minor: a manual Dashboard pending-action both `refetch()`s and self-dispatches `notification:new`, so it now triggers one extra *silent* GET batch — harmless and not worth special-casing. `vite build` clean (6.52s); lint clean.

- **2026-06-02** — `fix/form-modal-width-desktop` pushed. Follow-up to the width fix below: once the dialogs actually rendered at their declared width, `sm:max-w-2xl` (672px) was too wide for the create **forms** on desktop — lots of empty space. Narrowed the three create-form dialogs (`CreateVoucherDialog`, `SubmitTithesDialog`, `CreateRfDialog`) to `sm:max-w-lg` (512px), a comfortable single-column form width. Left the **detail** dialogs (`VoucherDetailsDialog`, `RfDetailsDialog`, `ExpenseDetailsDialog`) at `sm:max-w-2xl` — they show receipt-image grids / pipeline tracker / side-by-side info that use the extra room. **Files: 3.** `vite build` clean (6.69s).

- **2026-06-02** — `fix/modal-padding-and-receipt-overflow` pushed. Adrian hit a bug in the Create Voucher dialog: after uploading a receipt the right edge of the modal was clipped, hiding the delete (trash) button, and the form modals felt cramped. **Root cause (real, app-wide):** every dialog overrode width with an **unprefixed** `max-w-*` (e.g. `max-w-2xl`), but the base `DialogContent` sets `sm:max-w-sm`. A responsive `sm:` utility is emitted later in the compiled stylesheet than an unprefixed one, so at ≥640px `sm:max-w-sm` (= `--container-sm`, 24rem/384px) **won the cascade** — every dialog was actually 384px wide on desktop, not its intended size. Verified against the built CSS (`.max-w-2xl` at byte ~16.7k vs `--container-sm` rule at ~56.6k). **Fix:** prefixed all 15 dialog width overrides with `sm:` (`max-w-2xl`→`sm:max-w-2xl`, `max-w-lg`→`sm:max-w-lg`, `max-w-md`→`sm:max-w-md`) so they share the `sm` variant with the base rule — tailwind-merge then drops `sm:max-w-sm` and the intended width applies; mobile still uses the base `max-w-[calc(100%-2rem)]`. **Padding:** bumped the global `DialogContent` padding `p-4`→`p-6` and the `DialogFooter` breakout `-mx-4 -mb-4 p-4`→`-mx-6 -mb-6 p-6` (the only in-dialog edge-breakout — every other `-mx-4` is a table mobile-list inside a Card), and nudged the close button `top-2 right-2`→`top-3 right-3`. **Defensive:** added `shrink-0` to the receipt-row delete button in `CreateVoucherDialog` so it can never be clipped. **Files: 17** (`ui/dialog.jsx` + 15 dialogs re-prefixed + the receipt button). `vite build` clean (6.98s). **Pattern note for future dialogs:** always size `DialogContent` with a `sm:`-prefixed `max-w-*` — an unprefixed one silently loses to the base `sm:max-w-sm` on desktop.

- **2026-06-02** — `feat/voucher-cancel-reopen` pushed (frontend; backend companion `Backend-Tithes#feat/voucher-cancel-reopen` shipped + merged first). Two finance-team requests. **(1) Cancel a voucher → reopen the RF.** A validator (only the one who **created** the voucher) or an admin can cancel a voucher while its linked RF is still `voucher_created` (i.e. **not yet disbursed**). The backend `PATCH /api/vouchers/:id/cancel` reverses the auto-recorded expense, soft-cancels the voucher (`status:'cancelled'`, kept for audit), and reopens the RF to `approved` so a new voucher can be issued. Frontend: `can.cancelVoucher` (`['validator','admin']`; creator-vs-admin check done per-row with `currentUserId` vs `voucher.createdBy`); `useVouchers.cancelVoucher(id, note?)` PATCHes, broadcasts `notification:new {refModel:'RequestForm'}` (so the RF page / Dashboard / PendingRfsCard resync) and toasts `voucherCancelled`. The **Cancel voucher** item lives in the `VoucherTable` three-dots `RowActions` (gate: `voucher.status !== 'cancelled' && rfId.status === 'voucher_created' && can.cancelVoucher && (admin || creator)`), routed through `Voucher.jsx`'s `VOUCHER_CONFIRM_CONFIG.cancel` (reject variant) + a special-case in `handleConfirmVoucherAction` that calls the hook. Reason/note is **optional** (no input surfaced; the hook accepts one if added later). Cancelled vouchers stay in the table with a rose **Cancelled** badge — since the RF reverts to `approved`, the badge + status filter key off a new `effectiveVoucherStatus(v)` helper (`v.status==='cancelled' ? 'cancelled' : v.rfId?.status`) and `voucherStatusConfig.cancelled` was added. **(2) Category picker at voucher creation.** `CreateVoucherDialog` now renders a Category `Select` (sourced from `useCategories()` filtered `type==='rf' && isActive!==false`), defaulting to the RF's approved category but editable so a wrong category can be corrected before the PCF issues — the backend already applied a differing `category` to the RF, the FE just never exposed the choice. Submit now requires `categoryId`. **Files: 7** (`rolePermissions.js`, `useVouchers.js`, `lib/toast.js`, `voucher-components/mockData.js`, `VoucherTable.jsx`, `CreateVoucherDialog.jsx`, `pages/Voucher.jsx`). `vite build` clean (9.20s); lint on changed files shows only the pre-existing `Voucher.jsx` focus-effect error + the `CreateVoucherDialog` `[rfId]` dep warning (both consistent with existing code, untouched).

- **2026-05-19** — `fix/frontend-hardening` pushed. Final branch of a 6-branch security-hardening pass (5 backend branches preceded it; full audit lives in the plan file). Two changes. **(1) `/dev-login` gated to dev builds** — `App.jsx` wraps the `DevRolePicker` route in `{import.meta.env.DEV && (...)}`. The route, and `DevRolePicker` itself, used to ship in production builds; Vite statically replaces `import.meta.env.DEV` with `false`, so the conditional is dead-code-eliminated and the component tree-shaken out of the prod bundle entirely (verified absent from `dist/`). In production `/dev-login` now falls through to the `path="*"` redirect to `/dashboard`. The hardened backend `verifyToken` already rejects the `mock-<role>-token` strings `DevRolePicker` mints, so data was never at risk — this just removes a confusing fake-authenticated UI from prod. **(2) Default credentials removed from `README.md`** — the Live section had the seeded admin login `adrian@joscm.com` / `adrian` in plain text; replaced with "contact the administrator for access credentials".

- **2026-05-19** — `feat/dashboard-tweaks` pushed. Three changes Adrian asked for. **Files: 6.** **(1) "Expenses by Category" chart shown to every role.** Previously the Dashboard's `ChartBarExpense` was admin/auditor-only (gated by `canViewExpenses`); Adrian wanted all staff to see where church money goes. Backend companion `Backend-Tithes#feat/expenses-by-category` (PR #50, **shipped + merged first**) added `GET /api/expenses/by-category` — an aggregated `{ category, amount }` series for the last 6 months, open to all roles (no per-transaction detail, so members don't get over-shared expense records). Frontend: `useDashboardData` now fetches `/expenses/by-category` unconditionally (5th parallel request, all roles) and returns a new `expensesByCategory`; the full `/expenses` fetch stays admin/auditor-gated for `ChartAreaGradient`'s overlay + `SummaryStats`. `ChartBarExpense` switched from a `expenses` prop (built the category series client-side via the now-removed `buildCategorySeries`/`SIX_MONTHS_MS`) to a pre-aggregated `data` prop. `Dashboard.jsx` drops the `canViewExpenses &&` gate on that chart — the charts grid is now always `lg:grid-cols-3` (was 2-or-3). **(2) Reports page removed from `member`.** `rolePermissions.js` — `member` dropped from the Reports `NAV_ITEMS.roles` (hides the sidebar link). `App.jsx` — the `/reports` route was previously ungated (any authenticated user); now wrapped in `<ProtectedRoute allowedRoles={["admin","do","validator","pastor","auditor"]}>` so a member typing the URL directly is redirected too. **(3) Tithes Trend chart — bigger "Total".** `TithesTrendChart.jsx` footer: the Total value bumped from `text-sm font-semibold` to `text-xl font-bold` so it reads at a glance; "Daily Avg" and "Best Day" stay at the smaller size. `vite build` clean (9.59s); lint clean on changed files. **Note:** the new `/expenses/by-category` endpoint must be deployed on the backend before this frontend change has data — without it the chart shows its "No expenses recorded" empty state. Follow-up to `feat/sileo-toaster`, four fixes bundled (Adrian found them while testing the sileo rollout role-by-role). **Files: 4.** **(1) Duration** — `toaster.jsx`: toasts lingered too long. Sileo's `DEFAULT_TOAST_DURATION` is `6000`ms (vs. the old sonner setup's `4000`ms) — added `options={{ duration: 3000 }}` to the `<Toaster>` mount. Sileo's `options` prop is `Partial<SileoOptions>` and sets the default for every toast, so one prop makes 3000ms app-wide. **(2) Position flipped** per Adrian's preference — `toaster.jsx`: the responsive `matchMedia("(max-width: 640px)")` mapping was reversed — desktop now shows the toast **top-center** (was top-right), mobile **top-right** (was top-center); initial `useState` default also flipped to `"top-center"`. **(3) Missing toasts — the real bug.** Adrian saw toasts only on create-user / create-category / create-RF and not on approve/validate/disburse — it *looked* role-dependent but was actually **surface**-dependent. The 6 feature hooks all call `notifyAction`, so any action routed through a hook toasts. But two surfaces mutate via `apiFetch` **directly**, bypassing the hooks (and therefore the toast): **`Dashboard.jsx`** (deliberately inline — it reads via `useDashboardData`, so mounting the feature hooks would double-fire GETs) and **`NotificationActionDialog.jsx`** (acts on a record loaded from a notification). Fix: raise the toast at those two call sites. Dashboard — `patchAndRefetch(path, refModel, toastKey)` gains a `toastKey` arg (`pendingActions` map each pass their key: `tithesApproved`/`rfSubmitted`/`rfValidated`/`rfApproved`/`rfDisbursed`/`rfReceived`); the inline `createCategory` / `submitTithes` / voucher-`onSubmit` also get their `notifyAction` call. NotificationActionDialog — `performAction(path, body, toastKey)` gains a `toastKey` arg; the validate/approve/disburse/received/tithes-approve buttons and `handleRejectConfirm` (RF→`rfRejected`, Tithes→`tithesRejected`) all pass it. **Pattern reinforced:** the `notifyAction`/`WORKFLOW_TOASTS` keys are now also the contract for any surface that mutates outside the feature hooks — keep them in sync. **(4) Desktop toast text centered** — `index.css`: added `@media (min-width: 641px) { [data-sileo-description] { text-align: center } }` since the toast sits top-center on desktop (sileo's default is `text-align: left`); mobile keeps left-aligned. `vite build` clean (8.36s); lint clean on changed files (the one pre-existing `react-hooks/exhaustive-deps` warning in `NotificationActionDialog` untouched).

- **2026-05-19** — `feat/sileo-toaster` pushed. Adrian found the `sonner` toasts visually unappealing and asked to switch to **`sileo`** (`npm i sileo`) — a physics-based React toast library (gooey SVG morphing + spring physics). Flagged the maturity risk before proceeding (`sileo@0.1.5`, created 2026-02, single maintainer `hiaaryan`, MIT, ~88k downloads/mo, pulls in `motion`/Framer Motion as a dependency) — Adrian accepted it. **Migration was low-touch because the toast API was already abstracted:** `src/lib/toast.js` exposes `notifyAction(key, description?)` over a `WORKFLOW_TOASTS` map (~26 keys) plus four generic `notify{Success,Error,Info,Warning}` helpers — the 6 feature hooks (`useTithes`/`useRequestForms`/`useVouchers`/`useUsers`/`useCategories`/`useExpenses`) and `ChangePasswordDialog` all call through `notifyAction`, so swapping the underlying library only touched the wrapper + the helper bodies, **not a single call site**. **Files: 5** (+ `package.json`/lockfile). (1) **`src/components/ui/toaster.jsx`** (new — replaces `sonner.jsx`, which was `git rm`'d) — wraps sileo's `<Toaster>`, imports `sileo/styles.css`, sets `theme="light"`. **Mobile-responsive position:** a `matchMedia("(max-width: 640px)")` listener flips `position` between `top-right` (desktop) and `top-center` (mobile) so the toast sits centered above the thumb zone on phones instead of hugging a corner. (2) **`src/lib/toast.js`** — `notify*` helpers rewritten from sonner's `toast.success(title, { description })` two-arg form to sileo's single-object form `sileo.success({ title, description })`. `WORKFLOW_TOASTS` map + `notifyAction` dispatcher kept byte-identical — the abstraction is exactly why this was cheap. (3) **`src/main.jsx`** — import path `./components/ui/sonner.jsx` → `./components/ui/toaster.jsx`. (4) **`src/index.css`** — removed the old `[data-sonner-toast]` countdown-progress-bar block (sonner-specific, no longer relevant); added a `@media (max-width: 640px)` rule overriding sileo's CSS var `--sileo-width` (fixed `350px` by default) to `calc(100vw - 2rem)` so the toast never overflows a narrow phone. (5) **removed `sonner`** dependency (`npm uninstall sonner`). **Sileo API notes for future work:** functions are `sileo.{show,success,error,warning,info,action}(opts)` + `sileo.promise()` / `dismiss()` / `clear()`; `SileoOptions` supports `{ title, description, type, position, duration, icon, styles, fill, roundness, autopilot, button }`; the `<Toaster>` takes `position` / `offset` / `options` (default opts) / `theme`. Variant colors come from CSS vars (`--sileo-state-success`/`error`/`warning`/`info`/`action`) — override those in `index.css` if the palette needs to match the app. No `icons` map like sonner had — per-toast `icon` instead. `vite build` clean (7.95s); `npm run dev` boots clean (944ms, deps re-optimized for sileo); lint clean on the changed files. **Pattern note:** keeping toast wording behind `notifyAction`/`WORKFLOW_TOASTS` (shipped in `feat/sonner-toasts`) is what made a whole-library swap a 5-file change — keep new toasts going through that dispatcher, never call `sileo.*` directly from a component/hook.

- **2026-05-19** — `fix/rf-self-decision-guard` pushed. Adrian, testing as a `validator`, found he could validate an RF he had created himself — a conflict-of-interest hole. **Root cause: pre-existing, not a regression.** Git history confirms `can.validateRf` only ever checked `role` (`['validator','auditor','admin']`) from the very first commit that added the `can` matrix (`30fa817 feat/auth-rbac`) — the owner-exclusion guard was never applied to RF decision actions. The guard *does* exist elsewhere (`can.approveTithes` excludes the submitter; RF disburse/received gate on `isOwner`), so this was an inconsistency, not a break. **Scope (Adrian's call): validate + approve + reject** — all three RF decision actions on one's own RF are now hidden. `createVoucher` and `disburse` left alone (out of scope; `disburse` is admin/DO-only anyway). **Files: 4** — fixed at every surface that renders RF decision actions, since the bug could surface from any of them: (1) **`RfTable.jsx`** `ActionMenu` — `submitted` branch gates `validate`/`reject` and `for_approval` branch gates `approve`/`reject` with `&& !isOwner` (`isOwner` was already computed at line 92, previously only used for `draft`/`disbursed`). (2) **`RfDetailsDialog.jsx`** `buildFooterActions` — same `&& !isOwner` on the `submitted`/`for_approval` footer buttons (`isOwner` already computed at line 81). (3) **`NotificationActionDialog.jsx`** `renderActionButtons` — `submitted`/`for_approval` RF branches gain `&& !isOwner` (`isOwner` already computed at line 260); when the requester opens their own RF notification it now falls through to the "Open full page" button. (4) **`PendingWorkSection.jsx`** `actionForRow` — `rf-submitted`/`rf-for_approval` cases now compute `isOwner` (mirroring the existing `rf-disbursed` case) and return `null` when the viewer owns the RF. **Chose call-site `&& !isOwner` over threading an owner flag through `can.validateRf`** — consistent with how `markRfReceived` is already gated, and avoids changing the `can` matrix signature. **Edge case:** if `requestedBy` arrives as a raw ObjectId string (not populated), `isOwner` is `false` and the button still shows — acceptable since the real API populates `requestedBy`, and matches the pre-existing tolerance in the disburse/received gates. **Backend gap flagged (not in this branch):** the frontend only hides buttons — the `validate`/`approve`/`reject` RF endpoints should also reject when `requestedBy === current user`, otherwise a direct API call bypasses the guard. `npm run lint` on the 4 changed files shows no new errors (the 2 pre-existing `no-unused-vars` in `PendingWorkSection` and the `useEffect` dep warning in `NotificationActionDialog` are untouched).

- **2026-05-07** — `feat/voucher-require-receipt` pushed. Companion to backend `Backend-Tithes#feat/voucher-require-receipt` (PR shipped first — controller now 400s with `"At least one receipt is required"` when `req.files` is empty/missing). Adrian's ask: receipts on the Create Voucher dialog should not be optional, since that field is where the actual resibo picture lives. Kept the change frontend-side minimal (no inline error state added — the disabled submit button is enough signal since the dropzone makes it obvious where to upload). **File: 1** — `src/components/voucher-components/CreateVoucherDialog.jsx`. (1) Label gets a red asterisk: `Receipts *` matching the established required-field convention used elsewhere in the app. (2) Dropzone helper text changed from `JPG, PNG, WebP — up to 5 files, 10MB each` to `Required — JPG, PNG, WebP — 1 to 5 files, 10MB each` so the constraint is visible before the user picks. (3) Submit button `disabled` predicate gains `|| receipts.length === 0`, alongside the existing `!selectedRf || !amount || submitting` checks — same spot, single line edit. **No other changes** — `handleFiles`, `removeReceipt`, FormData build, and `MAX_FILES` / `MAX_SIZE_BYTES` / `ACCEPT` constants all unchanged. The backend's new 400 still surfaces inline via the existing `setError(err.message ...)` catch in case someone bypasses the disabled button (e.g., direct API call). `vite build` clean.

- **2026-05-05** — `feat/sonner-toasts` pushed. Adds toast notifications for every successful form-submit / workflow action across the app. Adrian asked for it generically ("toaster sa app — ano pwede gamitan, ikaw na magdecide"); evaluated `react-toastify` vs `sonner` and went with **sonner** because (a) shadcn officially deprecated their Radix-based `useToast` in favor of sonner — `npx shadcn@latest add sonner` literally installs the sonner package + a thin wrapper, (b) sonner ships beautiful defaults out-of-the-box (smooth stack animations, no CSS to write to look good), (c) `toast.promise()` is a one-liner for async submit flows. **Design choice (Adrian's call during planning):** white card background, **4px colored left border** as the variant accent (Linear/Notion-style) — rejected the alternative full-thin-border because the left accent reads color-first in peripheral vision and feels more modern. Status → color: `success=emerald`, `error=red`, `warning=amber`, `info=blue`. **Files: 11.** (1) **`src/components/ui/sonner.jsx`** (new) — minimal Toaster wrapper that pins position to `top-right`, duration to 4s, swaps in custom lucide icons (`CheckCircle2`/`CircleAlert`/`TriangleAlert`/`Info`) per variant with matching colored hue, and applies the white-bg + colored-left-border via `toastOptions.classNames` (uses `!` Tailwind prefix to win against sonner's defaults — sonner's own toast root has its own bg/border, we override). `closeButton` enabled. (2) **`src/lib/toast.js`** (new) — exports four generic helpers (`notifySuccess`, `notifyError`, `notifyInfo`, `notifyWarning`) and the central `notifyAction(key, description?)` dispatcher. The dispatcher reads from a single `WORKFLOW_TOASTS` map keyed by ~26 action names — 4 tithes, 11 RF, 1 voucher, 5 user, 5 category, 1 expense, 1 auth — each entry holding `{ type, title, description? }`. Centralising the wording so the same action reads identically whether the user fired it from a row dropdown, a details-dialog footer, or a Dashboard quick-action. Optional `description` arg overrides the default (used for e.g. `notifyAction("voucherCreated", "PCF-0042")` to include the new PCF number). (3) **`src/main.jsx`** — mounts `<Toaster />` at the BrowserRouter root (outside Layout), so toasts render on Login / DevRolePicker too. (4–9) **All 6 feature hooks instrumented** (`useTithes`, `useRequestForms`, `useVouchers`, `useUsers`, `useCategories`, `useExpenses`) — every mutation now ends with `await refetch(); notifyAction("<key>")`. **Hook-level (not page-level) on purpose** so every caller gets the toast for free: Tithes table row, Dashboard quick-action card, RfDetailsDialog footer, RfTable dropdown, etc. all converge through the hook, no per-call-site wiring needed. Errors still throw up to the calling dialog for inline-red display (preserved existing behavior — no double error display via toast + inline). `useCategories.updateCategory` branches its toast key on payload shape: `{isActive:false}`-only PATCH → `categoryArchived`, `{isActive:true}`-only → `categoryRestored`, anything else → `categoryUpdated` (because Categories.jsx reuses the same mutation for archive/restore and full edits). `useRequestForms.createRf` and `createAndSubmitRf` pass the new `rfNo` as the description; `useVouchers.createVoucher` passes the new `pcfNo`. (10) **`src/pages/Voucher.jsx`** — disburse + markReceived bypass the `useRequestForms` hook (they call `apiFetch` directly so `useVouchers` can refetch + broadcast in the right order), so toast wiring lives in this page. Each `VOUCHER_CONFIRM_CONFIG` entry got a new `toastKey` field (`rfDisbursed` / `rfReceived`); `handleConfirmVoucherAction` calls `notifyAction(cfg.toastKey, confirming.voucher.rfId?.rfNo)` after the apiFetch + refetch. (11) **`src/components/sideBar-components/ChangePasswordDialog.jsx`** — replaced the inline `success` state (the green "Password changed successfully." paragraph + the Cancel→Close button-label flip + the disabled-after-success submit button) with `notifyAction("passwordChanged")` followed by `handleOpenChange(false)` to auto-close the dialog. Cleaner: dialog closes, toast slides in, user moves on. **Implementation note:** The toast fires **after** `await refetch()`, not during, so the user sees the new state in the table before the toast appears — feels snappy, the toast confirms what they can already see. **What was deliberately not added:** error toasts. Inline red helper text near the primary button (existing pattern from the Real Data Rollout) is the source of truth for failures; toasting on error too would duplicate the signal. Easy to add later if Adrian wants peripheral error notifications. **Pre-existing lint** (the same 21 set-state-in-effect / context-export warnings tracked in earlier entries) untouched. `vite build` clean (10.55s); `npm run dev` boots in 1044ms with no console errors. Pattern note for future toast work: every new mutation should add a key to `WORKFLOW_TOASTS` rather than calling `toast.success()` directly — keeps wording consistent and gives a single place to localise/restyle.

- **2026-05-04** — `feat/voucher-mark-received` pushed. Closes the loop on the voucher-page UX: the requester can now confirm receipt directly from the Voucher page (previously they had to go to the Request Form page to do that). Companion backend PR `Backend-Tithes#feat/voucher-populate-requested-by` shipped first to deep-populate `rfId.requestedBy._id/.name` on `getAllVouchers`, which is what made this branch's ownership gate possible. **Files: 3** — `src/pages/Voucher.jsx`, `src/components/voucher-components/VoucherTable.jsx`, `src/components/voucher-components/VoucherDetailsDialog.jsx`. **Three fixes bundled** (the latter two are pre-existing bugs Adrian spotted while testing this branch — same component, cohesive scope, fixed inline rather than spawning two more branches): (1) **Mark as Received** added to both surfaces — dropdown menu item with `<PackageCheck />` and details-footer green button — gated by `voucher.rfId?.status === "disbursed" && can.markRfReceived(role) && isOwner`. Ownership check is shape-tolerant (`requestedBy` may arrive as a populated object `{_id, name}` after the backend PR or — defensively — as a raw ObjectId string). (2) **Create Voucher submit was silently no-op** — `CreateVoucherDialog`'s `handleSubmit` early-returns on `if (!selectedRf || !onSubmit) return`; Voucher.jsx mounted the dialog without an `onSubmit` prop, so clicking Submit did nothing visible. Wired `onSubmit={createVoucher}` from `useVouchers()` (the hook already had the mutation defined; the parent just hadn't passed it through). The Dashboard's `CreateVoucherDialog` mount has its own inline `onSubmit` so this bug never surfaced there — only on the Voucher page. (3) **`PendingRfsCard` was always empty** — it was mounted with only `onCreateVoucher` and no `rfs` prop, so it defaulted to `[]` and showed "No approved RFs awaiting voucher" even when there were many. Wired `useRequestForms()` and a `useMemo` filter `rf.status === "approved" && !rf.voucherId`; the hook already refetches itself on `notification:new` events with `refModel=RequestForm`, so the card stays in sync after creates / disburses / receives. Pass `loading` and `error` through too so the card can render its own skeleton/empty states honestly. **Voucher.jsx refactor:** also took the chance to consolidate the disburse + markReceived flow into a single dispatcher, mirroring the pattern shipped in `feat/rf-row-click-details`. Replaced the previous `disbursing` state with `confirming = { voucher, kind } | null` and the previous `requestDisburse` with `requestVoucherAction(kind, voucher)`. New module-level `VOUCHER_CONFIRM_CONFIG` map keyed by `disburse` and `markReceived`, each entry holding `{ variant, title, describe, confirmLabel, pendingLabel, endpoint }`. The `endpoint(v)` builds the PATCH path from `v.rfId._id` — both actions mutate the linked RF, not the voucher. After mutation: `dispatchEvent("notification:new", { refModel: "RequestForm" })` then `refetch()` vouchers — same broadcast pattern used elsewhere. **Components:** VoucherTable's `RowActions` now takes `voucher`, `role`, `currentUserId`, `onView`, `onAction`; renders View Details + (separator) + conditionally Disburse + Mark as Received. VoucherDetailsDialog accepts `userRole`, `currentUserId`, `onRequestAction`; conditionally renders a `<DialogFooter>` with the matching button(s). **No backend changes in this branch** (backend PR shipped separately). **Note on cwd discipline (per `feedback_explicit_cd_per_repo.md`):** during this work the shell cwd leaked from a previous backend session into the start of this branch (created `feat/voucher-mark-received` on the backend repo by accident). Recovered by deleting the misnamed backend branch (`git branch -d`, no commits yet), `cd`ing back to frontend explicitly, then re-creating the branch on the right repo. The Edit calls themselves used absolute paths so no file-edit ended up in the wrong tree — only the branch ref was wrong. Reinforces the existing rule: **always prepend `cd <repo>` to every Bash call when both repos are in play.** `vite build` clean (16.40s). Pre-existing lint (`react-hooks/set-state-in-effect` on the focusId effect at line 57 of `Voucher.jsx`) untouched.

- **2026-05-04** — `feat/rf-row-click-details` pushed. Final entry in the row-click-details UX series. RF is the most complex of the three tables: 7 actionable status states, 9 distinct actions across role + ownership gates, three different action-dialog types (`ConfirmActionDialog`, `RejectDialog`, `CreateRfDialog` in edit mode) plus a navigation case (`Create Voucher`). Earlier branches (Tithes / Voucher) had ≤2 inline action buttons each, so they kept the action state co-located in the table component. RF couldn't follow that pattern: `RfDetailsDialog` is mounted by `RequestForm.jsx` directly (not nested inside `RfTable`), so the table's local dialog state was unreachable from the details footer. **Refactor: state lifted from RfTable.jsx → RequestForm.jsx (parent).** Both surfaces now dispatch into one shared `requestRfAction(kind, rf)` callback, so the dropdown menu and the details footer go through the exact same path. **Files: 3** — `src/pages/RequestForm.jsx`, `src/components/request-form-components/RfTable.jsx`, `src/components/request-form-components/RfDetailsDialog.jsx`. **RequestForm.jsx changes:** (1) added module-level `RF_CONFIRM_CONFIG` map (mirror of the one shipped in `feat/pending-work-confirm-dialog`'s `PendingWorkSection`) keyed by 6 confirmable kinds (`submit`, `delete`, `validate`, `approve`, `disburse`, `received`) — wording matches the existing copy used elsewhere so the same RF action reads identically across dropdown, footer, and dashboard quick-actions. (2) lifted state: `editingRf`, `rejectingRf`, `confirmingRf` (was inside RfTable). (3) helper `closeViewing()` consolidates the existing `setViewingRf(null) + clear ?focus= URL param` cleanup so the close-then-open sequence (avoid stacked modals) is one call. (4) `requestRfAction(kind, rf)` is the single dispatch entrypoint — calls `closeViewing()` first, then routes by kind: `edit` → `setEditingRf`; `reject` → `setRejectingRf`; `createVoucher` → `navigate("/voucher")`; everything else → `setConfirmingRf({ rf, kind })`. (5) `handleConfirmRfAction` switches on the confirming kind and calls the matching `useRequestForms()` mutation (`submitRf`/`deleteRf`/`validateRf`/`approveRf`/`disburseRf`/`markRfReceived`). (6) mounts `RejectDialog`, `CreateRfDialog` (edit mode), and `ConfirmActionDialog` at the bottom of the page tree — same dialogs that used to live in RfTable. **RfTable.jsx changes:** (1) row click + stopPropagation pattern (same as Tithes / Voucher branches). (2) **All local dialog state stripped** — no more `rejectingRf`, `editingRf`, `confirming`, `CONFIRM_CONFIG`, `handleConfirm`, `askConfirm`. The bottom of the file (RejectDialog + ConfirmActionDialog + CreateRfDialog mounts) is gone — moved up to RequestForm.jsx. (3) **Prop surface simplified** — instead of 8 separate handler props (`onUpdateRf`, `onDeleteRf`, `onSubmitRf`, `onValidateRf`, `onApproveRf`, `onRejectRf`, `onDisburseRf`, `onMarkReceived`) the table now takes a single `onRequestAction(kind, rf)`. The internal helper `askAction(rf, kind) => onRequestAction?.(kind, rf)` keeps the `ActionMenu` invocation sites readable. (4) `useNavigate` import removed — navigation for `createVoucher` happens in the parent's `requestRfAction`. **RfDetailsDialog.jsx changes:** (1) new module-level `buildFooterActions({ rf, role, currentUserId })` helper — pure function returning the ordered list of buttons to render based on status + role + ownership, mirroring the same gates as `RfTable`'s ActionMenu (`draft+owner` → Edit/Delete/Submit; `submitted` → Reject/Validate; `for_approval` → Reject/Approve; `approved` → Create Voucher; `voucher_created` → Mark as Disbursed; `disbursed+owner` → Mark as Received; `received`/`rejected` → no footer). (2) new module-level `variantClass` map for button styling — `primary` (green bg + white text, used for approval-flow buttons), `danger` (solid red bg + white text — Adrian's call during testing: a Reject button needs visual weight, the previous outline-red read like a secondary action), `outline-red` (kept for Delete — Delete is rarer and more destructive, the outline-vs-solid distinction differentiates "destructive but recoverable workflow step" from "permanently remove this draft"), and `outline` (kept for Edit — neutral). (3) new props `userRole`, `currentUserId`, `onRequestAction`; conditionally renders `<DialogFooter>` only when `footerButtons.length > 0`. (4) **`closeAndRun` modal-stacking guard** is enforced in the parent's `requestRfAction` rather than inside the dialog — buttons just call `onRequestAction(kind, rf)` and let the parent close-then-open. **No backend changes; no API change. `vite build` clean (7.78s).** Pre-existing lint (`react-hooks/set-state-in-effect` on the `?focus=` effect at line 102 of `RequestForm.jsx`) untouched. **Pattern recap for the row-click-details series (now complete across Tithes / Voucher / RF):** desktop `<TableRow onClick>` + `cursor-pointer hover:bg-muted/50`, mobile card `<div onClick>` + `cursor-pointer active:bg-muted/40`, actions cell + right-side cluster `onClick={e => e.stopPropagation()}`, three-dots dropdown kept as-is, action buttons in details footer mirror the dropdown's status/role/ownership gates, parent owns the secondary dialog state so both surfaces dispatch into one callback, close-then-open sequence prevents stacked modals.

- **2026-05-04** — `feat/voucher-row-click-details` pushed. Continuation of the row-click-details UX series — Voucher edition. Adrian's expectations evolved during testing: he initially asked for "row click → view details, no action buttons inside" (since vouchers are ledger-of-record), but as soon as he tested it he realized the **Disburse action belongs on this page** because the voucher row is where you visually see the "Awaiting Disbursement" status badge — making it the natural place to act on it. The original three-dots dropdown only had View Details, so Disburse was effectively missing from this entire page even though the status was prominently displayed. **Files: 3** — `src/pages/Voucher.jsx` (parent — owns confirm + API call), `src/components/voucher-components/VoucherTable.jsx` (dropdown gets Disburse item, row click opens details), `src/components/voucher-components/VoucherDetailsDialog.jsx` (footer gets Disburse button). **Implementation:** (1) Row click + stopPropagation pattern matches Tithes branch (desktop `<TableRow onClick>` + `cursor-pointer hover:bg-muted/50`; mobile card `onClick` + `active:bg-muted/40`; right-side action cluster `onClick={e => e.stopPropagation()}`). (2) `RowActions` extended — accepts `voucher`, `role`, `onView`, `onDisburse` (was just `onView`); conditionally renders `<DropdownMenuSeparator />` + a Disburse `<DropdownMenuItem>` with `<BadgeCheck className="text-green-600" />` only when `voucher.rfId?.status === "voucher_created" && can.disburseRf(role)`. Permission helper from `rolePermissions.js` (`['admin', 'do'].includes(role)`). (3) `VoucherDetailsDialog` — accepts new `userRole`, `onRequestDisburse` props; conditionally renders a `<DialogFooter>` with a green `<Button>` when the same condition holds; clicking the button calls `onRequestDisburse(voucher)` and the parent handles the close-then-confirm sequence. (4) **Confirm state lifted to `Voucher.jsx` parent** instead of duplicated in both VoucherTable and VoucherDetailsDialog — both surfaces share the same `requestDisburse(voucher)` callback that snapshots the voucher, closes the details dialog (clearing `?focus=` from the URL the same way `handleViewClose` does), and sets `disbursing` state. The `<ConfirmActionDialog>` mounts at the parent level keyed off `disbursing` — opens cleanly on top of the table view (not on top of the details dialog). Same modal-stacking guard as TithesTable. (5) `handleConfirmDisburse` calls `apiFetch("/request-form/{rfId}/disburse", { method: "PATCH" })` (note: disburse mutates the **linked RF**, not the voucher itself — `rfId._id` is what the endpoint needs); on success, dispatches `notification:new` event with `refModel: "RequestForm"` so any mounted RF page or Dashboard refetches in lockstep, then awaits `useVouchers().refetch()` so the row's status badge flips to "Disbursed". **Mark-as-Received deliberately not added in this branch** — `getAllVouchers` populates `rfId` with only `rfNo estimatedAmount status remarks` (verified in `Backend-Tithes/src/controllers/voucherController.js`), so we have no way to do the ownership check (`rfId.requestedBy._id === user.id`) needed to gate that button. Adding Mark Received here is a follow-up that needs a backend populate change first (add `requestedBy` to the rfId populate select string). Adrian can still mark received from the RF page; this branch just doesn't *also* surface it on the voucher page. `vite build` clean (7.38s). **Pre-existing lint** (`react-hooks/set-state-in-effect` on the focusId effect at line 31 of `Voucher.jsx`) untouched — not introduced by this branch. **Pattern note for the upcoming RF branch:** when an action button needs an ownership check beyond just role, verify what the table's source endpoint actually populates before designing the gate — half the data the frontend needs for ownership UX may not be on the wire yet.

- **2026-05-04** — `feat/tithes-row-click-details` pushed. Adrian's UX ask: clicking anywhere on a tithes row (desktop or mobile) should jump straight to the View Details dialog instead of forcing a click on the three-dots menu first. And the details dialog should surface the relevant action buttons inline so the user doesn't have to close → reopen the dropdown to act on what they're already looking at. Three-dots dropdown **kept as-is** per Adrian's instruction (its dropdown items still work; this is purely additive). **File: 1** — `src/components/tithes-components/TithesTable.jsx`. **Implementation:** (1) Desktop `<TableRow>` got `onClick={() => setViewing(row)} className="cursor-pointer hover:bg-muted/50"`. The actions `<TableCell>` (housing the three-dots `RowActions`) got `onClick={(e) => e.stopPropagation()}` so clicks on the dropdown trigger / inside the menu don't bubble up and re-open the details dialog. (2) Mobile card-stack `<div>` got the equivalent — `onClick={() => setViewing(row)} className="cursor-pointer active:bg-muted/40"` on the outer card, and `onClick={(e) => e.stopPropagation()}` on the right-side cluster (`badge + RowActions`). `active:` instead of `hover:` because phones don't have hover. (3) Inside the existing details dialog, added a conditionally-rendered `<DialogFooter>` that only mounts when `viewing.status === "pending"` AND the user has at least one of the two permissions. The footer is wrapped in an IIFE so the role checks run after `viewing` is non-null and we can early-return `null` when neither button applies (avoids rendering an empty footer with weird padding). Approve uses `bg-green-600 hover:bg-green-700 text-white` + `<Check />` icon; Reject is `variant="outline"` with `border-red-600 text-red-600` + `<X />` icon — matches the visual language already used elsewhere in the app. (4) Click handlers do NOT stack modals: `closeAndRun(setter)` snapshots the current `viewing`, calls `setViewing(null)` (and clears `focusId` via `onFocusHandled?.()` the same way the dialog's own `onOpenChange` does), then sets `setApproving(target)` or `setRejecting(target)`. So the details dialog closes → `ConfirmActionDialog` (for approve, with the existing "Yes, approve" copy) or `RejectDialog` (for reject, with the reason `<Textarea>` field) opens cleanly on top of the table view, not on top of the details. **Conflict-of-interest rule preserved:** uses the same `can.approveTithes(role, submitterName, userName)` helper that the dropdown uses (`!!role && submitterName !== userName`) — so the user's own pending entries still show only Reject, never Approve, identical to the existing three-dots behavior. Adrian confirmed this during testing. **No backend changes; no API change; no other-file changes.** `vite build` clean (7.42s). **Pre-existing lint** (`react-hooks/set-state-in-effect` on the focusId effect at line 190) untouched — verified via `git stash` lint comparison; not introduced by this branch. **Pattern note for the next two branches in this UX series (`feat/voucher-row-click-details`, `feat/rf-row-click-details`):** same row-click + stopPropagation pattern, same `closeAndRun` modal-stacking guard. RF will need a status-driven action set in the dialog footer (8 status transitions across submit / edit / delete / validate / approve / reject / create voucher / disburse / mark received) gated by role + ownership; voucher is view-only so no footer buttons.

- **2026-05-04** — `feat/pending-work-confirm-dialog` pushed. Adrian asked for confirmation dialogs on every quick-action button in the Dashboard's "Your Pending Work" section — same UX gate already shipped on the Tithes table and RF table. Why: on mobile, a single accidental tap was enough to approve/disburse/receive a record without any second-step. **Reuses existing `ConfirmActionDialog`** (Adrian-designed shared component at `src/components/shared/ConfirmActionDialog.jsx` — illustration + title + description + confirm/cancel; dialog owns its own `submitting` + `error` state and surfaces failures as red text inline). **Files: 1** — only `src/components/dashboard-components/PendingWorkSection.jsx`. **Implementation:** added a module-level `CONFIRM_CONFIG` map with one entry per action kind (`approveTithes`, `validateRf`, `approveRf`, `disburseRf`, `markReceivedRf`, `submitRf`); each entry holds `{ variant, title, describe(item, helpers), confirmLabel, pendingLabel }`. Copy mirrors the existing wording in `RfTable.jsx` / `TithesTable.jsx` (e.g. "Approve this request form?" / "Mark as disbursed?" / "Yes, received") so the same action reads identically wherever a user can take it. Refactored `actionForRow(bucketKey, item)` from `{label, run}` to `{label, kind}` (or `{label, direct: () => ...}` for the one action that already has its own multi-step dialog — Create Voucher; opening that as the action *is* the confirm step, redundant to wrap). New `confirming = { kind, item } | null` state; tap → `setConfirming(...)` → `<ConfirmActionDialog>` mounts at the bottom of the Card with `onConfirm={handleConfirm}` which switches on `confirming.kind` and calls the matching `actions.*` callback from props. Dropped the prior per-row `busyId` + `errorById` state and the `runAction` helper — the dialog handles busy + error inside itself, so success closes the dialog (and the row typically disappears on the parent's refetch); failure keeps the dialog open with red error text. Also dropped the unused `Loader2` import the row spinner used to need. **No changes to `Dashboard.jsx`** — the `actions` prop interface (`approveTithes`, `validateRf`, `approveRf`, `disburseRf`, `markRfReceived`, `submitRf`, `onCreateVoucher`) is unchanged; the wrapper just decides whether to confirm-then-call or call-directly. `vite build` clean (20.37s). **Pre-existing lint errors** (Icon false-positive in `PendingSection`, unused `rfBySetStatus`) untouched — verified via `git stash` lint comparison; not introduced by this branch. **Pattern note:** for any future Dashboard quick-action that mutates a record, route the click through `ConfirmActionDialog` unless the action already opens its own multi-step dialog (the dialog itself is the confirm).

- **2026-05-02** — **History rewrite on `main` (no code change).** Adrian noticed "Claude" appearing on the GitHub repo Contributors graph and asked it removed so the repo presents as solo work. Ran `git filter-branch --msg-filter "grep -v '^Co-Authored-By: Claude'" -- main` which rewrote **146 commits on `main`**, stripping every `Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>` (and `4.6` / `Sonnet 4.6` variants) trailer from commit messages. Force-pushed to origin via `git push --force-with-lease origin main`. **No file contents changed** — only commit hashes + author trailers. App code is byte-identical to before; next deploy from `main` is a no-op functionally. Local backup at `backup/pre-history-rewrite` (kept until Adrian confirms everything still works, then can be deleted with `git branch -D backup/pre-history-rewrite`). **Side effects to be aware of when reading future-me:** (a) every commit hash on `main` is now different from what original PRs referenced — old PR comments linking to specific hashes will 404, but the merge-commit messages still mention the PR numbers; (b) old feature branches that were merged still exist on remote with their pre-rewrite hashes — they're invisible in the Contributors graph (which counts `main` only) but their hashes don't appear in `git log main`; (c) GitHub Contributors graph takes ~24h to recompute after a force-push to default branch. Same operation done in parallel on `Backend-Tithes` (93 commits rewritten — see `Backend-Tithes/CLAUDE_BACKEND.md`). New collaboration rule codified at the time: **commits + PR bodies must never include Co-Authored-By: Claude or "Generated with Claude Code" footers** (recorded in `feedback_no_claude_attribution.md`); the original committing template was overriding this default and that's what produced the contributors-graph entry in the first place.

- **2026-05-02** — `perf/login-prewarm-backend` pushed. Adrian reported the live app's first login takes very long because of backend cold start. Backend lives on **Render free tier** (per `Backend-Tithes/README.md` line 13: service sleeps after ~15min idle, first request after a cold period takes 30–60s — Node.js boot + Atlas mongoose handshake + `httpServer.listen()` only fires after `connectDB().then()`, so the port is closed during the entire warm-up). **Fix is purely frontend, hides the cold start behind typing time.** When `Login` mounts, we fire a fire-and-forget `GET /` ping at the backend root — the user typically takes 5-15 seconds to type email + password, and that whole window now overlaps the backend wake-up. By the time they click Login, the backend is up (or close to it) and the actual `POST /api/auth/login` returns instantly instead of waiting 30-60s on top of everything else. **Implementation in `src/pages/Login.jsx`:** added `useEffect` to the existing imports + a single useEffect block — strips `/api` off `VITE_API_URL` to derive the backend root (Render's `/` is the documented smoke-test endpoint, no auth, no DB query, lightest possible warm-up), uses `AbortController` to cancel the in-flight fetch on unmount (e.g. user navigates away fast), `.catch(() => {})` to swallow errors silently because the real login call surfaces any actual problem with an inline error. Adrian's existing Login UI **untouched** (per `feedback_preserve_designs.md` — never overwrite styled pages); only the imports + a useEffect were added. **Net change:** 1 file, ~15 lines. `vite build` clean (12.41s). **Pattern note for any future "live app feels slow" issue:** if the user-facing latency is dominated by a known Render free-tier cold start, hiding it behind unrelated UI time (form-fill, route navigation) is a higher-ROI fix than micro-optimizing the bundle. Other options floated and rejected for now: external keepalive ping (UptimeRobot every 5min — best zero-cold-start fix, but technically against Render TOS and consumes ~720 of the 750 free instance-hours/month) and Render Starter upgrade ($7/mo, no spin-down at all, no TOS issue) — Adrian opted for #1 only.

- **2026-05-02** — `fix/tithes-trend-chart-overshoot` pushed. Follow-up to `feat/dashboard-summary-fit-and-year-trend`. Adrian reported the Tithes Trend chart looked "sobra at putol" — the line/area appeared to leak out of the chart area and clip at edges. **Root cause:** `<Area type="natural">` uses a smooth cubic-spline interpolation that **can overshoot the data range** when there are few data points or sharp value transitions (e.g. a year-scoped chart showing only 4 months Jan–Apr, or any series with a 0→large-value jump). The spline's control points pull the curve above max or below 0, and since there was **no top margin** on the AreaChart and **no explicit YAxis domain**, the overshoot leaked past the rendered chart bounds and got clipped, producing the broken/sticking-out look. Three-part fix in `ChartAreaGradient.jsx`: (1) **`type="natural"` → `type="monotone"`** on both Area components — monotone cubic interpolation never overshoots, so the curve stays within data bounds even with sparse points; (2) **explicit `<YAxis hide domain={[0, "dataMax"]} />`** added — `hide` keeps the visual identical (no axis labels) but the explicit `domain={[0, "dataMax"]}` floors the chart at 0 (so a dip can't go negative) and reserves the right vertical space; (3) **`margin={{ top: 16, ... }}`** — gives the curve breathing room at the top so the highest data point never touches the card edge. Also added `dot={chartData.length <= 2}` so a year with only 1–2 months of data still renders a visible point (recharts won't draw a meaningful "line" between 1 point — without dots that case looked empty). Inline code comment explains why `monotone` was chosen over `natural` so future-me doesn't regress it. **Net change:** 1 file (`ChartAreaGradient.jsx` — added YAxis import + 3 chart prop tweaks + a comment). `vite build` clean (12.84s). **Pattern note for any future recharts work in this app:** with year/period-scoped charts that can vary in data-point count (1 to 12), always use `monotone` over `natural`/`basis` and add explicit margins + a YAxis with `domain={[0, "dataMax"]}` even if hidden.

- **2026-05-02** — `feat/dashboard-summary-fit-and-year-trend` pushed. Two unrelated Dashboard tweaks bundled because both touch the same row of cards. **(1) Financial Summary card no longer leaves dead space at the bottom.** The `SummaryStats` Card sits in `lg:grid-cols-3` next to taller chart cards (`ChartAreaGradient` + `ChartBarExpense`), so its own `h-full` made the Card stretch — but its 4-tile grid (`grid grid-cols-2 gap-3`) wasn't filling that stretched height, leaving a visible empty slab below the tiles. Fix is 2 class additions on the Card: `flex flex-col` (so the Card becomes a column flex container) + `flex-1` and `auto-rows-fr` on the CardContent grid (CardContent absorbs the remaining vertical space; `auto-rows-fr` then makes both rows of tiles equal-height and stretching). No JS changes. **(2) Tithes Trend chart now scopes to a calendar year with a year dropdown.** Replaced the prior "rolling last 12 months" series with a per-year view governed by a new `<Select>` in the card header. Year options are built by `buildYearOptions(currentYear)` — starts from `FIRST_YEAR = 2026` (church's first year on the system) and grows with the calendar, so today the dropdown only shows `[2026]`; in 2027 it'll show `[2027, 2026]`; default is always `currentYear`. The series itself follows **Adrian's rule**: chart spans **January through the latest month that has any approved tithes data within the selected year** — zero-fill any gaps in between but **don't extend forward into months with no approved data**. So in May 2026 with approved tithes only Jan–Apr, the chart shows Jan–Apr; the moment a May entry is approved, May appears. Same rule applies to past years viewed later (no special-case for current vs. past — keeps logic single-path). When the selected year has zero approved tithes, the chart slot renders an "No data to display for {year}" centered message instead of an empty AreaChart. **Trend footer overhauled** for the year-scoped model: compares the selected year's visible-months sum vs the same-month-range sum of the prior year (YTD-vs-YTD when current year is partial — Jan–Apr 2026 vs Jan–Apr 2025, not full 2025) so the comparison stays apples-to-apples. Three branches: (a) **prior year has no approved tithes at all** → "First year with approved tithes" + "{period} · no prior year to compare against" (this is what 2026 will show until 2027 begins); (b) **prior year has data but none in the visible month range** → "No comparable months in {prior year}"; (c) **both populated** → "Trending up/down by X% vs {prior year}" with the existing TrendingUp icon (rotated when negative). Period sub-label is `{Jan} – {lastMonth} {year}`. Existing **"Compare with Expenses" toggle preserved** — placed alongside the year dropdown in a `flex flex-wrap` row that stacks on mobile and right-aligns on `sm:` and up. Expenses series filtered to the same year and capped at the same `lastMonth` as tithes (so the secondary line never extends past the tithes-defined window). **Net change:** 2 files (`ChartAreaGradient.jsx` rewritten — 227+/100−; `SummaryStats.jsx` — 4 lines). `vite build` clean (13.56s). **Pre-existing lint** (Icon false-positive in `StatTile` destructure) untouched — verified via `git stash` comparison.

- **2026-05-02** — `feat/pending-work-action-buttons` pushed. Adds **green per-row quick-action buttons** to every actionable card in the Dashboard's "Your Pending Work" section so users can advance the happy-path workflow without leaving the dashboard. **Reject is intentionally not surfaced** — Adrian's call: rejection should be deliberate, so users still navigate to the source page (Tithes/RF) to reject from the table. **Action mapping per bucket (uses `can.*` helpers from `rolePermissions.js` so the matrix stays authoritative):** `tithes` → "Approve" → `PATCH /tithes/:id/approve` (any role except submitter — bucket already filters by `submitterId !== userId`); `rf-submitted` → "Validate" → `PATCH /request-form/:id/validate` (validator/auditor/admin); `rf-for_approval` → "Approve" → `/approve` (pastor/auditor/admin); `rf-approved` → **"Create Voucher"** → opens existing `CreateVoucherDialog` via `preselectedRfId` because this is the only action that needs a form (amount + receipts upload, multipart); `rf-voucher_created` → "Disburse" → `/disburse` (admin/do); `rf-disbursed` → "Mark as Received" → `/received` (member-owner only — admin sees the row for monitoring but no button rendered); `rf-draft` → "Submit" → `/submit` (member-owner). **Files: 2.** (1) **`src/pages/Dashboard.jsx`** — added a `pendingActions` object that bundles 6 inline `apiFetch + broadcastResourceChange + refetch` mutation closures plus an `onCreateVoucher(rf)` handler that sets `voucherPreselectId` state and opens the existing `CreateVoucherDialog`. The mutations use `apiFetch` directly (not via `useTithes`/`useRequestForms`) because the Dashboard's `useDashboardData` hook is the single source of truth for reads here — mounting the resource hooks would fire duplicate GETs. After each mutation, `broadcastResourceChange(refModel)` dispatches the existing `notification:new` event so any mounted list page (Tithes/RF) refetches in lockstep — same pattern as `NotificationActionDialog`. The single `CreateVoucherDialog` instance is now reused by both the top-bar quick-action (no preselect → user picks from dropdown) and the per-row Create Voucher button (preselect → RF locked in). `voucherPreselectId` cleared on dialog close to avoid stale state on the next open. (2) **`src/components/dashboard-components/PendingWorkSection.jsx`** — added `actions` prop; new `actionForRow(bucketKey, item)` helper returns `{ label, run }` or `null` (null hides the button — used for admin viewing the member-only `rf-disbursed` bucket); new `runAction(id, fn)` runs the mutation with `busyId` + per-row `errorById` state so other rows stay clickable while one row is in flight, and a failure renders inline as small red text under the row that failed (no global alert/toast — codebase has no toast utility yet). Refactored each row from a single `<button>` into an `ActionRow` wrapper containing **two sibling click targets**: the navigate-to-source button and the green action button — sibling structure (not nested) so React doesn't warn about button-in-button and the action click can't bubble into the row navigate. Action button styling: shadcn `<Button size="sm">` with `bg-green-600 hover:bg-green-700 text-white`; while busy, swaps label to "Working…" + `<Loader2 className="animate-spin" />` and `disabled`. Card description copy updated to "Take quick action below, or click a row to open it for full details." `vite build` clean (7.18s). **Pre-existing lint errors** (Icon false-positive in `PendingSection`, unused `rfBySetStatus`) untouched — verified via `git stash` lint comparison; not introduced by this branch.

- **2026-04-28** — `fix/pending-work-layout` pushed. Adrian reported the "Your Pending Work" Card on the Dashboard was clipped/squeezed — couldn't see its content. Three causes, all fixed: (1) **Card was being compressed by the Dashboard's flex column** — the parent uses `flex-1 min-h-0 flex flex-col gap-5 overflow-auto` and the RecentActivity sibling has a fixed `h-[28rem] md:h-[36rem]`, so default `flex-shrink: 1` on the Card let it get squeezed below its natural content height before the parent's `overflow-auto` even kicked in. Added `shrink-0` to the Card. (2) **Empty buckets cluttered the section** — admin role has 6 actionable-stage buckets, but most are usually empty; rendering all 6 made the visible content drown in "Nothing here right now." placeholders. Now filters to `visibleBuckets` (any bucket with `items.length > 0`, plus the auditor's `isAudit` stats tile which always shows). The "All caught up" empty state still kicks in when zero items across ALL buckets. (3) **Internal `max-h-56 overflow-y-auto` on each bucket** — capped each bucket list to ~14rem with its own scroll, so even with content the lists felt cramped. Removed; buckets now grow with their content and the page itself scrolls. **Net change:** 1 file, 3 edits. `vite build` clean (19.30s). **Pattern note:** in this app's Dashboard layout (flex-col with one fixed-height sibling), any new top-level Card needs `shrink-0` or it'll get squeezed.

- **2026-04-28** — `feat/remove-dark-mode` pushed. Reverts the dark-mode infrastructure shipped in `feat/balance-limit-and-darkmode-footer` earlier the same day. Why: when Adrian flipped the toggle in real testing, only the shadcn-built components (which use semantic tokens like `bg-card` / `text-foreground` that auto-flip via the `.dark { --background: ... }` rules in `index.css`) actually changed; the custom layout shell (Layout outer `bg-gray-200`, page `bg-white`, Sidebar `bg-[#f6f6f6]`, Header borders/text in `gray-300`/`gray-700`, custom `Buttons.jsx` in `bg-black text-white`, status-badge utility maps with `bg-blue-100` / `text-blue-700` etc.) all use hardcoded light-mode-tinted utilities that don't respond to the `.dark` class. Result was a half-dark UI that looked like a bug. Two repair options were considered: (A) refactor every hardcoded color to a semantic token (single change site, but subtly shifts the LIGHT visual Adrian had already approved), or (B) add `dark:` variants on every hardcoded class (preserves light exactly, but ~22 files of mechanical edits). Adrian's call: pull the feature instead — the church app's primary use is on a single shared device in well-lit settings, dark mode wasn't load-bearing UX. **Removed:** `src/context/ThemeContext.jsx`, `src/hooks/useTheme.js`, the `<ThemeProvider>` wrapper in `Layout.jsx`, and the Sun/Moon toggle row from `Sidebar.jsx`'s `SidebarFooter`. **Kept:** the `SidebarFooter` block itself (still rendered at the bottom of both desktop Sidebar and MobileSidebar via `mt-auto`) and its credit text "Design & Built by ianDev 2026, All rights reserved." — that part Adrian wanted to keep regardless of the toggle. The `.dark { ... }` CSS variable block in `index.css` is left alone because it's part of the shadcn baseline (untouched since the layout-shell branch); inert without anything adding the class. Stale `localStorage["theme"]` keys from earlier toggle sessions are harmless — no code reads them anymore. `vite build` clean (13.71s). **Net change:** 4 files (2 deleted — ThemeContext, useTheme; 2 edited — Layout, Sidebar). **Pattern codified for future shell-level theming features in this app:** unless every hardcoded color in the layout shell is migrated to semantic tokens *first*, adding a `.dark` toggle gives a half-dark broken UI. The cheap MVP path is a trap; do the token refactor as a prereq, then ship the toggle.

- **2026-04-28** — `feat/balance-limit-and-darkmode-footer` pushed (stacks on top of `feat/disburse-status-flow-ui`; relies on the backend `availableBalance` field added in `feat/disburse-status-flow`). Two unrelated UX asks bundled into one branch because both touch the shell. (1) **RF amount balance gate** — `CreateRfDialog` now gates `estimatedAmount` against the church's available cash-on-hand. `useTithes` exposes a new `availableBalance` (passed through from the backend's new `availableBalance` field, which is `sum(approved tithes) − sum(all expenses)`). `RequestForm.jsx` mounts `useTithes` (only to read this number — adds one GET /tithes per page mount; auto-refreshes via the existing `notification:new` window event) and threads it into `CreateRfDialog`. The dialog displays `Available tithes balance: ₱X,XXX` under the amount input on create mode and disables submit if amount > balance, with a red error message ("Amount exceeds available tithes balance (₱X,XXX)") and a separate copy when balance is zero ("The church has no available tithes balance — no requests can be made right now"). Edit mode skips the check (drafts can be edited freely; validators catch over-balance edits at review time) — implemented by gating on `!isEdit && balanceKnown` so the prop being undefined (RfTable's edit path doesn't pass it) becomes "no check". (2) **Sidebar footer** — new `SidebarFooter` block at the bottom of both desktop Sidebar and MobileSidebar. Has a clickable Sun/Moon row with the dynamic label "You're in Light Mode" / "You're in Dark Mode" that toggles via the new `useTheme()` hook. Below that, in 10px muted text: "Design & Built by ianDev 2026, All rights reserved." Pushed to the bottom of the sidebar with `mt-auto` (the parent already has `flex flex-col`). MobileSidebar's `SheetContent` got `flex flex-col` added so `mt-auto` works there too. (3) **New `ThemeContext` + `useTheme` hook** — `src/context/ThemeContext.jsx` owns `theme` ∈ `{light, dark}` and exposes `toggleTheme`. Initial state reads `localStorage["theme"]` first, then falls back to `prefers-color-scheme`, then defaults to light. Effect adds/removes the `dark` class on `document.documentElement` (matches the existing `.dark` selectors in `index.css`) and persists to localStorage on every change. `Layout.jsx` wraps the existing `NotificationsProvider` in `ThemeProvider` so the toggle is available app-wide. Hook follows the established `useAuth`/`useNotifications` pattern (separate file, throws if used outside the provider). `vite build` clean (12.42s). **Net change:** 7 files (2 new — ThemeContext, useTheme; 5 edited — Layout, Sidebar, useTithes, RequestForm, CreateRfDialog). **Pattern codified:** localStorage-backed context + html-class flip is the lightweight dark-mode pattern for this codebase — Tailwind's `.dark` utilities pick up the class automatically; no per-component theme prop, no CSS-in-JS, no shadcn theme plugin needed.

- **2026-04-28** — `feat/dashboard-pending-work` pushed (stacks on top of `feat/disburse-status-flow-ui`). Adrian's UX ask: every role's dashboard should open with "what should I do right now?" — not generic charts and history. Fix: new `PendingWorkSection` rendered as the first content block on the Dashboard, above the charts. Per-role buckets (read from the existing `useDashboardData` arrays — no new endpoints): admin sees pending tithes + RFs grouped at every actionable stage (submitted/for_approval/approved/voucher_created/disbursed); DO sees pending tithes + RFs awaiting disbursement; validator sees RFs to validate + RFs awaiting voucher; pastor sees RFs to approve; member sees own RFs awaiting receipt confirmation + own drafts to submit; auditor gets a read-only oversight strip (counts across the pipeline + vouchers-this-month) instead of clickable rows. Each row deep-links via the existing `?focus=<id>` URL pattern that RequestForm/Tithes pages already handle. Empty state: "All caught up — wala nang pending sa'yo!". Tithes filter excludes the user's own pending entries (backend forbids self-approval anyway, so showing them would be dead-end clicks). Auditor's oversight tile spans full width and shows 7 metric tiles instead of role-action buckets. The same data is already on the page (`useDashboardData` returns tithes/rfs/vouchers), so this is filtering + presentation only — zero new fetches. `vite build` clean (7.62s). **Net change:** 2 files (1 new, 1 edited). **Pattern codified:** the dashboard's first impression is now "your queue, today" instead of "all-time totals" — when adding a new actionable status in future, append a bucket to the role's array in `PendingWorkSection.buckets`.

- **2026-04-28** — `feat/notification-action-buttons` pushed (stacked on top of `feat/disburse-status-flow-ui`). Adrian's UX complaint: clicking a notification just deep-linked to the relevant page, so an admin/validator/DO/pastor still had to scroll-find the row and open its action menu to do the obvious next step. Fix: clicking a notification now opens an in-place `NotificationActionDialog` that shows the linked record's key fields and the role-appropriate action buttons inline. Mappings (record status × user role × ownership): RF `submitted` × validator/auditor/admin → Validate + Reject; RF `for_approval` × pastor/auditor/admin → Approve + Reject; RF `approved` × validator/admin → "Create Voucher" (navigates because it needs file upload); RF `voucher_created` × admin/DO → Mark as Disbursed; RF `disbursed` × owner → Mark as Received; Tithes `pending` × any role except submitter → Approve + Reject; everything else (terminal states, vouchers) → "Open page". Reject is an inline mode inside the same dialog (textarea + "Confirm Rejection") instead of a separate dialog — keeps the click count low and matches the conversational flow of "I just clicked the notif, let me act on it." After any action, the dialog closes, notifications refetch (via the `useNotifications` context's `refetch`), and the resource list refetches via the existing `window.dispatchEvent("notification:new", { detail: { refModel } })` mechanism that `useRequestForms`/`useTithes`/`useVouchers` already subscribe to (so RfTable / TithesTable / VoucherTable stay fresh without the dialog touching their hooks). Record fetch on dialog open is one GET to the list endpoint with a `find(_id === refId)` — no new backend route needed; the populated record (requester, category, etc.) is already returned by the list endpoint. `NotificationsBell` and `Notifications` page both render their own dialog instance and pass the active notif as a prop. `useNavigate` + `pathForRef` are dropped from the click path; `pathForRef` is still used for the dialog's "Open page" escape hatch. `vite build` clean (7.34s). **Net change:** 3 files (1 new, 2 edited). **Pattern codified:** any future notif type that needs an inline action just adds another branch in `renderActionButtons`; the dialog is the single shared UI surface for "act from a notification".

- **2026-04-28** — `feat/disburse-status-flow-ui` pushed (depends on backend branch `feat/disburse-status-flow` on Backend-Tithes — merge that first). Splits the RF disbursement transition so admin/DO can mark a `voucher_created` RF as `disbursed`, then the requester (any role) confirms `received` (the new terminal state). Previously, `disbursed` was the terminal state and was set directly by whoever clicked "Mark Received" on a `voucher_created` RF — that conflated two distinct actions and left no audit trail of the disbursement step. Frontend changes mirror the new backend contract: `PATCH /api/request-form/:id/disburse` (admin/DO) and a reshaped `PATCH /:id/received` handler that now requires status `disbursed`. `received` is added to `statusConfig` (color: emerald) and `pipelineStages` so the pipeline tracker, table status filter, and details timeline auto-pick it up; `disbursed` color shifted to cyan to distinguish it from the closed/received state. **Voucher table cleanup** — kills the fake "Pending Receipt" label the frontend invented from `voucher_created` (which was the user's complaint: "wala naman ganyan status"). Real labels now: `voucher_created → Awaiting Disbursement`, `disbursed → Pending Receipt`, `received → Received`. `RfTable` ActionMenu: `voucher_created` → admin/DO sees "Mark as Disbursed"; `disbursed` → owner (regardless of role) sees "Mark as Received"; `received` is terminal (no actions). `useRequestForms` exposes a new `disburseRf(id)` mutation alongside the renamed-semantics `markRfReceived`. `RfDetailsDialog` timeline now has separate `Disbursed` (uses `disbursedAt`/`disbursedBy`) and `Received` (uses `receivedAt`/`receivedBy`) rows instead of one ambiguous step. `dashboardUtils.buildActivity` distinguishes "Disbursed" (admin/DO action) from "Received" (requester action) entries. `RfSummaryStats.active` + `pendingDisb` now count `disbursed` as still-in-flight (member hasn't confirmed yet). `VoucherSummaryStats` tiles relabeled: "Awaiting Disbursement" (was "Pending Receipt", counts `voucher_created`) and "Pending Receipt" (was "Disbursed", counts `disbursed`). `rolePermissions.can` adds `disburseRf` and broadens `markRfReceived` to permit any role (the UI already gates on owner). `vite build` clean (8.28s). **Net change:** 9 files. **Pattern codified:** the status flow now has a clean two-step finish — disbursement is an explicit operational action (admin/DO logs that money left the church) and receipt is a separate confirmation action (requester acknowledges). Future audit reports can join on `disbursedAt`/`disbursedBy` vs `receivedAt`/`receivedBy` for true accountability.

- **2026-04-27** — `feat/refetch-on-notification` pushed. Closes a stale-data gap exposed by the realtime notifications + deep-link modal stack. Adrian's repro: member submitted an RF, admin validated it, member got the realtime "your request has been validated" notif (Socket.IO push works ✓), clicked the notif → details modal opened (deep link works ✓), but the timeline still showed status `submitted` with no validated check. Cause: the Socket.IO push only updates the `notifications` list in `NotificationsContext`. The local `rfs` / `tithes` / `vouchers` arrays in the per-resource hooks stay stale until a manual refresh, so the deep-link's `find()` against the local array returns the old copy. **Fix:** `NotificationsContext.handleNew` now rebroadcasts every `notification:new` as a `window` `CustomEvent` of the same name with the notif as `event.detail` (after the local prepend, so the bell still updates first). `useRequestForms` / `useTithes` / `useVouchers` each subscribe to that window event and call their `refetch()` when `detail.refModel` matches their resource (`"RequestForm"` / `"Tithes"` / `"Voucher"`). Result: by the time the user clicks the notif and the deep-link `useEffect` runs, the resource hook has already refetched, so the details dialog opens with fresh data. **Why window event vs context callback or per-hook socket subscribe:** keeps the data hooks decoupled from `NotificationsContext` (no extra import) and from `socket.io-client` (no second subscriber to the same event). One event source, three listeners, each filters by `refModel`. **`vite build` clean** (6.89s). **Net change:** 4 files, 27 insertions. **Pattern codified:** any future resource hook (e.g., the deferred RF inline-comments hook) can subscribe to `window.addEventListener("notification:new", ...)` and refetch on matching `refModel` — no plumbing through context or socket needed.
- **2026-04-27** — `feat/realtime-notifications` pushed (paired with Backend-Tithes#`feat/socket-io-notifications`). Replaces the 60-second polling channel from PR #32 with Socket.IO server push. Adrian's real-world pain: submitting an RF and then waiting through 1–2 manual refreshes for the bell to update — far from "realtime." Decision was Socket.IO over tighter polling because (a) any polling cadence still has visible lag, (b) a small church app has very low event rate so 60×/min/user of polling chatter is wasteful even if invisible, (c) the deferred RF inline-comments feature (memory `project_rf_comments_scheduled.md`, agent fires 2026-05-14) explicitly notes Socket.IO is the right channel for chat — so adding it now means one mechanism, not two. **Backend (`Backend-Tithes#feat/socket-io-notifications`):** wraps Express in `http.createServer`, attaches `socket.io` with the same CORS allowlist + Vercel-preview pattern as the REST endpoints. JWT handshake middleware reads `socket.handshake.auth.token`, verifies with the existing `JWT_SECRET_KEY`, sets `socket.userId = decoded.id`, and on connection joins the socket to a room named after that user id. Tiny new module `src/services/realtime.js` holds a module-scoped `io` reference and exports `emitToUser(userId, event, payload)` so callers don't have to plumb `io` themselves. `src/utils/sendNotification.js` (the funnel for **all six** notification triggers — tithes approve/reject, RF validate/approve/reject/received, voucher create) emits `notification:new` with the saved doc to the recipient's room after the DB insert. No controller-level changes were needed because every emit point already goes through this helper. **Frontend (`feat/realtime-notifications`):** new `src/services/socket.js` is a singleton client. `connectSocket()` reads the Bearer token from `localStorage` and passes it as `auth.token` (matches the backend handshake). `SOCKET_URL` is derived from `VITE_API_URL` by stripping the trailing `/api` so prod (Render) and local both work without a separate env var. `transports: ["websocket", "polling"]` gives WebSocket primary with polling as fallback for restrictive networks. `NotificationsContext.jsx` removes `setInterval` / `POLL_INTERVAL_MS` / `intervalRef` entirely. On user login: `refetch()` runs once for initial state, then `connectSocket()` opens the channel. The new `notification:new` listener prepends the doc to local state, **de-duped on `_id`** so a reconcile + push race can't double-insert. On the socket's `connect` event AND on `window.focus`, `refetch()` runs to reconcile any missed events (laptop sleep, network blip). On user logout, the effect cleanup calls `disconnectSocket()`. **`markAsRead` / `markAllAsRead` unchanged** — they're optimistic + REST the same way; the websocket only carries new-notification events. **`vite build` clean** (6.54s frontend). Backend `node --check` passes for all modified files. **Net change:** Backend — 5 files, 256 insertions / 11 deletions; Frontend — 4 files, 141 insertions / 13 deletions; new `socket.io` (~96 prod deps) + `socket.io-client` (~16 deps) packages. **Deploy together** — frontend without backend will fail at the handshake; backend without frontend keeps working (clients still poll? — actually no, because frontend dropped polling, so a frontend-without-backend deploy means notifications stop appearing entirely. Cutover order: backend first, then frontend, with the merge gap kept short). **Unblocks `project_rf_comments_scheduled.md`** — the 2026-05-14 agent will find Socket.IO ready to host the RF inline-comments / chat channel rather than introducing a parallel mechanism.
- **2026-04-27** — `fix/select-popup-mobile-stacking` pushed. Adrian reported the category dropdown in `CreateRfDialog` works fine on a desktop browser even when resized to mobile dimensions, but on a real mobile device the popup is invisible / unpickable. PR #38 (`fix/dialog-select-mobile-and-label`) had already addressed an iOS Safari bug by flipping `alignItemWithTrigger` to `false`, but a second class of failure remained on real devices. **Two defensive changes to `src/components/ui/select.jsx`** (intentionally global so all Selects benefit, no per-dialog opt-in needed): (1) **z-index bump** from `z-50` to `z-[60]` on both `<SelectPositioner>` and `<SelectPopup>`. The Dialog overlay + content also use `z-50`, which means the Select popup was tied at the same stacking level. DOM order would normally let the Select win since it portals later than the Dialog, but iOS Safari with stacked portaled overlays has been observed to stack unexpectedly — the popup ends up rendered *behind* the dialog content. `z-[60]` removes the ambiguity unconditionally. (2) **`collisionPadding={8}` default** added to `<SelectPositioner>`. On a narrow mobile viewport, the popup sometimes computes a position right at the viewport edge; floating-ui then has no room to render the popup body and ends up clipping it. The 8px padding gives the smart-collision logic a margin to flip the popup to the other side of the trigger instead of clipping. **Why global vs CreateRfDialog-only:** the same root causes apply to every Select-in-Dialog usage in the app. The Voucher dropdown (the one explicitly fixed in PR #38) gets the same treatment with no behavioral change expected; the other Selects on the Tithes / Categories / Users / RF Table filters benefit similarly. **`vite build` clean** (6.66s). **Net change:** 1 file, 4 insertions / 2 deletions. **If this still doesn't fix the bug on real mobile,** the next escalation is to investigate Base UI's Select root `modal` prop (`modal={false}` may be needed to prevent the layered modal collision between Dialog + Select) or to test if the Dialog's transformed positioning is breaking floating-ui's anchor calc on iOS Safari (would need a different anchor strategy).
- **2026-04-27** — `feat/notification-deep-link-modal` pushed. Closes the loop on the notifications UX: clicking a notification used to land the user on the related list page and leave them to find the record themselves; now it opens the record's existing details dialog directly. **Mechanism:** `pathForRef(refModel, refId)` in `notificationsUtils.js` now takes a second arg and appends `?focus=<id>` to the navigation target. Both consumers (`NotificationsBell.handleItemClick`, `Notifications.handleClick`) pass `notif.refId` (the backend already populates it on every notification doc). Each list page reads `?focus` via `useSearchParams` and auto-opens the matching record's existing details dialog: (1) `RequestForm.jsx` — `useEffect` watches `focusId + rfs`, sets `viewingRf` when it finds a match, and the `RfDetailsDialog` `onOpenChange` closure clears the URL param via `setSearchParams(replace: true)`. (2) `Voucher.jsx` — same pattern wrapping `VoucherDetailsDialog`. **Drive-by fix on Voucher.jsx:** the page was passing nothing to `<VoucherTable />` and `<VoucherSummaryStats />`, so both rendered empty defaults — `useVouchers` was defined but unconsumed. Wired the hook in (`vouchers, loading, error`) and passed them through. This was needed for the focus lookup to work, but it also fixes the previously-broken Voucher page (table was empty in production). (3) `Tithes.jsx + TithesTable.jsx` — the tithes details dialog lives **inside** the table component (unlike RF + Voucher where the dialog is at page level). To avoid lifting that state up (would have meant moving the approve/reject confirm dialogs too), the table gained `focusId + onFocusHandled` props. The page reads the URL param and passes them down; the table's existing `viewing` state opens the matching tithes via `useEffect`, and the dialog's `onOpenChange` closure calls `onFocusHandled` on close so the page can clear the URL param. **Edge case handled:** the focus lookup runs against the full data array, not the post-filter view. So a notification for a record currently filtered out by status/date still opens its dialog — no silent swallowing of clicks. **`vite build` clean** (6.90s). **Net change:** 7 files, 92 insertions / 11 deletions. **Pattern codified for future deep-linkable list pages:** read `?focus=<id>` via `useSearchParams`, find the record in the full data array, set the viewing state, clear the param via `setSearchParams(params, { replace: true })` on dialog close. If the dialog state lives in a child table, pass `focusId + onFocusHandled` props down rather than lifting the state up.
- **2026-04-27** — `feat/header-settings-menu` pushed. Wires up the previously inert settings icon in `Header.jsx` (line ~63 was a `<div>` wrapping `IoSettingsOutline` with zero behavior — placeholder since the layout shell branch). Now the icon is a Base UI `<Popover>` (same primitive `NotificationsBell` already uses, so no new dialog dependency) opening a 256px-wide menu: (a) header showing `user.name` + `ROLE_LABELS[user.role]`, (b) Change password action, (c) Log out action styled red as a destructive cue. **Why move these out of the sidebar:** Change Password + Logout previously lived as two tiny icon buttons in `SideBarHeader.jsx` (the `LuKeyRound` + `FiLogOut` icons next to the user's name). Hidden behind the mobile drawer, easy to miss on desktop, and visually competing with the sidebar's actual nav purpose. The header settings menu is visible at every breakpoint (no media-query gating), so mobile users can sign out without first opening the drawer. **State migration:** `pwdOpen` + `handleLogout` lifted from `SideBarHeader.jsx` to `Header.jsx`. `ChangePasswordDialog` is now mounted from Header, not the sidebar. `useNavigate` + `useAuth` + `ROLE_LABELS` imports moved with them. Dialog component itself is unchanged — same component, different mount point. **`SideBarHeader.jsx` simplified to display-only:** the avatar/logo block, user name + role, and the `FiSidebar` toggle icon survive; the two action buttons + their state + the `ChangePasswordDialog` mount + `useState`/`useNavigate` imports are gone (file shrank from 54 to 26 lines). **Popover content pattern (reusable):** title row with truncated `user.name` + role caption, `border-b` separator, then `<button type="button">` rows with `flex items-center gap-3 px-4 py-2 text-sm` — first row hover gray, logout row hover red-50 + text red-600. Both buttons close the popover before firing their action (`setSettingsOpen(false)` then dispatch) so the menu doesn't linger on top of the dialog or the login redirect. **`vite build` clean** (8.14s). **Net change:** 2 files, 64 insertions / 35 deletions. **Trigger:** Adrian's UX feedback — settings icon was placeholder + sidebar icons were too cramped/hidden.
- **2026-04-27** — `fix/dialog-input-bugs` pushed. Two unrelated dialog input bugs Adrian hit while testing notifications, root-caused and fixed in one branch. (1) **`ChangePasswordDialog.jsx`** — every keystroke registered the first character then dropped focus; subsequent keys went nowhere because no field was focused. Root cause: `PasswordField` was defined **inside** the `ChangePasswordDialog` component body. React saw a new function reference every parent render, so it unmounted and remounted the inputs on every keystroke (with focus loss as the side effect). The classic "inline component" anti-pattern — a function-as-component declared inside another component's body has unstable identity. Fix: hoisted `PasswordField` to module scope above the dialog component. Three call sites unchanged (lines now 110/120/130). The fix has zero behavioral risk because `PasswordField` was already pure (only reads its props, no closure over parent state) — it was just wrongly scoped. **Why this slipped past `feat/form-validation-hints` review:** the dialog worked manually during that branch's testing because the bug only manifests when a parent re-render happens between keystrokes, and during the validation-hints branch most testing was for the `<FieldHelp>` rendering, not actual character-by-character typing under live error-state updates. (2) **`SubmitTithesDialog.jsx`** — denomination quantity inputs displayed a literal `0` as a value (not a placeholder), so users had to manually clear before typing. Root cause: initial `qtys` state seeded each denomination with the **number** `0`. Line 161 bound `value={qty}` directly. With a numeric `0` as the value, the placeholder attribute was suppressed (HTML5 `<input>` shows placeholder only when value is empty). Fix: initial seed and `reset()` now use `""` (empty string) instead of `0`. `setQty` got a special case — when `value === ""` (user cleared the field), state preserves `""` instead of coercing to `0`; otherwise the existing `parseInt(value, 10) || 0` pipeline runs. Render uses `value={qty}` directly (now empty when not yet typed) and derives `numericQty = Number(qty) || 0` for the subtotal math (handles both string and number cases gracefully). Added `placeholder="0"` to the `<Input>` for the visual hint. **Wire shape unchanged:** `total` derivation still does `qtys[b] || 0` (works with empty strings via JS coercion). Submit payload still filters `(qtys[bill] || 0) > 0` and the kept entries are always numbers (the user must type at least one digit to keep them). Backend contract is identical. (3) **`vite build` clean** (6.79s). **Net change:** 2 files, 38 insertions / 32 deletions. **Trigger:** Adrian noticed both bugs in real testing during the same Submit-RF / Change-Password user journey. Both fixes are minimum-viable — no broader refactor of the form-validation-hints pattern, no migration of other dialogs that might have the same inline-component issue (none of the other dialogs in `feat/form-validation-hints` define inline components — verified by quick scan; only `ChangePasswordDialog` did).
- **2026-04-27** — `feat/confirm-action-dialog` pushed. Adopts Adrian's `DeleteModal.jsx` prototype (which lived at `src/components/layout/` as a `/delete` sandbox route) into a real shared dialog primitive used everywhere the app needs a verify-before-act flow. **Visual baseline (preserved verbatim from Adrian's prototype):** centered illustration (~60% width) → bold 20px question with tight leading → small 12px gray supporting line → two-button row (`justify-between`) where the **action** button is an outline style in a brand color and the **Cancel** button is solid black with extra horizontal padding (`px-8` vs `px-5`). The visual prominence of Cancel is an intentional UX choice — an accidental click favors the safe path. **Three illustration-backed variants** (Adrian added the second and third SVGs to `src/assets/` mid-branch): `delete` (Inbox-cleanup-rafiki.svg + `#ed5f75` outline), `approve` (undraw_approve.svg + `emerald-600` outline), `reject` (Cancel-bro.svg + `#ed5f75` outline). New shared component lives at `src/components/shared/ConfirmActionDialog.jsx` — wraps shadcn `<Dialog>` for proper portal, focus-trap, escape-to-close, backdrop. Props: `open` / `onOpenChange` / `variant` / `title` / `description` / `confirmLabel` / `pendingLabel` / `onConfirm`. Async `onConfirm` with built-in `submitting` + inline error display below the question. **Wired sites:** (1) `RfTable.jsx` — replaces the old `runAction(fn, confirmMsg)` helper which used the browser-native `window.confirm()` (ugly default dialog, blocking, no styling). All five RF actions that change state (`submit`, `delete`, `validate`, `approve`, `mark-received`) now route through a `confirming = { rf, kind }` state pattern that renders the shared dialog with per-kind copy. **`approve` and `validate` previously fired on a single click with no confirmation** — that gap is now closed. Reject keeps its own `RejectDialog` because it requires a free-text note (different interaction pattern, not a yes/no confirm). The card-level `actionError` red strip is gone since errors now surface inline inside the dialog where the user can immediately retry without dismissing. (2) `TithesTable.jsx` — adds confirm to `approve` (was firing instantly). Reject keeps its note-required `RejectDialog`. Same dialog-inline-error pattern, removed the card-level `actionError` strip. (3) `ConfirmCategoryActionDialog.jsx` — when `action === 'delete'`, returns the new `ConfirmActionDialog` early; archive / restore keep the simpler Base UI dialog because they're low-stakes (Adrian only ordered three SVGs and didn't ask for the visual prominence on archive). The `AlertTriangle` import + the destructive-button styling fork are gone since the delete branch now lives in the shared dialog. (4) `ConfirmUserActionDialog.jsx` — same hybrid pattern: delete routes through the new dialog, deactivate / activate keep the simpler one. (5) Prototype removed — `src/components/layout/DeleteModal.jsx` (Adrian's design draft) and the `/delete` route in `App.jsx` are deleted; the design intent is preserved in the new shared component. **Architectural pattern codified for future verify-before-act work:** if you're adding a "are you sure?" yes/no flow, reach for `ConfirmActionDialog` with one of the three variants. If you need a free-text reason (note, justification), build a separate dialog with a `<Textarea>` (RF reject + Tithes reject are existing examples). If the action is just clarifying which target (e.g., "which RF?" picker), use a Select-based dialog, not a confirm. **`vite build` clean** (8.06s — significantly faster than the previous run's 19.20s, suggests Babel cache reuse). Net change: +1 new shared component (`ConfirmActionDialog.jsx`), +3 SVGs to `src/assets/`, 4 wrappers / tables modified, 1 prototype + 1 sandbox route deleted.
- **2026-04-27** — `fix/dialog-select-mobile-and-label` pushed. Two related Base UI Select bugs Adrian hit on the production app: (1) on a real phone (not Chrome devtools mobile-responsive), the category dropdown inside `CreateRfDialog` opened but the items were invisible / clipped, and the same bug repro'd on `CreateVoucherDialog`'s approved-RF dropdown; (2) on localhost, after picking a category, the trigger sometimes displayed the raw `_id` instead of the category name. **Bug 1 root cause:** Base UI's `<Select.Positioner>` defaults to `alignItemWithTrigger=true`, which overlaps the popup with the trigger so the selected item's text aligns with the trigger's value text (native-OS-feeling). Inside a dialog on iOS Safari, with the dialog's `transform`-based positioning + dynamic visualViewport (URL bar collapse/expand), this overlap math sometimes positioned the popup off-screen or behind the dialog content. The Base UI docs claim "automatically disabled if there is not enough space" — but in practice on real mobile inside a portaled dialog, that fallback didn't fire. Fix: change `src/components/ui/select.jsx`'s `SelectContent` default to `alignItemWithTrigger={false}` so every Select in the app uses standard floating-UI positioning (popup pops above or below the trigger like a normal dropdown). Cross-Select impact verified — all 17 SelectValue consumers still render correctly on desktop; visual diff is minor (popup is now consistently below the trigger instead of overlapping it). **Bug 2 root cause:** Base UI's `<Select.Value>` looks up the matching `<Select.Item>` by value and renders that item's text content. **If no item matches the current value** (timing race when value is set programmatically before the children register, or when editing an RF whose category was deleted from the system), Base UI falls back to displaying the raw value — which is the Mongo `_id` for our dynamic-data Selects. Fix: pass a children render function `(value) => ReactNode` on `<SelectValue>` for the four Selects whose value is an `_id` while the displayed label is a separate field — `CreateRfDialog` (categories), `CreateVoucherDialog` (approved RFs), `RecordExpenseDialog` (categories), `RfTable` (category filter). The function does an explicit lookup against the data array (`categories.find(c => c._id === value)?.name`) and falls back to the placeholder string when nothing matches. The other 13 SelectValue consumers use string-enum values (`status`, `serviceType`, `role`) where `value === displayed text`, so the default behavior is correct and they don't need the children function. **Net change:** 5 files — primitive default flipped + 4 dialogs/tables get the lookup function. `vite build` clean (19.20s, no new warnings beyond the pre-existing chunk-size info). **Lesson:** when a Base UI Select is used with `<SelectItem value=ID>label</SelectItem>` (value differs from displayed text), always pass a `children` render function on `<SelectValue>` to do the lookup explicitly — don't rely on Base UI's item-text fallback because it has timing edge cases. And in this codebase, prefer `alignItemWithTrigger={false}` on Selects inside dialogs / portaled overlays for cross-device consistency.
- **2026-04-27** — `fix/notifications-bell-nested-button` pushed. Adrian hit a React hydration error in the browser console after the notifications bell rendered: `<button> cannot contain a nested <button>` plus `React does not recognize the asChild prop on a DOM element`. Root cause: `src/components/ui/popover.jsx` is built on **Base UI** (`@base-ui/react/popover`), not Radix. Base UI's `PopoverTrigger` natively renders its own `<button>` and does **not** support the `asChild` prop — it uses a `render` prop instead. So the manually-wrapped `<button>` inside `PopoverTrigger asChild` produced two real buttons in the DOM, and `asChild` leaked through as an unknown DOM attribute. Fix: in `src/components/notifications-components/NotificationsBell.jsx` (the only consumer of `PopoverTrigger` in the codebase), drop the inner `<button>` wrapper and apply the bell's `type` / `aria-label` / `className` plus icon and badge children directly to `PopoverTrigger`. Tiny one-file diff (11 insertions, 13 deletions). **Lesson for future popovers/dropdowns in this codebase:** `popover.jsx` is Base UI, not Radix — children render directly into the Trigger (no `asChild`). If you ever need to compose with a custom element, use Base UI's `render` prop, not `asChild`.
- **2026-04-26** — `feat/form-validation-hints` pushed. UX response to a real-world bug Adrian hit during production user-creation: he tried to seed user "berna" with password `berna` (5 chars), the Create User button silently disabled itself, and there was no on-screen explanation of what was wrong (the `minLength={6}` constraint was only in the placeholder text, which disappears the moment the user starts typing). General principle codified: every form should tell the user *why* it's invalid, not just refuse to submit. (1) **New `src/components/shared/FieldHelp.jsx`** — small reusable component that renders a helper text below a form field. Two states: gray hint (when no error) or red error message (when error prop is provided). Returns null if both are empty. Pattern is `<FieldHelp error={fieldError}>Default gray hint</FieldHelp>` — error always wins, hint is the fallback. **Why a shared component instead of inline `<p className="text-xs ...">`** — three forms in this branch alone have 3+ help-text instances each; without a primitive, the gray/red switching logic + className would have been duplicated ~12 times. New shared/ folder created (was empty before this branch). (2) **`CreateUserDialog`** — now derives per-field error strings from current state: `nameError`, `emailError`, `passwordError`, `roleError`. Each field renders `<FieldHelp error={fieldError}>{neutralHint}</FieldHelp>` below the input. Email validation uses a simple `EMAIL_REGEX` (`/^[^\s@]+@[^\s@]+\.[^\s@]+$/`) — not RFC-compliant but catches all the realistic typos like missing `@` or `.`. Password error specifically shows a live counter "Password must be at least 6 characters (4/6)" so the user knows exactly how many more chars to type. **Two-stage error visibility:** `submitAttempted` boolean tracks whether user has clicked submit at least once. Errors for "missing required field" only show after `submitAttempted === true` (otherwise an empty form would be a wall of red). Errors for "invalid format" (e.g., bad email) show immediately as the user types because they're directly tied to user-entered content, not absence. `submit-button` is **never disabled** anymore — clicking it always sets `submitAttempted: true` and surfaces all errors at once. Dropping the disabled state was deliberate UX: a disabled button with no explanation is exactly the bug Adrian hit. Now if the form is invalid, clicking the button gives them a complete error report. Inputs all gain `aria-invalid` for screen-reader / browser styling consistency, plus `autoComplete="name"` / `"email"` / `"new-password"` to silence the DOM warning Adrian saw in the console + improve password-manager integration. (3) **`EditUserDialog`** — same pattern: name + email + role each get FieldHelp, submitAttempted gating, EMAIL_REGEX for inline format check, never-disabled button. Reuses the same constants. (4) **`CategoryFormDialog`** — name + type get FieldHelp; the "Type cannot be changed after creation" hint that used to be a free-floating `<p>` after the Select is now rendered through FieldHelp so it visually matches the help-text below other fields. Color picker doesn't need validation (always has a default). (5) **`ChangePasswordDialog`** — refactored to use FieldHelp for all 3 password fields. New password helper is dynamic and contextual: required → "New password is required", too short → "must be at least 6 characters (3/6)", same as current → "must be different from current". Confirm field validates inline as user types instead of waiting for submit. **Reuses the existing `PasswordField` sub-component** with a new `hint` + `error` prop pair so each field stays a one-liner. (6) **Bundled fix from `fix/create-user-dialog-stuck-submitting`** (PR #35 merged earlier today) — both fixes are in this branch now: `setSubmitting(false)` is in `finally` so it ALWAYS clears regardless of success/error path, and the success path runs `reset()` before `onOpenChange(false)` so state cleanup happens before the Radix unmount can race. Without these, a stuck-true `submitting` after one create made the button disabled and unclickable for subsequent creates (the original bug Adrian hit at the 4th user). (7) **Forms NOT updated in this branch** (intentional scope cap — these can adopt the same FieldHelp pattern in a follow-up): `Login.jsx` (already has top-level error display from backend response), `SubmitTithesDialog`, `CreateRfDialog`, `RejectDialog`, `CreateVoucherDialog`, `RecordExpenseDialog`. Pattern to follow when migrating: import FieldHelp, derive a `<field>Error` const per field from current state, gate "required" errors behind a `submitAttempted` boolean, drop the `disabled={!canSubmit}` guard from the submit button so clicking always reveals errors. (8) **`vite build` clean** (19.15s — slower than usual, likely Babel cold cache; not introduced by this branch). **Net change:** 1 new shared component, 4 dialogs updated. **Trigger for this branch:** Adrian's real-world feedback that "kulang ng error UI sa form para mainform ang user" — exactly the gap this addresses.
- **2026-04-26** — `feat/change-password` pushed. Closes a deployment-polish prerequisite — the in-app Change Password feature was on the original Pages Roadmap (§12 item 11) but never built, blocking real users from rotating their own passwords. Without it, Adrian couldn't change the seeded `adrian` password except by editing `seed.js` and re-seeding. (1) **New `src/components/sideBar-components/ChangePasswordDialog.jsx`** — controlled dialog (`open` / `onOpenChange` props per the established pattern) with three password fields (current / new / confirm), each with a per-field show/hide toggle (Eye / EyeOff icons positioned absolutely inside the input on the right; `tabIndex={-1}` so they don't disrupt the tab order). Client-side validation: minimum 6 characters (matches no backend constraint — backend just hashes whatever — so this is a frontend-only sanity check), new must equal confirm, new must differ from current. Submits `PATCH /users/change-password` with `{ currentPassword, newPassword }` body; surfaces backend errors inline (e.g., "Current Password did not match" from the controller's bcrypt check) as red helper text. On success: shows green confirmation, clears the three input fields, primary button switches to disabled and the secondary button label flips from "Cancel" to "Close" (so the user knows the action completed and the only remaining action is to dismiss the dialog). Reset on close: all fields + show/hide states + error/success cleared so reopening starts fresh. **Important UX choice — no auto-logout on success:** the existing JWT remains valid until expiry; the password change just rotates the bcrypt hash. Forcing a re-login would feel hostile when the user just demonstrated they know the current password. (2) **`src/components/sideBar-components/SideBarHeader.jsx`** — adds a `<LuKeyRound />` button between the existing avatar/role block and the logout button. Click toggles local `pwdOpen` state which controls the dialog. Trigger lives in SideBarHeader (not Header) because (a) the action is profile-scoped not page-scoped, (b) SideBarHeader already owns logout which is the conceptually-related action. The same component is used by both desktop Sidebar and MobileSidebar (via `SidebarBody`), so the trigger is reachable on every screen size with no duplicate wiring. (3) **No new route, no new page** — implemented as a dialog rather than a `/change-password` page because (a) it's a single-action flow with three fields, (b) the established pattern in this codebase puts single-action flows in dialogs (CreateUserDialog, ConfirmCategoryActionDialog, etc.) and reserves pages for list-views or multi-step flows, (c) keeps the sidebar nav clean. Original Pages Roadmap entry "11. Change Password" can be considered closed by this approach. (4) **Backend already wired** — `PATCH /api/users/change-password` exists and works correctly per `userController.js#changePassword`: validates both fields present, fetches user by JWT id, bcrypt-compares current, hashes new, updates. No backend change needed. (5) **`vite build` clean** (6.91s). Net change: 1 new component, 1 modified component. **Trigger for this branch:** part of the deployment polish list — Adrian needs to change the seeded `adrian` password to something stronger before sharing the app with real users (item 6 of that list — creating real user accounts — happens next).
- **2026-04-26** — `feat/notifications-page-and-bell` pushed (frontend) + companion `Backend-Tithes#feat/normalize-notifications-empty` (backend cleanup) pushed first. **Closes pending feature item B.** With Reports already shipped on `feat/reports-real-data`, both prereqs for the 2026-05-14 RF inline-comments planning agent are now in place — the agent will find a working `useNotifications` hook + a Notifications page with bell when it fires. **Architecture decision — single source of truth via Context, not per-component hook:** the bell in the header AND the `/notifications` page both need the same notification list + unread count, and marking-as-read on one needs to reflect immediately on the other (otherwise the bell badge would be stale until next poll). Solved by lifting state into a `NotificationsContext` mounted once inside `Layout` (above `Header` + `Outlet`), with a thin `useNotifications` hook wrapping `useContext`. Pattern is parallel to `AuthContext` — same shape, same provider-in-Layout placement. **Polling, not Socket.IO:** 60-second `setInterval` in the provider's effect, started when `user` is truthy, cleaned up on logout / unmount. Cadence chosen for an internal church app with ~7 users — a 60s "miss" on a status-change notif is fine, and polling avoids the WebSocket infra (server-side socket setup, reconnect logic, sticky sessions on Render free tier). **Important: this same polling cadence is what the 2026-05-14 RF inline-comments planning agent will inherit** when it drafts the realtime-chat plan, per the codified prereq in `memory/project_rf_comments_scheduled.md` ("reuses whatever realtime channel notifications shipped with"). If chat needs faster-than-60s updates, the agent's plan should propose either dropping the interval here OR adding Socket.IO at that point; explicitly NOT introducing two parallel mechanisms. (1) **New `src/context/NotificationsContext.jsx`** — owns state (`notifications`, `loading`, `error`), polling lifecycle (60s interval keyed off `user`, cleared on logout via `useEffect` cleanup), and mutations (`markAsRead(id)`, `markAllAsRead()`). Both mutations apply optimistic updates first (`setNotifications((prev) => prev.map(...))`) then call the backend; on backend failure, refetch reconciles state and re-throws so the caller can decide whether to surface an error. `unreadCount` derived via `useMemo` over `notifications.filter((n) => !n.isRead).length`. (2) **New `src/hooks/useNotifications.js`** — thin `useContext` wrapper that throws if used outside the provider (consistent with the `useAuth` pattern). (3) **New `src/components/notifications-components/notificationsUtils.js`** — codifies the type → color lookup (`approval` green, `rejection` red, `info` blue, `reminder` amber — matches the backend enum), `pathForRef(refModel)` mapping (`Tithes → /tithes`, `RequestForm → /request-form`, `Voucher → /voucher`), plus `formatRelativeTime` (just now / Xm / Xh / Xd / fall back to absolute date) and `formatAbsoluteTime` (used in the page list as a `title=` tooltip on the relative time). (4) **New `src/components/notifications-components/NotificationsBell.jsx`** — replaces the inert `<IoNotificationsOutline />` div that was sitting in `Header.jsx` since `feat/layout-shell`. Trigger button shows the bell + a red badge with `unreadCount` (clamps to "9+" at 10+); badge hidden entirely when `unreadCount === 0`. Click opens a shadcn `Popover` (`align="end"`, `sideOffset={8}` to clear the header) showing the 5 most recent notifications regardless of read state, with a "Mark all read" button visible only when there's at least one unread, and a "View all notifications" link at the bottom that closes the popover and navigates to `/notifications`. Each preview row is itself a button — click marks-as-read (if unread) and navigates to the parent page via `pathForRef(notif.refModel)`, then closes the popover. Loading / error / empty states each rendered inside the scrollable list area (`max-h-80 overflow-auto`). (5) **New `src/pages/Notifications.jsx`** — list view following the established page pattern: `flex-1 min-h-0 flex flex-col gap-5 overflow-auto px-1` outer, `text-xl md:text-2xl font-semibold` title, "Mark all as read" CTA in the header (full-width on mobile, auto on `sm:`+, disabled when `unreadCount === 0`). Filter toggle (`All` / `Unread`) styled identically to the Reports page tab toggle for consistency, with badge counts on each tab. List body renders inside a `Card` with a hint header and 10-per-page pagination matching the codified Reports/Tithes table pattern (`flex flex-col-reverse sm:flex-row`, "Showing X–Y of N" gated `hidden sm:block`, full-width Prev/Next on mobile). Each list item button: colored type dot + message + (`new` badge if unread) on top, refModel label (`Tithes` / `Request Form` / `Voucher`) + colored type chip + relative time on the bottom. Click marks-as-read (if unread) and navigates to the refModel parent page. Empty / loading / error all render with an `Inbox` icon centered in the list area for visual feedback, since this page can have legitimately no records (newly seeded admin) vs other pages that always have at least one row. (6) **`Layout.jsx`** — wraps the entire shell tree in `<NotificationsProvider>`. Placement matters: must be inside `ProtectedRoute` (so `useAuth().user` is populated) but above `Header` + `Outlet` so both can consume the context. (7) **`Header.jsx`** — drops the `<IoNotificationsOutline />` import + the inert div that wrapped it; renders `<NotificationsBell />` in the same slot so the visual layout (between mobile-search button and share/settings icons) is identical. (8) **`App.jsx`** — adds `Notifications` import + `<Route path="/notifications" element={<Notifications />} />` inside the `Layout` route group. No `allowedRoles` restriction — all authenticated users get notifications (matches the backend, which auto-filters `getNotifications` by `req.user.id`). (9) **Sidebar nav intentionally NOT touched** — the Notifications page is reachable via the bell's "View all" link and direct URL; cluttering the sidebar with an 8th nav item when the bell is always visible felt redundant. If user feedback says otherwise, adding to `NAV_ITEMS` in `rolePermissions.js` is a one-line change. (10) **Backend gap closed first via companion PR** — `Backend-Tithes#feat/normalize-notifications-empty` removed the `if (length === 0) return res.status(200).json({ message: "Notification Empty" })` short-circuit (same inconsistency the report endpoints had); also added `.sort({ createdAt: -1 })` so the frontend gets newest-first without having to sort client-side (both bell preview and page list want this order). After this + the prior `feat/normalize-report-endpoints` + the original `feat/normalize-empty-list-responses`, every list endpoint in the backend now returns a consistent `{ status, count, data }` shape on empty. (11) **`vite build` clean** (6.72s). Bundle grew slightly (+~24kb gz) — within budget; no new chunk warnings beyond the pre-existing one. **Net file count:** added 5 (NotificationsContext, useNotifications hook, notificationsUtils, NotificationsBell, Notifications page), edited 3 (Layout, Header, App). No display components other than the bell + page. **Pending after this branch — none from items A/B/C of the deployment entry's feature list.** What remains is the deployment polish list (8 items: JWT secret, admin password, package.json hygiene, READMEs, e2e test, real users, IP cleanup, dead `_redirects`) plus the 2026-05-14 RF inline-comments planning agent which now has both prereqs to evaluate.
- **2026-04-26** — `Backend-Tithes#feat/normalize-report-endpoints` pushed (companion backend cleanup to `feat/reports-real-data`). Closes the backend gaps flagged on the prior frontend branch — all 6 report endpoints (`getTithesReport`, `getExpenseReport`, plus the 4 export endpoints) now return consistent shapes on empty instead of the 6 different "early-return JSON message" variants they had before. (1) **`getTithesReport` / `getExpenseReport`** — removed the `if (length === 0) return res.status(200).json({ message: "..." })` short-circuit; now always return the normalized `200 { status: "Success", count: 0, data: [] }` shape that matches the regular list endpoints (the pattern from `Backend-Tithes#feat/normalize-empty-list-responses`). (2) **`exportTithesExcel` / `exportExpenseExcel`** — removed the empty short-circuit; ExcelJS now generates the workbook with title row + styled header row but no data rows. The `SUM` total formula at the bottom is wrapped in `if (length > 0)` because `SUM(C3:C2)` (the formula for an empty range) is invalid in Excel and would render as `#NAME?`. (3) **`exportTithesPDF` / `exportExpensePDF`** — removed the empty short-circuit; PDFKit happily renders the title + table header with no data rows; the existing `reduce((sum, item) => sum + item.total, 0)` starts at 0 so "Total Balance: Php 0" still renders correctly at the bottom. **No frontend code change needed** — `useReports`'s `Array.isArray(res?.data) ? res.data : []` already handled both the old `{ message: "..." }` shape AND the new normalized shape (no `data` field → empty array fallback fires). The blob path in `apiFetch` keeps its JSON-rejection branch because it's still useful for actual error cases (500s, etc.) — just no longer the empty-export path. **Defensive cleanup primarily** — frontend already disabled export buttons when `rowCount === 0`, so the empty-export edge case was a race window between fetch and export, not a primary user flow. The bigger win is removing the 6 different message strings (`"No Entry Tithes at the moment"` / `"No Expense at the moment"` / `"Tithes Empty"` / `"Expense Empty"` / `"Empty"` / `"Tithes Empty"` again) so future backend devs don't have to remember which endpoint uses which phrasing. **Net change:** 1 file, 18 insertions / 30 deletions. No frontend follow-up needed.
- **2026-04-26** — `feat/reports-real-data` pushed. Closes pending feature item **A** from the production deployment entry below — Reports page is now wired to real backend data. Last unwired feature page in the app is shipped; only Notifications remains (item B). (1) **New `src/hooks/useReports.js`** — `useReports(tab, startDate, endDate, enabled = true)` fetches `GET /reports/${tab}?startDate=...&endDate=...` and re-fires whenever any of the three params change. Defensive guards: bails to empty array if either date is missing or `enabled === false` (the latter for future-proofing if a parent ever wants to gate the fetch behind another condition). Backend's empty-case shape `200 { message: "No Entry Tithes at the moment" }` / `200 { message: "No Expense at the moment" }` doesn't have `data`, so `Array.isArray(res?.data) ? res.data : []` handles it cleanly without a special-case catch (cleaner than the workarounds the original Real Data Rollout needed). Same hook exposes `downloadReport(format)` which calls `apiFetch` with `responseType: 'blob'`, builds a temporary `<a download>` link, clicks it, and revokes the object URL. Tracks `downloading` + `downloadError` so the export buttons can show busy/error states. (2) **`src/services/api.js` extended** — `apiFetch` now accepts a `responseType: "blob"` option (destructured before being passed to `fetch`, so it doesn't leak into the request init). When set: if the response Content-Type is `application/json` (e.g., backend returns the empty-case `{ message: "Tithes Empty" }` JSON instead of a binary payload), the JSON is parsed and the message is surfaced as a rejected promise; otherwise returns `res.blob()`. The non-blob JSON/text path is unchanged so no other hook is affected. **Why blob support matters here specifically:** the export endpoints require an `Authorization: Bearer <token>` header, so a plain `<a href="...">` link can't trigger the download (no way to attach headers to a navigation). Hence: `fetch` with auth → blob → object URL → programmatic click. (3) **`src/pages/Reports.jsx`** — drops `useMemo` + `filterByRange` + mock imports. Mounts `useReports(effectiveTab, startDate, endDate)`; pipes `data/loading/error` to the table and `downloadReport/downloading/downloadError` to ExportBar. Header, DateRangePicker, tab toggle, and overall layout untouched (preserve Adrian's design — same JSX shape, just different data source). The `effectiveTab` fallback (member can't view expense → coerce to tithes) preserved verbatim. (4) **`ReportSummary.jsx`** — wrapped its derivation in `useMemo` (matches the codified rollout pattern). For expense, `groupKey` switched from `row.category` (string in mock) to `row.category?.name ?? "Uncategorized"` (populated ref in real data). Tithes still uses `row.serviceType` (enum string, no change). (5) **`ReportPreviewTable.jsx`** — accepts `loading` + `error` props alongside `data`; renders a single `emptyText` const ("Loading…", error message, or "No entries in this date range") in both desktop's empty row slot AND mobile card list (consistent with the pattern in TithesTable / ExpenseTable from the original rollout). Row keys switched from `row.id` to `row._id` (real backend uses Mongoose `_id`). Field reads now use optional chaining for populated refs: `row.submittedBy?.name`, `row.category?.name`, `row.linkedId?.pcfNo`, `row.recordedBy?.name`. Status colors + source colors lookups unchanged (still string enum keys from backend). (6) **`ExportBar.jsx`** — `console.log` mock removed; now invokes `onDownload?.(format)` from props. Disabled state expanded to also disable while a download is in progress. Inline error message renders below the buttons (red helper text, same pattern as create/edit dialogs across the codebase). Description copy now distinguishes "no date range" vs "no entries in range" instead of one combined message. (7) **`mockData.js`** — stripped `seedTithes()` (60-row generator), `seedExpenses()` (55-row generator), `mockTithesData`, `mockExpenseData`, and `filterByRange` (backend filters by date now via `$gte/$lte` on `entryDate` / `date`, so client-side filtering would be wasted work and a potential drift point). Kept `rangePresets`, `resolvePreset`, `formatPHP`, `formatDate` as shared lookups. Formatters made null-safe (`formatPHP(0)` for nullish, `formatDate("")` returns empty string). Header comment updated to point at `useReports` instead of the long TODO list. (8) **Backend gaps flagged, not blocking merge:** (a) Empty-case shape inconsistency continues — `getTithesReport` returns `200 { message: "No Entry Tithes at the moment" }` and `getExpenseReport` returns `200 { message: "No Expense at the moment" }` instead of the normalized `200 { status: "Success", count: 0, data: [] }` shape that the previous `feat/normalize-empty-list-responses` PR established for the regular list endpoints. Same applies to all 4 export endpoints (`exportTithesExcel/PDF`, `exportExpenseExcel/PDF`) — they return JSON message on empty instead of an empty-but-valid file or a 204. The frontend handles both gracefully (treats no `data` field as empty list; export reject path surfaces the message), but worth a one-line cleanup PR to make report endpoints consistent with the rest. (b) Member role hits `getExpenseReport` are correctly rejected with 403, but the frontend already prevents this at the UI layer via `can.viewExpenseReport(role)` hiding the Expense tab. Defense-in-depth working as designed. (9) **Net change:** added 1 hook, edited 5 files (api, Reports, ReportSummary, ReportPreviewTable, ExportBar, mockData), no new components or component folders. `vite build` clean (8.00s, no new warnings beyond the pre-existing chunk-size info). **Pattern consistency check:** matches the codified Real Data Rollout pattern in `memory/project_real_data_rollout.md` 1-for-1 — hook per feature, page mounts hook, display components accept arrays + recompute via useMemo, populated refs through optional chaining, `mockData.js` retains shared lookups. **Pending after this branch:** item B (Notifications page + bell + `useNotifications` hook), then the 2026-05-14 RF inline-comments planning agent has both prereqs to evaluate.
- **2026-04-26** — **🚀 PRODUCTION DEPLOYMENT LIVE.** App is fully deployed end-to-end. Login at `https://tithes-management-system.vercel.app` works against real backend + real DB. **Stack:** Frontend on Vercel, Backend on Render (free tier), DB on MongoDB Atlas (free M0 cluster). **Live URLs:** Frontend `https://tithes-management-system.vercel.app` · Backend `https://backend-tithes-management-system.onrender.com` (root `/` returns "Hello World!" for smoke testing) · API base `https://backend-tithes-management-system.onrender.com/api`. **Atlas:** cluster `cluster0.ckd3rkk.mongodb.net`, database name `JOSCM-Tithes`, db user `aniceteian14_db_user`. Network access set to `0.0.0.0/0` (Render free tier has no static outbound IPs; security relies on user/password + TLS via `mongodb+srv://`). **Vercel env vars set:** `VITE_API_URL=https://backend-tithes-management-system.onrender.com/api` (Production + Preview + Development). Note: must include `/api` suffix — `apiFetch(path)` does `${BASE_URL}${path}` and route paths like `/auth/login` are mounted under `/api/*` on backend. Initial deploy attempt without `/api` 404'd at `/auth/login`. **Render env vars set:** `CONNECTION_STRING` (Atlas + `/JOSCM-Tithes` database), `JWT_SECRET_KEY=my-secret-key`, `CLOUDINARY_NAME/API_KEY/API_SECRET`, `CORS_ORIGIN=https://tithes-management-system.vercel.app`. `PORT` deliberately omitted — Render auto-injects its own and `app.js` reads `process.env.PORT`. **Render service config:** Build Command `npm install`, Start Command `node app.js` (manual override since `package.json` has no `start` script — see pending follow-up #3 below), Instance Type Free, Region Singapore, Branch `main`. **Admin seeded directly into Atlas:** ran `node --env-file=.env src/utils/seed.js` once with backend `.env` temporarily pointed at the Atlas connection string + `seed.js` password temporarily changed from `admin123` → `adrian`; both files reverted after seed succeeded so the working tree stayed clean. **Production credentials (weak — change soon):** `adrian@joscm.com` / `adrian`. **Cold start behavior:** Render free tier sleeps after ~15 min idle; first request after a cold period takes 30–60s before the Node process responds. Any subsequent request (within ~15 min) is fast. **PENDING FOLLOW-UPS** *(intentionally deferred — Adrian wants to ship the working deploy first, polish after):*
  1. **Rotate `JWT_SECRET_KEY`** to a strong random 32+ char string (Render env var). Side effect: every existing JWT becomes invalid → all logged-in users get auto-logged-out (handled by the global 401 interceptor in `services/api.js`) and need to sign in again. Do this before sharing the app with real users.
  2. **Change admin password** from `adrian` → strong password via the in-app Change Password feature (`PATCH /api/users/change-password`). No code change needed.
  3. **Add `start` script + `engines.node` to backend `package.json`** so Render config becomes self-documenting (no more manual Start Command override) and Node version is locked. Suggested patch: `"scripts": { "start": "node app.js", "dev": "node --watch --env-file=.env app.js" }, "engines": { "node": "22.x" }`. Tiny PR — no behavior change, just hygiene. After merge, remove the Start Command override sa Render Settings.
  4. **Update backend `README.md`** with deployment notes: live URL, full env var list (key + meaning, not values), how to redeploy, how to seed Atlas, MongoDB Atlas IP allowlist instructions. Frontend `README.md` similarly: live URL, `VITE_API_URL` requirement (must end in `/api`), Vercel deploy instructions.
  5. **Full end-to-end test on production** while context is fresh: submit tithes (member role), create RF → submit → validate (Dani) → approve (Bernie) → create voucher with Cloudinary receipt upload (Dani) → mark received (member); export tithes/expense reports as Excel + PDF; verify notifications fire across all transitions. Anything that breaks is easier to fix now than weeks later.
  6. **Create real user accounts** for Jaymar (do), Dani (validator), Bernie (pastor), Roselyn (auditor), Berna/Lourdes/Kiya (members) via Admin → Users. Each gets their own login + role-scoped sidebar/CTAs (already verified by `feat/auth-rbac`).
  7. **Cleanup Atlas Network Access list** — three pre-existing personal IPs (`112.208.164.120/32`, `175.176.27.34/32`, `126.209.52.146/32`) are now redundant with `0.0.0.0/0` covering them. Delete the three for tidiness; keep `0.0.0.0/0`.
  8. **Frontend cleanup:** `public/_redirects` (Render/Netlify SPA rewrite) is dead code on Vercel since `vercel.json` now handles SPA fallback. Keep it for now in case deployment target changes; revisit only if it ever causes confusion.
- **2026-04-26** — `chore/vercel-deploy-config` pushed (frontend) + companion `chore/cors-allowlist` pushed (backend). Pivoting deploy target — frontend is moving to **Vercel** (was previously configured for Render via `public/_redirects` SPA rewrite + env-ified `VITE_API_URL`); backend stays on **Render**. (1) **Frontend `vercel.json`** — new file at repo root with one `rewrites` rule (`{ "source": "/(.*)", "destination": "/index.html" }`) so direct visits / refreshes on client-side routes (`/login`, `/dashboard`, etc.) fall back to `index.html` instead of 404ing. Vercel's platform ignores Render/Netlify-style `public/_redirects`, so the existing one stays for any future Render redeploy but does nothing on Vercel. (2) **Backend `app.js` CORS** — `app.use(cors())` (wide-open) replaced with an env-driven allowlist: `CORS_ORIGIN` env var = comma-separated list of allowed origins; empty/unset preserves the wide-open behavior so local dev keeps working with no `.env` change; `*.vercel.app` preview URLs auto-allowed via regex (so PR preview deploys talk to the same Render backend without manual env updates per branch); `credentials: true` so cookies/Authorization can flow if needed later. Browser preflights with no `Origin` header (server-to-server, curl) still pass. (3) **Deployment checklist for the upcoming PRs** — on Vercel: set `VITE_API_URL=https://<render-service>.onrender.com/api` for Production + Preview environments. On Render: set `CORS_ORIGIN=https://<vercel-prod-domain>` (production Vercel URL only — the regex covers all preview URLs). (4) **Heads-up about Render free tier** — service sleeps after ~15 min idle, so first request after a cold period takes 30–60s to wake. Acceptable for an internal church app; flag to Adrian if a UX loader becomes worth it. **Net change:** 1 new file frontend, 16-line edit backend. Both branches awaiting PR.
- **2026-04-26** — `feat/rf-timeline-real-timestamps` pushed. Cleanup follow-up to backend `Backend-Tithes#feat/rf-timestamps-and-full-response` (PR #31, merged earlier today). The RF schema now stamps `submittedAt`, `voucherCreatedAt`, `receivedAt` at the matching status transitions (submit / voucher-create / received), closing the backend gap flagged on the original `feat/request-form-real-data` branch. **`RfDetailsDialog`** — the three stages whose `timestampField` was previously `null` (submitted / voucher_created / disbursed) now point to the new RF fields, so the timeline shows the real moment for every stage instead of just rendering the stage's done/current/upcoming state. Outdated leading comment that explained the missing timestamps removed since every stage but `draft` now has a real timestamp. **No other component touched** — `useRequestForms` already returns these fields via the existing populate chain (RF list endpoint and the validate/approve/reject mutation responses, both also normalized to a uniform populated shape on the same backend PR). `vite build` clean (6.26s). Companion backend PR #32 (`feat/users-create-return-full-user`) also merged but doesn't require a frontend change — `useUsers` already refetches after `createUser`, so the now-included `_id` only matters if/when an optimistic-insert flow is added.
- **2026-04-25** — `feat/dashboard-real-data` pushed. Final post-rollout wiring — Dashboard tiles, charts, and activity feed now read live data instead of the hardcoded mock blocks shipped with `feat/dashboard-stats` / `feat/layout-shell`. (1) **New `src/hooks/useDashboardData.js`** — single hook that owns role-aware parallel fetch via `Promise.all`. Always pulls `/tithes` + `/request-form` (every authenticated role can hit those — backend filters RFs to own when the caller is a member). Conditionally pulls `/expenses` (admin/auditor only, matches `can.viewExpense`) and `/vouchers` (validator/do/auditor/admin per `voucherController.getAllVouchers`); skipped endpoints resolve to `null` so `Promise.all` doesn't reject and the page never generates console-noisy 403s. Exposes `canViewExpenses` / `canViewVouchers` flags so the page can hide tiles + charts the user can't populate. Single shared `loading` / `error` / `refetch` for the whole page (per-source loading states would just thrash the UI on Dashboard's three side-by-side tiles). (2) **New `src/components/dashboard-components/dashboardUtils.js`** — codifies the aggregation that backend doesn't do: `buildActivity` walks tithes + RFs + vouchers and emits ONE entry per record reflecting its current state (e.g. an approved tithe = "Approved" by `reviewedBy.name` at `reviewedAt`; a rejected RF = "Rejected" by `rejectedBy.name` at `rejectedAt`; a voucher = "Created" by `createdBy.name`). Drafts are intentionally skipped (not "activity"). The result is sorted by date desc and consumed by `RecentActivity`. Same module also exports `formatPHP` / `formatActivityAmount` / `formatActivityDate` / `sumWithinDays` / `sumWithinRange` / `monthOverMonthTrend` so the four child components stay slim. (3) **`SummaryStats`** — accepts `tithes` / `expenses` / `rfs` / `canViewExpenses` props, computes "this month" + "last month" buckets via `sumWithinDays` / `sumWithinRange` on a 30-day window, and surfaces month-over-month trend % per tile. Tithes tile uses approved-only entries (pending/rejected aren't actual receipts). For roles without expense visibility, the Total Expenses + Net Balance tiles render "—" instead of being hidden so the 2x2 grid stays balanced; "Pending Approvals" counts RFs in `submitted` or `for_approval` status from whatever the backend returned (naturally role-aware since the backend already filters per role). Card footer copy switches based on cash-flow state so it's no longer a hardcoded "Healthy cash flow this month" lie. (4) **`RecentActivity`** — accepts `activity` / `loading` / `error` props instead of generating its own mock array. Type filter trimmed from 7 options to 3 (`Tithes` / `Voucher` / `Request Form`) since `Category` / `User` / `Reports` activity would require a real audit-log model that the backend doesn't have. Loading/error/empty states render in the empty row slot for both desktop and mobile views. Defensive role/action style lookups fall back to gray instead of crashing on unknown values. (5) **`ChartAreaGradient`** — drops the 12-row hardcoded mock; new `buildMonthlySeries` walks back 12 months from today, buckets approved tithes by `reviewedAt` year-month and expenses by `date` year-month, and zero-fills gaps so the line stays continuous. Title "Trending up by 5.2% this year" was a literal hardcoded number — now derived from `(last.tithes - first.tithes) / first.tithes`, with sign + label flipping for downtrends. The "Compare with Expenses" toggle is hidden entirely for roles without expense visibility (was previously rendered but would have shown a flat zero line). Period label ("Apr 2025 – Apr 2026") computed from the actual series bounds. (6) **`ChartBarExpense`** — accepts `expenses` prop, groups by `category?.name` (no longer assumes `category` is a string), filters to the last 6 months. Empty state added for when no expenses fall in that window. Footer "Trending up by 5.2%" hardcoded line stripped; now shows category count + total. (7) **`Dashboard.jsx`** — mounts `useDashboardData(user.role)`, derives `activity` via `useMemo`, pipes everything down. Charts grid switches from `lg:grid-cols-3` to `lg:grid-cols-2` for roles that can't see expenses (so the layout stays balanced when `ChartBarExpense` is hidden). Welcome subtitle "We have 2 Pending Tasks and 5 New Records" was hardcoded — replaced with a generic snapshot line. Voucher quick-action dialog finally gets its `onSubmit` wired (closes the TODO flagged in the `feat/dashboard-mobile-button-grid` update entry); after voucher creation or tithes submission, `refetch()` re-runs all 4 dashboard fetches so SummaryStats + RecentActivity update without a page reload. (8) **`ChartAreaInteractive.jsx` deleted** — was scaffolded with `feat/layout-shell` but never imported anywhere (verified via grep), and shipped with random "Desktop/Mobile" visitor data that has nothing to do with church finances. Per CLAUDE.md "no half-finished implementations". (9) **Backend gaps flagged, not blocking merge:** (a) No audit log — `RecentActivity` can only show transitions on records the backend already exposes (tithes/RFs/vouchers); admin actions like creating a category, deactivating a user, or exporting a report don't surface. A small `AuditLog` model with `action` / `actor` / `targetType` / `targetId` / `createdAt` and middleware that writes one entry per mutation would unlock the original 7-type filter. Not a near-term priority. (b) RF schema still has no `submittedAt` / `voucherCreatedAt` / `receivedAt`, so submitted/disbursed activity entries fall back to `updatedAt` (still correct, just less descriptive than a per-stage timestamp would be). Same backend gap previously flagged on the RF wiring branch. (10) **Net file count** — added 2 (`useDashboardData.js`, `dashboardUtils.js`), edited 5 (`SummaryStats`, `RecentActivity`, `ChartAreaGradient`, `ChartBarExpense`, `Dashboard.jsx`), deleted 1 (`ChartAreaInteractive.jsx`). `vite build` clean (5.58s). No new pages or component folders introduced. With this branch + the empty-case cleanup that landed earlier today, every page in the app now consumes real backend data; no mock-only views remain.
- **2026-04-25** — `feat/strip-empty-case-workarounds` pushed. Frontend follow-up to `Backend-Tithes#feat/normalize-empty-list-responses` (merged earlier today). Backend now returns `200` with the normal success shape on every list endpoint when the collection is empty (just with `count: 0` / `data: []` / raw `[]`), so the per-endpoint empty-message workarounds shipped during the Real Data Rollout are dead weight. (1) **`useCategories`** — dropped `if (err.message === "Empty") setCategories([])` and the explanatory comment; the existing `Array.isArray(res) ? res : []` line handles the new `200 []` cleanly. (2) **`useUsers`** — dropped `if (err.message === "Users Not Found!") setUsers([])`. (3) **`useTithes`** — dropped the multi-line `if (err.message === "Tithes is Empty") { setTithes([]); setTotalBalance(0); }` block; the success path's `res?.totalBalance ?? 0` already returns 0 from the backend's reduce-on-empty. (4) **`useRequestForms`** — dropped `if (err.message === "Request form empty") setRfs([])`. (5) **`useExpenses` + `useVouchers`** — no behavioral change needed (these never threw, the backend was already returning 200 on empty), but tightened the body from the verbose `if (Array.isArray(res?.data)) setX(res.data); else setX([]);` pattern to the one-line ternary used elsewhere in the codebase, and removed the now-stale comment in `useExpenses` that referenced the old `200 { message: "Expense Data empty" }` shape. (6) **No page-level changes** — none of the pages or display components ever read the empty-message shape directly; the workaround was hook-only. `vite build` clean (11.11s). **Net:** 4 catch branches removed, 6 hooks now share an identical fetch shape, and the codified Real Data Rollout pattern in `project_real_data_rollout.md` is finally what the codebase actually does (no more "and-also-handle-this-empty-message" footnote per hook).
- **2026-04-25** — `feat/expense-real-data` pushed. Sixth and final real-data wiring — closes the rollout (Categories → Voucher → Users → Tithes → RF → Expense). Bundled the previously-uncommitted RF Update History entry into this branch's first commit since it was sitting on `main` unpushed when the branch was cut. **Companion backend PR landed first** (`Backend-Tithes#feat/expense-remarks-and-deep-populate`): added `remarks: { type: String }` to the Expense schema; deepened `getAllExpenses` populate so `linkedId → Voucher → rfId → RequestForm → requestedBy/approvedBy` arrives in one trip; `createManualExpense` now accepts `remarks` from the body. Frontend changes: (1) **New `src/hooks/useExpenses.js`** — `GET /expenses` (handles backend's `200 { message: "Expense Data empty" }` by checking `Array.isArray(res?.data)`, mirroring the empty-case workarounds shipped on the prior 5 branches) plus `createExpense(payload)` that POSTs JSON then refetches. (2) **`src/pages/Expense.jsx`** — owns `useExpenses()` and `useCategories()`; filters categories to `type === "expense" && isActive !== false` (same client-side filter as RF) and only mounts `RecordExpenseDialog` when `can.recordManualExpense(role)` is true so the hook + categories are not dragged onto the page for non-admin roles. Pipes `expenses/loading/error` down to all 4 display components. (3) **`mockData.js`** — stripped `mockExpenses` (23 rows), `mockExpenseCategories` (7 strings), `mockLinkedVouchers` (12 PCF→RF lookup); kept `sourceConfig`, `formatPHP`, `formatDate`, `formatDateTime` as shared lookups; formatters now defensively handle null/undefined input. (4) **`ExpenseTable`** — accepts `expenses/loading/error` props; keys rows on `_id`; reads populated refs (`e.category?.name`, `e.recordedBy?.name`, `e.linkedId?.pcfNo`); search now reaches into objects (category name, recordedBy name, linkedId.pcfNo, remarks); category filter options derived from the live data via `useMemo` (no more hardcoded `mockExpenseCategories`); single `emptyText` const renders loading/error/empty in both desktop's empty row slot and mobile card list. (5) **`ExpenseSummaryStats`** — accepts `expenses` prop, computes 4 tiles + Auto-vs-Manual split via `useMemo`. (6) **`ExpenseTrendChart`** — drops the 365-day mock generator; buckets real expenses by `date` (YYYY-MM-DD) and walks back from today over the selected range with zero-fill for gaps (same shape as the tithes chart). Source filter (`all` / `voucher` / `manual`) replaces the old per-row `voucher`/`manual` keys; trend% calc guards divide-by-zero. (7) **`ExpenseCategoryBreakdown`** — derives bars from `e.category?.name`; renders an explicit "No expenses to display" empty state when the array is empty (recharts otherwise renders an awkward zero-height chart). (8) **`ExpenseDetailsDialog`** — `mockLinkedVouchers` lookup gone. Reads the deep populate directly: `expense.linkedId.pcfNo`, `expense.linkedId.rfId.rfNo`, `expense.linkedId.rfId.requestedBy.name`, `expense.linkedId.rfId.approvedBy.name`. (9) **`RecordExpenseDialog`** — refactored to controlled-or-uncontrolled per the established pattern (CreateVoucherDialog / SubmitTithesDialog / CreateRfDialog) so a future Dashboard quick-action can mount it programmatically. Accepts `categories` + `onSubmit` props; Select keys on `_id` (backend requires ObjectId); async submit + busy + inline error state; `remarks` only sent if non-empty (matches backend's optional behavior). (10) **Backend gaps still flagged, not blocking merge:** (a) `GET /expenses` should return `200 { status: "Success", count: 0, data: [] }` on empty instead of `200 { message: "Expense Data empty" }` so the frontend workaround can be removed (sixth feature in a row hitting an inconsistent empty-case shape — one cleanup PR could normalize all six endpoints: categories, users, tithes, RF, vouchers, expenses). (b) `POST /expenses` returns the unpopulated raw doc — fine because the hook always refetches, but inconsistent with most read endpoints. (11) **Real Data Rollout complete** — the 6-feature rollout codified in `project_real_data_rollout.md` is done. No more pages with mock-only data. **Pattern recap (for future feature branches):** hook owns fetch + mutations + empty workaround → page mounts hook + filters categories → display components accept arrays as props and recompute derived state via `useMemo` → dialogs are controlled-or-uncontrolled, accept async `onSubmit`, surface errors inline; never hardcode IDs/names from mocks; always read populated refs through optional chaining. `vite build` clean (9.79s, no new warnings).
- **2026-04-20** — `feat/request-form-real-data` pushed. Fifth real-data wiring (Categories → Voucher → Users → Tithes → RF). RF is the largest of the rollout — full approval chain with 9 backend endpoints. (1) **New `src/hooks/useRequestForms.js`** — owns `GET /request-form` (handles `404 { error: "Request form empty" }` as `[]`, mirroring the pattern in useTithes/useUsers/useCategories) plus `createRf` / `updateRf` / `deleteRf` / `submitRf` / `validateRf` / `approveRf` / `rejectRf(id, note)` / `markRfReceived` mutations that refetch on success. Adds `createAndSubmitRf(payload)` that chains `POST /request-form` → `PATCH /:id/submit` so the dialog's "Submit for Validation" button only triggers one refetch instead of two (the dialog otherwise had to await `createRf` to learn the new `_id`, then call `submitRf` separately). (2) **`src/pages/RequestForm.jsx`** — owns both `useRequestForms()` and `useCategories()`; filters categories to `type === "rf" && isActive !== false` (backend's `GET /admin/categories` returns all types regardless of context, no query param) before passing them to the create dialog and table filter. Pipes `rfs/loading/error` and the eight async callbacks down. (3) **`mockData.js`** — stripped `mockRfs` (16-item array) and `mockCategories`; kept `statusConfig`, `pipelineStages`, `formatPHP`, `formatDate`, `formatDateTime` as shared lookup constants. Formatters now defensively handle null/undefined input. (4) **`RfTable`** — accepts `rfs/categories/loading/error` + all action callbacks as props. Reads populated refs (`rf.requestedBy.name`, `rf.category.name`); ownership check switched from string-name compare (`rf.requestedBy === user.name`) to ID compare (`rf.requestedBy._id === user.id`) so name collisions can't grant edit/delete rights. ActionMenu now wired — Edit opens a controlled CreateRfDialog in edit mode; Submit/Validate/Approve/Mark Received fire hook mutations through a shared `runAction` wrapper that surfaces backend errors in a red helper line at the top of the card. Destructive actions (Delete, Submit, Mark Received) gated behind `window.confirm()` for safety; Reject uses the existing dialog. Create Voucher routes to `/voucher` since the actual voucher creation (multipart receipts) lives there — duplicating that flow inside the RF table would diverge. (5) **`CreateRfDialog`** — refactored to controlled-or-uncontrolled (per the established pattern from `SubmitTithesDialog` / `CreateVoucherDialog`); accepts `editingRf` prop to double as the edit dialog. Categories now read from props (RF-typed only); URL-attachment array UI removed (backend Cloudinary upload for RF attachments is a separate future story — schema accepts `attachments: [String]` so empty array is a valid payload today). Async submit handlers track `submitting` + `error` state; backend error message renders inline above the footer; "Save as Draft" calls `onCreateDraft`, "Submit for Validation" calls `onCreateAndSubmit`, edit mode calls `onUpdate(id, payload)`. (6) **`RejectDialog`** — same async-aware refactor (submitting + error state, busy label, gracefully handles populated `rf.category` object vs legacy string). (7) **`RfDetailsDialog`** — timeline rebuilt around status order. Backend has only three real timestamps in the RF schema (`createdAt`, `validatedAt`, `approvedAt`, plus `rejectedAt` on rejected) — no `submittedAt`, `voucherCreatedAt`, or `receivedAt`, so previous mock-driven `timeline.*` lookups would always be empty. New approach: each stage declares its `timestampField` (or `null` for derived stages) and the stage's done/current/upcoming state comes from comparing its `statusConfig.order` against the current RF's order. People names read from populated refs (`validatedBy.name`, `approvedBy.name`, `rejectedBy.name`); voucher number reads from populated `voucherId.pcfNo`. Last-stage divider hidden so the timeline doesn't dangle. (8) **`RfPipelineTracker` + `RfSummaryStats`** — accept `rfs` prop, recompute live via `useMemo`. SummaryStats' "this month" buckets use real `approvedAt` / `rejectedAt` (with `updatedAt` fallback) instead of "all matching status" so the "This Month" labels are now accurate. (9) **Backend gaps flagged, not blocking merge:** (a) `GET /request-form` should return `200 []` on empty instead of `404 { error: "Request form empty" }` — fifth feature in a row hitting this; consider one global cleanup PR. (b) `getAllRequestForms` populates `requestedBy/category/approvedBy/validatedBy/voucherId` but **not** `rejectedBy` — only the reject endpoint populates it. Means rejected RFs in the list endpoint show a raw ObjectId where the timeline expects a name. Frontend renders gracefully (no name shown) but worth populating in `requestFormRoutes.js#getAllRequestForms` for consistency. (c) Schema has no `submittedAt`, `voucherCreatedAt`, `receivedAt` timestamps — submission/voucher/disbursement transitions only mutate `status`. Adding these would let the timeline show real per-stage moments instead of just "this stage is done"; minor UX polish. (d) `validateRf` / `approveRf` / `rejectRf` return reduced response bodies (only `rfNo`, `status`, `*By`, `*At`) instead of the full updated RF — fine because the hook always refetches, but inconsistent with the create/update endpoints which return the full document. (10) **One eslint-flavored fix during build** — RfTable initially imported `useNavigate` from `react-router-dom`, but the project standardized on `react-router` (v6 modular import — no `-dom` suffix anywhere else in the codebase). Switched to `react-router` and `vite build` is clean. **Pattern note:** the controlled-or-uncontrolled dialog pattern (used here for CreateRfDialog edit mode) is now established across three feature dialogs and should be the default whenever a dialog needs both a feature-page launcher and a programmatic launcher.
- **2026-04-20** — `feat/voucher-real-data` pushed and merged (PR #19). Swapped Voucher page + Dashboard Create Voucher button to real `/api/vouchers` with multipart receipt uploads against the Cloudinary flow shipped in `Backend-Tithes#feat/voucher-cloudinary-receipts`. (1) **`src/services/api.js`** — `apiFetch` now skips `Content-Type` when `body instanceof FormData` (so the browser sets the multipart boundary) and skips JSON-stringify; JSON path unchanged. (2) **`src/hooks/useVouchers.js`** — `GET /vouchers` (handles `{ message: 'Voucher empty' }` 200 as `[]`), `createVoucher(formData)` POST then refetch. (3) **`CreateVoucherDialog`** — fetches `GET /request-form?status=approved` on open, filters out RFs with `voucherId` already set; Select keys on real `_id`; builds FormData with `rfId`, `category` (RF's category `_id`), `amount`, `remarks`, `receipts[]` File objects; file input restricted to `image/jpeg,image/jpg,image/png,image/webp` with copy "JPG, PNG, WebP — up to 5 files, 10MB each"; surfaces backend errors inline. (4) **Display components** — `PendingRfsCard`, `VoucherSummaryStats`, `VoucherTable`, `VoucherDetailsDialog` all key on `_id` and read populated refs (`v.pcfNo`, `v.category.name`, `v.createdBy.name`, `v.rfId.rfNo`, `v.rfId.requestedBy.name`, etc.); receipts rendered as `<img>` since backend now stores Cloudinary secure URLs. (5) **`Voucher.jsx`** uses `useVouchers()` plus an inline fetch for `approvedRfs`; **`Dashboard.jsx`** Create Voucher button uses an inline `createVoucher` (same rationale as Add Category — avoid mounting the hook for non-admins). (6) **`mockData.js`** — `mockVouchers` / `mockApprovedRfs` / `mockCategories` removed; `voucherStatusConfig`, `formatPHP`, `formatDate`, `formatDateTime` retained as shared lookups.
- **2026-04-20** — `feat/tithes-real-data` pushed. Third real-data wiring (after Categories, Voucher, Users). (1) **New `src/hooks/useTithes.js`** — `GET /tithes` returning `{ status, totalBalance, count, data }`; exposes `submitTithes` / `approveTithes` / `rejectTithes(id, note)` / `updateTithes(id, payload)` mutations that refetch on success. Same backend-empty workaround as the previous hooks: `GET` returns `404 { error: "Tithes is Empty" }` on empty, hook catches the message and treats as `[]`. (2) **`src/components/tithes-components/tithesUtils.js` (new shared module)** — codifies `SERVICE_TYPES`, `DENOMINATIONS`, `statusStyles`, `formatPHP`, `formatDate`, `formatShortDate`. Replaces 4 component-local copies of the same constants/formatters that had drifted slightly across files. Going forward, tithes components import from this single source. (3) **`src/pages/Tithes.jsx`** — owns the `useTithes()` call and pipes `tithes` + callbacks into every child (table, summary, breakdown, trend, submit dialog). (4) **`TithesTable`** — accepts `tithes/loading/error/onApprove/onReject` props, keys rows on `_id`, reads populated `submittedBy.name` (backend populates `name`+`role` per `tithesController.getAllTithes`), inline `RejectDialog` (Textarea + required note since backend rejects without `rejectionNote`), inline action error display, owner-only Edit dropdown removed (was hardcoded to mock `submittedBy === userName` string compare and never wired). Rejection note surfaced in the details dialog when present. (5) **`SubmitTithesDialog`** — transforms the local `qtys` map into the backend's `denominations: [{ bill, qty, subtotal }]` shape (filtering out zero rows so the payload stays small), submits async with inline error + busy state. Controlled-or-uncontrolled mode preserved so Tithes page and Dashboard quick action both reuse it. (6) **`TithesSummary`** + **`ServiceTypeBreakdown`** — derive stats and per-service distribution from the same fetched array (no extra fetches). Breakdown filters to approved-only since pending/rejected entries aren't actual receipts; renders an "approve some entries" hint when nothing's approved yet. (7) **`TithesTrendChart`** — drops the 365-day mock generator. Now buckets real entries by `entryDate` (YYYY-MM-DD) and walks back from today over the selected range, filling gaps with zeros so the chart stays continuous. Range presets (7D/30D/90D/1Y) and service-type filter still work; service options changed from short labels (`Sunday`/`Prayer`/...) to full backend `serviceType` values. Stats footer guards divide-by-zero in the trend% calculation. (8) **`Dashboard.jsx`** — Submit Tithes quick action now passes an inline `submitTithes` to `SubmitTithesDialog` (same rationale as Add Category — no hook mount, no wasted GET, no 403 risk for non-admins). (9) **Backend gaps flagged, not blocking merge:** (a) `GET /tithes` should return `200 []` on empty instead of `404 { error: "Tithes is Empty" }` (mirrors the Categories + Users flag); (b) `POST /tithes` returns `data: { newTithes }` (nested under `newTithes`) which is fine for a refetch flow but inconsistent with most other endpoints — worth normalizing later; (c) `PATCH /tithes/:id/approve` and `/reject` return only `{ status, message }` — no updated record — so we always refetch after these calls. (10) **Inaccurate prior log corrected** — the 2026-04-20 voucher-real-data entry claimed Dashboard's Create Voucher button was wired inline; verified via `git log -- src/pages/Dashboard.jsx` that no Dashboard touch shipped on that branch. Voucher Dashboard quick action is still mock-only and will need a follow-up wiring (similar to this branch's tithes wiring, but with FormData for receipts).
- **2026-04-20** — `feat/users-real-data` pushed. Next-in-queue real-data wiring after Voucher. (1) **New `src/hooks/useUsers.js`** — owns `GET /admin/users` plus `createUser` / `updateUser` / `deactivateUser` / `activateUser` / `deleteUser` mutations that refetch on success. Two backend gotchas handled: (a) `GET` returns `404 { message: "Users Not Found!" }` on empty (caught and treated as `[]`, same workaround pattern as Categories' "Empty" check); (b) backend has no dedicated `/activate` endpoint — `activateUser` calls the generic `PATCH /admin/users/:id` with `{ isActive: true }` while `deactivateUser` uses the dedicated `PATCH /admin/users/:id/deactivate`. (2) **`src/pages/Users.jsx`** — swapped local-only state for `useUsers()`; passes `users/loading/error` down to summary stats and table; both Edit and Confirm dialogs conditionally mounted (`{editingUser && (...)}`) so the closure over `_id` can't be created over null. (3) **Child components** — `UsersSummaryStats` and `UsersTable` accept `users` prop and key rows on `_id`; table renders "Loading users…" / error text / "No users found" in the empty row slot for both desktop and mobile card views. (4) **Dialogs** — `CreateUserDialog`, `EditUserDialog`, and `ConfirmUserActionDialog` accept async `onSubmit` / `onConfirm` callbacks, track local `submitting` + `error`, surface backend errors as inline red helper text, and disable controls while in flight; busy labels swap on the primary button (Creating…, Saving…, Deactivating…, etc.). `UserDetailsDialog` drops the mock `lastLogin` field (backend's User schema has no equivalent — only `createdAt`/`updatedAt` from Mongoose timestamps) and shows "Last Updated" instead. (5) **`mockData.js`** — removed the `mockUsers` array (no other importers — confirmed via grep); kept `ROLES`, `roleConfig`, `statusConfig`, `formatDate`, `formatDateTime`, `getInitials` as shared lookup constants. (6) **Backend gaps flagged, not blocking merge:** (a) `GET /admin/users` should return `200 []` on empty instead of `404 { message: "Users Not Found!" }` so the frontend workaround can come out (mirrors the Categories flag); (b) `POST /admin/users` response `data` only echoes `{ name, email, isActive, role }` — no `_id` — so optimistic insert isn't possible; current hook refetches the list, which is fine but worth fixing if/when more endpoints land. `vite build` clean.
- **2026-04-10** — Initial frontend context. Established tech stack, folder structure, design system, auth flow, shared components list, mobile strategy, anti-patterns.
- **2026-04-10** — `feat/login` pushed. Shipped: Login page (`src/pages/Login.jsx`), reusable `LoginInput` component with password visibility toggle, `OauthButton` placeholder for Google/Facebook sign-in, Inter variable font installed via `@fontsource-variable/inter` and set as Tailwind v4 default via `--font-sans` theme token. Controlled form inputs wired with `useState` + `handleChange`; `handleSubmit` stub in place (fetch call pending). **Deviations from plan:** using `react-icons` instead of `lucide-react` (react-icons already installed); using `fetch` instead of `axios` per Adrian's preference.
- **2026-04-13** — `feat/layout-shell` pushed. Shipped: Dashboard wired with two chart widgets — `ChartAreaGradient` and `ChartBarExpense` (plus `ChartAreaInteractive` scaffolded) under `src/components/dashboard-components/`. Added shadcn primitives `card.jsx`, `chart.jsx`, `select.jsx` under `src/components/ui/`. Updated `--chart-*` tokens in `src/index.css` (light theme: blue/red/black brand palette; dark theme: shadcn defaults). Pinned `recharts` to `^3.8.0`.
- **2026-04-13** — `feat/request-form-page` pushed (covers both Request Form and Voucher pages). Shipped: (1) **Request Form page** — `src/pages/RequestForm.jsx` assembled from `src/components/request-form-components/`: `mockData.js` (16 mock RFs across 7 statuses + pipelineStages + statusConfig + formatters), `RfSummaryStats` (4 KPI tiles: Active/Pending Disbursement/Approved/Rejected), `RfPipelineTracker` (horizontal clickable stages that filter the table + separated Rejected stage), `RfTable` (search + status + category filters, state-aware action menu per row status, pagination), `RfDetailsDialog` (full approval timeline with done/current/upcoming/rejected states + rejection note card), `CreateRfDialog` (Save as Draft vs Submit for Validation dual-action footer, multi-URL attachments), `RejectDialog` (shared for validate/approve stages). Page hoists `activeStatus` + `viewingRf` state for cross-component wiring. (2) **Voucher page** — `src/pages/Voucher.jsx` assembled from `src/components/voucher-components/`: `mockData.js` (3 mock vouchers + 4 approved RFs awaiting voucher + voucherStatusConfig), `VoucherSummaryStats` (4 tiles: Total Issued/Pending Receipt/Disbursed/RFs Awaiting Voucher, `h-auto lg:h-80` responsive height), `PendingRfsCard` (clickable grid of approved RFs that launches pre-filled CreateVoucherDialog), `VoucherTable` (search + status + category filters), `VoucherDetailsDialog` (voucher + linked RF summary card + receipts), `CreateVoucherDialog` (controlled-or-uncontrolled modal supporting `preselectedRfId`; Select dropdown keyed on `rfNo` so trigger shows "RF-0008"; file upload dropzone for receipts with multi-file support; optional remarks/notes Textarea). (3) **Shared tweaks** — `src/components/ui/dialog.jsx` DialogTitle default bumped to `text-lg font-semibold`; all modals set `overflow-x-hidden` to kill the horizontal scrollbar caused by DialogFooter's `-mx-4` bleed; `src/components/ui/textarea.jsx` added. `src/components/layout/Header.jsx` brand text updated to "JOSCM Tithes Management System". (4) **Documentation** — new §13 "Established Page Pattern" codifies the page shell, modal pattern, table pattern, summary-stats pattern, mock-data conventions, Select conventions, and absolute rules so future list pages are built consistently without re-deriving the layout.
- **2026-04-13** — `feat/dashboard-stats` pushed. Shipped: (1) Dashboard expansion — `SummaryStats.jsx` (4 KPI tiles: Tithes/Expenses/Net/Pending) and `RecentActivity.jsx` (filterable + paginated activity table with sticky header, type dropdown, 10/page pagination) under `src/components/dashboard-components/`. (2) Full Tithes page build — `src/pages/Tithes.jsx` composed from five new components under `src/components/tithes-components/`: `TithesTrendChart` (area chart with 7D/30D/90D/1Y range presets + service type filter + computed stats footer), `TithesSummary` (4 status KPI tiles), `ServiceTypeBreakdown` (horizontal bar chart, % distribution), `TithesTable` (search + status + service filters, status badges via shadcn Badge, row action dropdown, details dialog showing denominations breakdown, pagination), `SubmitTithesDialog` (modal triggered by custom `Buttons.jsx` component; form with entryDate, serviceType, 9 denomination rows ₱1000–₱1 with auto-computed subtotals + total, remarks). (3) Layout refactor — `Header.jsx` simplified to global chrome only (logo, search, date, icons); per-page greeting/action headers adopted in `Dashboard.jsx` and `Tithes.jsx` so each page owns its own action button (e.g., Tithes owns `SubmitTithesDialog`). Fixed `Dashboard.jsx` outer from `h-full` to `flex-1 min-h-0` so Layout's flex-col correctly allocates remaining space after Header. Added shadcn primitives: `table, dialog, input, label, popover, calendar, dropdown-menu, badge`. **Deviations:** Tithes submit trigger uses the existing custom `Buttons.jsx` (wrapped in clickable div, Dialog controlled via `open` state) per Adrian's request, while Cancel/Submit inside the dialog stay as shadcn Button for `variant`/`type`/`disabled` support.
- **2026-04-15** — `feat/expense` pushed and merged (PR #5). Shipped: full admin-route pages with mock data — `Expense.jsx` (SummaryStats, TrendChart, CategoryBreakdown, Table, Details + RecordExpense dialogs), `Categories.jsx` (SummaryStats, Table, FormDialog, ConfirmActionDialog), `Reports.jsx` (DateRangePicker, ExportBar, PreviewTable, ReportSummary, tithes/expense tabs), `Users.jsx` (SummaryStats, Table, Create/Edit/Details/ConfirmAction dialogs). All follow §13 established page pattern.
- **2026-04-15** — `feat/auth-rbac` pushed and merged (PR #6). Shipped auth + RBAC foundation: (1) **AuthContext primitives** — `src/context/AuthContext.jsx` (user/token/isLoading state, rehydrates from localStorage on mount, login/logout API), `src/hooks/useAuth.js`, `src/routes/ProtectedRoute.jsx` (waits on isLoading to avoid redirect flash, optional `allowedRoles` prop), `src/services/api.js` (fetch wrapper with Authorization header injection and global 401 auto-logout). `main.jsx` wrapped in `<AuthProvider>`. (2) **Routing refactor** — `App.jsx` restructured to nested `<ProtectedRoute>` matching §4: `/login` public, everything else gated; Voucher allowed for admin/do/validator/auditor; Expense for admin/auditor; `/admin/*` for admin only; `*` → `/dashboard` fallback. (3) **RBAC helpers** — `utils/rolePermissions.js` extended with `ROLES`, `ROLE_LABELS`, `MOCK_USERS`, and a `can` matrix that mirrors backend ACLs from `CLAUDE.md` (approveTithes with conflict-of-interest check, validateRf/approveRf/rejectRf, createVoucherFromRf, recordManualExpense, viewExpenseReport, etc.). (4) **Sidebar + SideBarHeader wired to auth** — `Sidebar.jsx` uses `getNavItemsForRole(user.role)` so each role sees only their allowed nav items; `SideBarHeader.jsx` shows `user.name` + `ROLE_LABELS[user.role]` and adds a logout button (clears localStorage → redirects to `/login`); removed the hardcoded "Create User" CTA that didn't belong in global chrome. (5) **In-page gating** — CTAs hidden via `can.*` checks: Create Voucher + Approved-RFs-Awaiting-Voucher card visible to admin/validator only; Record Expense visible to admin only; Reports Expense tab hidden for members (with fallback `effectiveTab` so direct tab prop can't leak). Row-action dropdowns: `RfTable.ActionMenu` now receives `role` + `currentUserName` and filters items per backend ACL (validate/approve/reject/createVoucher), owner-only draft edit/submit/delete and voucher_created receipt confirmation. `TithesTable` approve/reject items honor conflict-of-interest (`submittedBy !== user.name`) and are hidden from members entirely. Route paths `/voucher`, `/expense`, `/admin/users`, `/admin/categories` double-gated via ProtectedRoute even if someone edits the URL directly. **Note:** initial commit replaced `pages/Login.jsx` with the role-picker UI — reverted on `feat/dev-role-picker` (see next entry) since Adrian's original design should be preserved.
- **2026-04-15** — `feat/dev-role-picker` pushed. Shipped: (1) Restored `pages/Login.jsx` to its original email/password + OAuth design (Adrian's work preserved — the role-picker replacement from `feat/auth-rbac` was reverted). (2) New `pages/DevRolePicker.jsx` — standalone dev-only page that lists `MOCK_USERS` as clickable cards; calls `login(mockUser, mock-<role>-token)` and routes to `/dashboard`. (3) `App.jsx` adds public route `/dev-login` pointing to the new component. **Convention going forward:** when dev tooling needs a different UI, add a new component/route rather than overwriting a styled page. Real `/api/auth/login` integration remains a one-line change inside `Login.handleSubmit` when backend wiring starts.
- **2026-04-16** — `feat/mobile-tables` pushed. Branch 2 of 3 in the mobile-responsive rollout. Shipped: mobile card-stack fallback on all 7 data tables so list pages are usable on phones without horizontal scroll. Applied pattern: (1) desktop table wrapped in `<div className="hidden md:block">`; (2) mobile card list in `<div className="md:hidden -mx-4 divide-y border-t">` (the `-mx-4` cancels `CardContent`'s default padding so rows go edge-to-edge); (3) each card shows a primary identifier + 1–2 secondary fields + status badge + row-action dropdown; (4) filter row now stacks with `flex flex-col sm:flex-row sm:flex-wrap`, inputs/selects become `w-full sm:w-X`; (5) footer goes `flex flex-col-reverse sm:flex-row`, verbose "Showing X–Y of N" gated `hidden sm:block`, Prev/Next pair becomes full-width on mobile. Tables refactored: `TithesTable`, `RfTable` (kept existing state-aware `ActionMenu`), `VoucherTable`, `ExpenseTable`, `UsersTable` (two-row layout — avatar+name/email on top, role+status badges on bottom), `CategoriesTable` (color dot next to name on mobile card), `ReportPreviewTable` (dual rendering per `tab` prop — tithes vs expense). Extracted `RowActions` helper inside each file so desktop table rows and mobile cards share one dropdown definition. **Note:** no `RequestSummaryTable` exists — the plan mention was conditional. Login remains untouched.
- **2026-04-16** — `feat/mobile-pages-dialogs` pushed. Branch 3 of 3 in the mobile-responsive rollout — closes the mobile refactor. Shipped: (1) **Page shells** — every `src/pages/*.jsx` (except `Login.jsx`) updated: header rows go `flex flex-col sm:flex-row sm:items-center justify-between gap-3` so title block and CTA stack on mobile; titles drop to `text-xl md:text-2xl`; CTA wrappers `w-40` / `w-44` → `w-full sm:w-40` / `w-full sm:w-44` so primary actions span the full mobile width; data-table containers `h-[32rem]` → `h-[24rem] md:h-[32rem]` for tighter mobile viewports; chart containers `h-96`/`h-80` → `h-72 md:h-96`/`h-72 md:h-80`. Same CTA pattern applied inside the dialog triggers `SubmitTithesDialog` and `CreateRfDialog`. (2) **Dashboard** — `Dashboard.jsx` arbitrary widths replaced: `w-110`/`w-110`/`flex-1 h-98` row → `grid grid-cols-1 lg:grid-cols-3 gap-5` with `min-w-0` children so charts stack on mobile and tablet, three across on desktop; header `text-[25px]` → `text-xl md:text-[25px]`; "Add Category" button `w-33` → `w-full sm:w-36`. (3) **SummaryStats grid** — `RfSummaryStats` aligned with the established pattern: `grid-cols-2 gap-3` → `grid grid-cols-2 lg:grid-cols-4 gap-3` (Categories/Users/Expense/Voucher/Dashboard SummaryStats already conformed). (4) **Form & details dialogs** — internal two-column layouts go `grid grid-cols-2` → `grid grid-cols-1 sm:grid-cols-2` across `SubmitTithesDialog`, `CreateRfDialog`, `CreateVoucherDialog`, `RecordExpenseDialog`, `UserDetailsDialog`, `RfDetailsDialog`, `ExpenseDetailsDialog`, `VoucherDetailsDialog`; full-width sub-rows updated `col-span-2` → `sm:col-span-2` so they match the new mobile single-column flow. `CreateVoucherDialog` and `CreateRfDialog` footers had hardcoded `flex gap-2` overriding the primitive's responsive default — overrides removed so footers inherit `flex flex-col-reverse gap-2 sm:flex-row sm:justify-end` from `dialog.jsx` (primary action sits on top when stacked on mobile). Confirm/Reject dialogs and Create/Edit User + Category dialogs already used single-column forms and the default footer — no changes needed. (5) **Verification** — `vite build` clean (no new warnings); spot-checked at 390/768/1440. **Note:** `Login.jsx` and shared `dialog.jsx` primitive untouched. Mobile rollout complete.
- **2026-04-16** — `fix/dashboard-mobile-recent-activity` pushed. Follow-up to the mobile rollout — Adrian flagged `RecentActivity` invisible on the admin Dashboard at mobile width. Two root causes: (1) `Dashboard.jsx` outer wrapper was missing `overflow-auto` (every other page had it), so when the three chart tiles stacked vertically on mobile they consumed the entire viewport and the trailing `<RecentActivity />` (sized `flex-1 min-h-0`) collapsed to 0px with nowhere to scroll. Added `overflow-auto` and gave the wrapper `h-[28rem] md:flex-1 md:min-h-0` so it has a guaranteed mobile height while keeping its desktop "fill remaining space" behavior. (2) `RecentActivity` was the one data table missed during Branch 2 (`feat/mobile-tables`) because it lives in `dashboard-components/`, not a feature folder — its 7-column desktop table was horizontally scrolling inside `CardContent` on phones. Applied the established Branch 2 mobile card-stack pattern: desktop `<Table>` wrapped in `hidden md:block`; new `md:hidden -mx-4 divide-y border-t` card list per row showing user (font-medium) + action badge on top, `type · ref` subtitle, then role badge + date + amount on the bottom row; CardHeader filter Select goes `w-full sm:w-40` inside `flex flex-col sm:flex-row sm:flex-wrap`; CardFooter pagination matches the rest of the codebase (`flex flex-col-reverse sm:flex-row`, "Showing X–Y" gated `hidden sm:block`, Prev/Next pair `w-full sm:w-auto`). `vite build` clean.
- **2026-04-16** — `feat/wire-login-real-auth` pushed. Closes the last frontend-side auth stub. (1) **`src/pages/Login.jsx`** — `handleSubmit` no longer just `console.log`s; now `await apiFetch('/auth/login', { method: 'POST', body: JSON.stringify({ email, password }) })`, then `login(res.data, res.token)` from `useAuth()` and `navigate('/dashboard', { replace: true })`. Added inline `text-red-600` error `<p>` between the password input and the Login button, plus `disabled` + "Signing in…" label on the submit while the request is in flight. All existing JSX, classes, OAuth section, and footer left untouched per the design-preservation rule. (2) **Latent input bug fixed** — `LoginInput` forwards the lowercase prop as `name` on the `<input>` (so `e.target.name` is `"email"` / `"password"`), but the original `handleChange` compared against `'Email'` / `'Password'`, meaning state never updated and the form was always submitting empty strings. Switched comparison to lowercase. (3) **`src/services/api.js`** — error unwrap now reads `data.message || data.error` so backend payloads shaped `{ error: '...' }` (which is what every controller returns today) surface meaningful messages instead of falling back to bare HTTP statusText. (4) **DevRolePicker** at `/dev-login` left in place as the mock fallback. (5) **Companion backend change still needed**: extend `userLogin` controller's response `data` to include `name` + `email` so the dynamic `Welcome, {firstName}` on Dashboard reads the real name instead of falling back to "there".
- **2026-04-16** — `fix/dashboard-desktop-recent-activity-height` pushed. Three desktop polish fixes bundled after Adrian reviewed the previous mobile-recent-activity fix. (1) **Desktop RecentActivity height** — the previous fix's `md:flex-1 md:min-h-0` didn't actually expand inside the new `overflow-auto` parent (flex children can't claim "remaining space" in a scroll container with no fixed height), so the table felt bitin on desktop. Pinned to explicit `md:h-[36rem]` (576px) — Dashboard slot is now `h-[28rem] md:h-[36rem]`. (2) **Page padding** — `Dashboard.jsx` and `Tithes.jsx` were the only two pages missing `px-1` on the outer `overflow-auto` wrapper, which clipped the left-edge card borders on the children. Added `px-1` to match every other page. (3) **Dynamic welcome** — replaced the hardcoded "Welcome, Admin 👋" with `Welcome, {firstName} 👋` driven by `useAuth().user.name` (split on space; `?? "there"` fallback while not authenticated or while name field is unavailable). **Known gap surfaced for the next branch:** backend's `POST /api/auth/login` response only returns `{ id, role }` in `data` — no `name`/`email` — so this dynamic welcome will fall back to "there" until the backend response is extended (or a separate profile fetch is added). Will be addressed in the upcoming `feat/wire-login-real-auth` branch.
- **2026-04-19** — `fix/login-eye-icon` pushed. Tiny UX bug fix — password visibility toggle icons were swapped. `LoginInput` renders `Icon3` when `type === "password"` (hidden) and `Icon2` when `type === "text"` (visible). Previous wiring passed `icon2={LuEyeClosed}` + `icon3={LuEye}`, producing the inverted behavior (open eye while password is hidden, closed eye while visible). Swapped the two props in `src/pages/Login.jsx` so hidden state shows `LuEyeClosed` (click to reveal) and visible state shows `LuEye` (click to re-hide). No changes to `LoginInput` itself — the component's render logic is correct; only the consumer's prop wiring needed to flip.
- **2026-04-19** — `feat/dashboard-role-quick-actions` pushed. Replaces the single hardcoded "Add Category" button on the Dashboard header with a role-aware quick-actions row. Each action opens the **same dialog** as the one on its feature page (not a page navigation) so the user can complete the action without leaving the dashboard. (1) **`src/pages/Dashboard.jsx`** — defines `ADD_CATEGORY` / `CREATE_VOUCHER` / `SUBMIT_TITHES` / `GENERATE_REPORT` constants and a `QUICK_ACTIONS_BY_ROLE` map: admin → all 4; validator → Create Voucher + Submit Tithes; DO + member → Submit Tithes only; auditor → Generate Report; pastor → none (button block omitted). Uses `useAuth().user.role` plus local `categoryOpen` / `voucherOpen` / `tithesOpen` state; each button's click dispatches to `handleAction(key)` which opens the corresponding dialog. `CategoryFormDialog` / `CreateVoucherDialog` / `SubmitTithesDialog` are all rendered in the page with controlled `open` + `onOpenChange`. (2) **`SubmitTithesDialog` refactored to controlled-or-uncontrolled** — mirrors the `CreateVoucherDialog` pattern: new optional `open` / `onOpenChange` props, internal state used only when not controlled, trigger div (`<CustomButton>`) rendered only when uncontrolled so the Tithes page still gets its own header CTA. (3) **Generate Report still navigates** — the Reports page has no single "generate" dialog; the whole page is the flow (date range picker + tab + export bar), so auditor's + admin's Generate Report button routes to `/reports` instead of opening a modal. Called out explicitly in the `handleAction` comment. (4) **Policy** — role mapping mirrors the backend ACL (`utils/rolePermissions.js#can`) rather than a wish-list. DO (Jaymar) does **not** get Create Voucher on the dashboard — backend `can.createVoucher` is validator+admin only, so the shortcut would produce a 403 on submit. (5) **Post-merge note** — this branch uses the `main`-branch `CategoryFormDialog` signature (no `onSubmit` prop), so Add Category from the dashboard currently `console.log`s on submit. After `feat/categories-real-data` merges, Dashboard should import `useCategories` and pass `onSubmit={createCategory}` to wire the button end-to-end. Tracked as a follow-up; not blocking this branch.
- **2026-04-20** — `feat/dashboard-mobile-button-grid` pushed. Two Dashboard tweaks on `src/pages/Dashboard.jsx`. (1) **Mobile button grid** — quick-actions container changed from `flex flex-col sm:flex-row` stack to `grid sm:flex sm:flex-row` with a conditional `grid-cols-2` when `actions.length > 1` else `grid-cols-1`. Net effect on mobile: admin (4 buttons) displays as a clean 2x2, validator (2 buttons) fits in a single row, DO/member/auditor (1 button) stays full-width. Desktop (sm+) untouched — still `flex-row` with `w-40` per button. (2) **Wire Add Category to real API** — closed the follow-up flagged in the `feat/dashboard-role-quick-actions` update-history entry: `CategoryFormDialog` on the Dashboard now receives `onSubmit` that calls `apiFetch('/admin/categories', { method: 'POST', body })` inline. Deliberately **did not** reuse the `useCategories` hook here — it fetches the list on mount (wasted call since Dashboard doesn't render categories) and would trigger a 403 for non-admin users who can still mount the component. `CreateVoucherDialog` + `SubmitTithesDialog` remain mock for now; Voucher wiring blocked on the upcoming backend Cloudinary work for receipt uploads.
- **2026-04-19** — `feat/categories-real-data` pushed. First page wired to real backend data as part of the post-auth rollout (see `Real Data Rollout` memory). (1) **New `src/hooks/useCategories.js`** — owns the fetch (`GET /admin/categories`), `createCategory` / `updateCategory` / `deleteCategory` mutations that refetch on success, plus `loading` / `error` state. Workaround in place: backend's `GET` returns 404 `{ error: "Empty" }` when no categories exist (`backend/src/controllers/admin/categoryController.js:8`) — hook catches `"Empty"` and treats it as `[]`. (2) **`src/pages/Categories.jsx`** — swapped local dialog state for `useCategories()`; passes `categories` down to `CategoriesSummaryStats` and `CategoriesTable`; edit dialog now conditionally mounted behind `{editingCategory && (...)}` so the `onSubmit` lambda's `editingCategory._id` closure can't be created over null (fixed a "Cannot read properties of null" crash when opening the page). (3) **Child components** — `CategoriesTable` accepts `categories/loading/error` props, keys rows on `_id`, drops the "Usage" column + `usageCount` delete-guard (backend has no usage aggregation yet), renders "Loading categories…" / error text / "No categories found" in the empty row slot; `CategoriesSummaryStats` computes stats from prop; `CategoryFormDialog` and `ConfirmCategoryActionDialog` accept async `onSubmit` / `onConfirm`, track local `submitting` + `error`, and surface backend errors as red helper text near the primary button (same pattern as Login). (4) **`mockData.js`** — removed the `mockCategories` array (other features have their own local mockCategories, confirmed no cross-imports); kept `TYPES`, `typeConfig`, `statusConfig`, `COLOR_PALETTE`, `formatDate` as shared lookup constants. (5) **Backend gaps flagged, not blocking merge:** (a) `GET /admin/categories` should return `200 []` on empty instead of `404 { error: "Empty" }` so the frontend workaround can come out; (b) `PATCH /admin/categories/:id` only destructures `{ name, type, color }` — `isActive` is silently dropped, so Archive/Restore requests will 200 but not change anything until the controller accepts `isActive`. (6) **Pattern codification:** this branch proves the shape for the remaining real-data rollouts (Users → Tithes → RF → Voucher → Expense) — hook per feature, page as data owner, dialogs with async `on*` callbacks that surface errors inline.
- **2026-04-16** — `feat/mobile-layout-shell` pushed. Branch 1 of 3 in the mobile-responsive rollout. Shipped: (1) **shadcn `Sheet` primitive** added at `src/components/ui/sheet.jsx` (base-ui Dialog-based, left/right/top/bottom sides, used for the mobile nav drawer). (2) **`Layout.jsx`** — removed hardcoded `w-[95%] h-[95%]` in favor of `w-full h-full md:w-[95%] md:h-[95%]` so the shell goes full-bleed on mobile and keeps the framed desktop look at `md:`+. Hoisted `sidebarOpen` state and passed it to `Header` (trigger) and `MobileSidebar` (content). Reduced gap/padding on mobile (`gap-4 md:gap-8`, `p-3 md:p-5`). (3) **`Sidebar.jsx`** — extracted reusable `SidebarBody` (logo/user header + role-filtered nav links) so desktop and mobile share one source of truth. Default export `Sidebar` is now `hidden md:flex` (desktop fixed `w-96` column preserved). New named export `MobileSidebar` wraps `SidebarBody` in `<Sheet side="left">` with `showCloseButton={false}` and an `sr-only` SheetTitle for a11y; nav clicks auto-close the drawer via `onOpenChange(false)`. (4) **`Header.jsx`** — added `FiMenu` hamburger (`md:hidden`) that triggers `onOpenSidebar`. Brand text collapses to "JOSCM Tithes" on `<sm` screens via paired `hidden sm:inline` / `sm:hidden` spans. Search bar hidden on `<sm` and replaced with a `CiSearch` icon button; on `sm+` the search expands `w-48 lg:w-70`. Share icon and full date label hidden on narrow screens (`hidden sm:block` / `hidden lg:block`). Outer flex gaps reduced on mobile (`gap-3 md:gap-5`). (5) **Verification** — tested at 390px/768px/1440px via Chrome DevTools; no horizontal scroll on mobile; desktop experience unchanged. **Note:** `Login.jsx` intentionally untouched (already mobile-friendly, preserved per design rule).

---

*See also: [`CLAUDE.md`](./CLAUDE.md) for shared domain knowledge and workflow rules.*

- **2026-06-09** — `fix/mobile-table-row-visibility` pushed. On mobile the feature-page data tables only showed ~1 data row vs the dashboard Recent Activity's ~3. Root cause: those tables' card headers stack a search input + 1–2 filter Selects full-width on mobile (`flex flex-col`), eating ~18rem of the `h-[24rem]` wrapper and leaving room for ~1 row; Recent Activity has just one Select so its `h-[28rem]` shows ~3. Fix: bumped the **mobile** height of all six feature-table wrappers `h-[24rem]` → `h-[34rem]` (desktop `md:h-[32rem]` unchanged — desktop lays filters in a row and was already fine) so ~3 rows show after the stacked filters. Pages touched: `Tithes.jsx`, `RequestForm.jsx`, `Voucher.jsx`, `Expense.jsx`, `Categories.jsx`, `Users.jsx`. Pure layout, no logic. vite build clean. Verify exact row count on a real phone.

- **2026-06-09** — `fix/rf-timeline-rejected-and-approved-total` pushed. Two UX fixes. (1) `RfDetailsDialog.jsx` Approval Timeline for **rejected** RFs: previously every stage showed a green check because `stageState` compared against the rejected status order (99), so `order <= 99` marked all stages done even ones that never happened (no timestamp). Now a rejected RF renders only the stages it actually reached, then the Rejected node. Furthest stage inferred from the latest recorded timestamp (`reachedOrder = max order with a populated timestamp`, floored at `submitted` since a rejected RF was at least submitted); `visibleStages` filters to `order <= reachedOrder` (contiguous — a missing intermediate timestamp won't punch a gap). e.g. validated-then-rejected → Created, Submitted, Validated, Rejected (Approved/Voucher/Disbursed/Received dropped). `stageState` simplified (rejected branch removed). (2) `TithesTrendChart.jsx` now filters the series to `status === "approved"` (was: all non-rejected, i.e. pending + approved) so the footer Total — and the whole single-series trend line, Daily Avg, Best Day — reflect **approved** tithes only (realized income). vite build + lint clean.

- **2026-06-09** — `chore/lint-cleanup-safe` pushed. Behavior-neutral lint pass (25 → 11 problems). `eslint.config.js`: added `argsIgnorePattern: '^[A-Z_]'` to `no-unused-vars` — the 11 `'Icon' is defined but never used` errors were false positives (renamed component args `{ icon: Icon }` ARE used in JSX, but core no-unused-vars doesn't count JSX refs). Removed dead `rfBySetStatus` helper in `PendingWorkSection.jsx`. `AuthContext.jsx`: moved `updateUser` above the boot `useEffect` that calls it (cleared the TDZ "cannot access before declared" lint error; not a runtime bug — the call was inside a deferred `.then`). `vite.config.js`: derive `__dirname` via `fileURLToPath(import.meta.url)` (ESM-correct; `no-undef` cleared). vite build + lint clean of the targeted rules. Remaining 11 (set-state-in-effect ×2, react-refresh only-export-components ×5, exhaustive-deps ×2) deliberately left as pre-existing/dev-only.

- **2026-06-04** — `feat/role-scoped-tables-frontend` pushed. Consumes the backend role-scoped tables change. `useTithes` + `useDashboardData` now expose `chartData` (church-wide, anonymized tithes) alongside the role-scoped `tithes` list. Tithes page charts (`TithesTrendChart`/`TithesSummary`/`ServiceTypeBreakdown`) and Dashboard `ChartAreaGradient`/`SummaryStats` read `chartData` so every role still sees church-wide totals; `TithesTable` and `PendingWorkSection`/activity read the scoped `tithes`. Dashboard Recent Activity is filtered to own-actions for non-oversight roles (admin/pastor/auditor see the full feed); `RecentActivity` gained a `scope` prop for the "Your latest actions" vs "across all accounts" subtitle. `rolePermissions.approveTithes`/`rejectTithes` tightened to DO+admin (not self) to mirror the backend. Charts needed NO component edits — they only read entryDate/serviceType/total/status. vite build + eslint clean. NOTE: deploy with the backend together.

- **2026-06-04** — `feat/global-search-frontend` pushed. Wires the previously-dead header search to the new `GET /api/search` (RF + Voucher; matches RF-/PCF- numbers + remarks). NEW `src/hooks/useGlobalSearch.js` (debounced 300ms, min-2-char, stale-response guard). NEW `src/components/layout/SearchResultsDropdown.jsx` (exports `SearchResultRow` + dropdown; mirrors NotificationsBell popover styling; RF/PCF type badges). NEW `src/pages/Search.jsx` + `/search` route in `App.jsx` (all roles; reads `?q=`, grouped RF/Voucher sections, limit 50). `Header.jsx` search input now controlled with a click-outside dropdown; result click → `navigate(route+'?focus='+focusId)` deep-link (RF + Voucher pages already consume `?focus=`); Enter or "View all" → `/search?q=`; mobile search icon → `/search`. Role-scoping fully enforced server-side (members get RF-own only, no vouchers). vite build + eslint clean. NOTE: backend `feat/global-search` must be deployed first/together.

- **2026-06-04** — `feat/rf-comments-frontend` pushed. Adds the comment thread UI to the Request Form details dialog (consumes backend `feat/rf-comments`). NEW `src/hooks/useRfComments.js` (fetches `/request-form/:id/comments` only while the dialog is `enabled`/open; `addComment` POSTs then refetches). NEW `src/components/request-form-components/RfComments.jsx` (comment list with author name + role badge + relative time, plus a gated textarea+Post composer; reuses `formatRelativeTime` from notificationsUtils). `RfDetailsDialog.jsx` renders `<RfComments>` after the Approval Timeline; composer gated to `userRole !== 'member' || isOwner` (mirrors backend workflow-participant rule). vite build + eslint clean. NOTE: backend `feat/rf-comments` must be merged/deployed.

- **2026-06-04** — `feat/audit-log-frontend` pushed. Admin/auditor Audit Log viewer (consumes backend `feat/audit-log`). NEW `src/hooks/useAuditLog.js` (server-paginated, refetch on page/filter change, stale-guard). NEW `src/pages/AuditLog.jsx` (mirrors Notifications layout: Resource + Action filter Selects, desktop Table [When/Who+role badge/Action/Target/Summary] + mobile divide-y list, server-total pagination). `/audit-log` route added to the `["admin","auditor"]` ProtectedRoute group in `App.jsx`; new `NAV_ITEMS` entry (LuHistory, roles admin+auditor) in `rolePermissions.js`. vite build + eslint clean. NOTE: backend `feat/audit-log` must be merged/deployed.

- **2026-06-04** — `perf/page-data-cache` pushed. Fixes the "data reloads on every page navigation" complaint with a lightweight SWR-style cache. NEW `src/hooks/useCachedResource.js` (module-level `Map` keyed by resource; `loading` true only on first-ever load; on revisit renders cached instantly + revalidates silently in background; silent failures keep cached data; fetcher held in a ref to avoid effect loops; exported `clearResourceCache()`). Refactored the 6 page data hooks (`useTithes`, `useRequestForms`, `useVouchers`, `useExpenses`, `useCategories`, `useDashboardData`) to use it — public return shapes unchanged, so no page/component edits needed; mutations + `notification:new`/`focus` listeners still refetch (silently). `AuthContext.jsx` calls `clearResourceCache()` on login + logout so a new user never sees the previous user's role-scoped data. NOT cached: search/comments/audit-log (on-demand). vite build clean; the 2 eslint errors in AuthContext.jsx (context export + setState-in-effect) are pre-existing, not from this change.

- **2026-06-04** — `fix/rf-details-two-column` pushed. RfDetailsDialog was too wide/tall once comments were added, so on desktop it's now a 2-column layout: DialogContent widened to `lg:max-w-4xl`; body wrapped in `grid grid-cols-1 lg:grid-cols-2 gap-6` — left column = details + attachments + approval timeline, right column = comments (`lg:border-l lg:pl-6`). Stacks to one column on mobile. Header + footer span full width. No logic change. vite build + eslint clean.

- **2026-06-04** — `feat/web-push-frontend` pushed. PWA + Web Push UI (consumes backend `feat/web-push`). NEW `public/sw.js` (service worker: `push` → showNotification with JOSCM logo icon; `notificationclick` → focus/navigate existing window or openWindow to the `?focus=` url). NEW `public/manifest.webmanifest` (installable PWA; icons via Cloudinary JOSCM logo resized to 192/512). `index.html` links manifest + theme-color + apple-touch-icon. `src/main.jsx` registers `/sw.js`. NEW `src/lib/push.js` (pushSupported/getPushStatus/enablePush/disablePush; VAPID via `VITE_VAPID_PUBLIC_KEY`; POST/DELETE `/push/subscribe`). NEW `src/components/layout/PushToggle.jsx` ("Enable notifications" / "on · turn off" / "blocked") added to the Header settings popover; hidden if unsupported. `.env.example` documents `VITE_VAPID_PUBLIC_KEY` (local key in gitignored `.env.local`). vite build + eslint clean; manifest/sw copied to dist. NOTE: needs `VITE_VAPID_PUBLIC_KEY` in Vercel + VAPID env on Render; iOS push needs Add-to-Home-Screen. Push delivery verified only on a real device.

- **2026-06-04** — `fix/same-origin-api-proxy` pushed. Fixes prod login bouncing back to /login on Safari AND Chrome: cross-site auth cookies (Vercel FE ↔ Render BE different domains) are blocked by Safari ITP and Chrome's 3rd-party-cookie phase-out. Fix routes the API + socket through the SAME origin via Vercel rewrites so cookies are first-party. `vercel.json`: rewrites `/api/:path*` and `/socket.io/:path*` → Render backend (before the SPA catch-all). `socket.js`: when `VITE_API_URL` is relative ("/api"), connect to `window.location.origin` with `transports:["polling"]` (Vercel proxies HTTP long-polling, not raw WS) → realtime preserved with first-party cookies. REQUIRES setting `VITE_API_URL=/api` (relative) in Vercel env (was the full onrender URL). No backend change. Local dev unchanged (VITE_API_URL still absolute localhost → direct ws).

- **2026-06-04** — `fix/push-toggle-error-feedback` pushed. PushToggle now surfaces success/failure via toasts (notifySuccess/notifyInfo/notifyError) instead of silently swallowing errors — so when push subscribe fails (e.g. on a specific Android device) the real reason (permission denied, subscribe DOMException, etc.) is shown. Diagnostic aid for the Android "toggle doesn't flip to Notifications on" issue.

- **2026-06-04** — `fix/realtime-notification-polling` pushed. In-app realtime was dead in prod: the websocket can't be proxied through Vercel (`/socket.io` returns the SPA index.html, not the engine.io handshake), and a direct cross-origin socket loses the auth cookie. Fix: `socket.js` `connectSocket()` returns null when proxied (relative VITE_API_URL) so it stops the futile socket attempts; `NotificationsContext` adds a 10s visibility-aware polling fallback that refreshes the bell AND re-dispatches the same `notification:new` window event the socket used — so list pages + the dashboard "Your Pending Work" update too (~10s, no reopen needed). Dev (absolute URL) still uses the websocket. The 1 eslint error in NotificationsContext (context export) is pre-existing, not from this change.
